# 🏠 Home & Garden — PROJECT STATE (Single Source of Truth)

**Last consolidated:** May 29, 2026
**Purpose:** One file that combines everything from both Home Garden Schedule chats. Read this first at the start of any new chat — it carries the full project context so nothing has to be re-explained.

> ✅ This file replaces the need to keep multiple chats open. Keep ONE chat going; this doc is the memory.

---

## 1. The Project

Organize and transform every room of the house into a purposeful space supporting hobbies, work-from-home life, and wellbeing.

- **Timeline:** May 13 – Aug 9, 2026
- **Main organization push completes:** ~June 28, 2026
- **🎉 Celebration milestone:** First full-house vacuum (~June 28)

---

## 2. ⚠️ Standing Rules (always follow)

1. **Don't take control of the computer without asking first.**
2. **Ask clarifying questions before any heavy task. Only act when I say "Go."**
3. **Only deploy live when I say "GO LIVE."**
4. **Deploy only via the bash sandbox** (`python3 deploy-github.py`) — never via computer use / File Explorer.
5. **Warn me before a chat gets too full**, with directions to continue without disrupting work.
6. **Don't reinvent the wheel** — check existing files/chats before redoing something.
7. **When I reference something you don't remember, pull it from this doc / past chats** instead of asking me to re-send.
8. **HTML hygiene:** never use unescaped inch marks (write "inches"); syntax-check braces; no duplicate `const`.
9. Avoid the multiple-choice question tool (it glitches) — ask in plain text.
10. **"Update schedule" rule:** whenever Cyrus says "update schedule" / "update my schedule" / similar (in ANY chat), write a full handoff into Section 10 below — what changed in `index.html`, why, exact location, and safe deploy steps — and stage the `index.html` edit. This is how changes from other chats reach this (the deploy) chat. Deploy still only on "GO LIVE".
11. **One editor for the app:** ONLY this (Home/Garden) chat edits and deploys `index.html`. Topic chats (Aquarium, Recipes, 3D Printing, etc.) decide WHAT should change and hand it off here — they must NOT edit `index.html` directly. Editing the ~250KB file through the OneDrive House mount truncates/corrupts it and parallel chats collide (this caused real breakage on 6/1). This chat can pull a topic chat's work straight from its session (session-reader tools) and is the sole editor/deployer.
12. **Auto-clear checked items:** once Cyrus checks something off in the app, it should be removed the next day (or at the next "GO LIVE" deploy, whichever comes first) so completed items don't linger. Recurring/daily items still reappear on their schedule; rollover tasks already disappear once checked. (Added Jun 10 2026.)
13. **ALWAYS PRESERVE CYRUS'S CHANGES — every deploy is non-destructive (he asked for this explicitly, Jun 10 2026).** Before ANY deploy: (a) pull/clone fresh from the latest committed HEAD and re-apply edits onto it — NEVER deploy from a stale copy, so newer committed work is never overwritten; (b) deploys push CODE only (index.html / data.js / recipes.js / sw.js) and must NEVER wipe live Firebase state — anything Cyrus did in the app (checkmarks, added rollover tasks, water / streak / reminder / habit progress, in-app edits) is preserved through the deploy; (c) every data.js seed / default / migration MUST be gated behind a one-time dated guard flag so it runs once and never re-overwrites what he changed; (d) if a change lives at the CODE level, read the current committed value and preserve it before editing. If a deploy would risk losing ANY of his changes, STOP and flag it to him instead of overwriting.

---

## 3. The App / Schedule Tool — Deployment

**CURRENT hosting: GitHub Pages.** (History: Netlify → Cloudflare → GitHub. The first two are dead.)

- **Live URL:** https://mrosyrus21.github.io/home
- **Repo:** https://github.com/mrosyrus21/home

### SOURCE OF TRUTH = the GitHub repo
The canonical, version-history backup is the **GitHub repo** (every change is a commit). As of May 29 the House-folder `index.html` was **re-synced down from the repo**, so it now matches the deployed app exactly (158 KB, ~2,109 lines: Today / Plants / Tank / List / Print / Health tabs; Plants has Watering / Harvest / Care sub-tabs; Rooms + Timeline are header pills).

**Deploy method (what works):**
1. `git clone https://mrosyrus21:<token>@github.com/mrosyrus21/home.git` into a scratch dir. The token is NO LONGER stored in the repo — `deploy-github.py` now reads it from the `GH_TOKEN` environment variable. Cyrus holds the current token.
2. Edit the cloned `index.html`.
3. `node --check` the inline JS before pushing.
4. `git commit` + `git push origin main`. GitHub Pages serves it.

**OneDrive gotcha:** the House folder is OneDrive "Files On-Demand", so the bash sandbox sometimes only *reads* a truncated prefix of a large file. Writes (cp) into the folder work fully. Always edit from the freshly cloned repo copy, then sync the result into the House folder.

**Files in House folder root (cleaned May 29 — only these remain):**
- `index.html` — current deployed schedule app (synced to GitHub)
- `deploy-github.py` — the deploy script (git add/commit/push of index.html)
- `DEPLOY.bat` — Windows one-click launcher that runs deploy-github.py
- `home-and-garden-project/` — this doc, README, docs (plant inventory, nicotine plan), and reference plant photos
- `images/` — the May 29 plant photos Cyrus uploaded

*(Removed as junk: old app versions, Cloudflare/Netlify deploy experiments, zips, logs, empty .bat files, a broken half-initialized .git.)*

**The app itself:** vanilla HTML/CSS/JS, dark mode. Tabs: Today / Plants / Tank / List / Print / Health. Plants sub-tabs: 💧 Watering, 🌾 Harvest, ❤️ Care. State syncs via Firebase.

### ⚠️ Two open cleanup items
- **Stale artifact redirect:** the Cowork "Home Garden Schedule" artifact (`OneDrive\Documents\Claude\Artifacts\home-garden-schedule\index.html`) still points to the OLD Netlify URL. Needs repointing to the github.io URL above. *(Awaiting "Go".)*
- **Security (May 30, resolved):** the old hardcoded token was scrubbed from `deploy-github.py` (now reads `GH_TOKEN` env var). Repo history was checked — no token was ever committed to the repo.

---

## 4. Schedule Constraints (task capacity)

- **Monday:** 0 tasks (pool league)
- **Wednesday:** max 1 task (7pm commitment)
- **Weekdays:** max 2 tasks
- **Weekends:** max 3 tasks
- **Daily, non-negotiable (pinned red):** Call DT before noon.
- Cyrus works from home 8am–4pm.

---

## 5. Room-by-Room Goals

| Room | Goal | Status / Notes |
|------|------|----------------|
| 🏠 Garage | Workshop + tool organization | Every tool gets a home |
| 🛏️ Bedroom | Calm sleep; 3D printer relocated OUT to office | — |
| 💻 Office | WFH + music studio; drum kit + 3D printer IN | **Blocked:** needs a sturdy desk first |
| 🍳 Kitchen | Clutter-free counters, organized pantry | — |
| 🌿 Living Room | Aquarium (target June 5) + hydroponics | See aquarium project |
| 🌞 Backyard | Seating, BBQ, weeds managed, string lights | Summer hangout |
| 🚿 Bathroom | Deep clean, organized, fresh | — |

---

## 6. Key Milestones

**Schedule rebaselined May 29, 2026** — Cyrus hadn't started yet, so the whole plan restarts fresh. Gentle, **kitchen-first**, easy wins first, one room finished per week (mini-celebrations) to avoid overwhelm. 5/29 + 5/30 are rest/prep days (Sat morning = breakfast + dog); the journey begins **Sunday 5/31**.

| Date | Milestone |
|------|-----------|
| May 31, 2026 | 🚀 Day 1 — kitchen begins |
| ~June 7 | 🎉 Kitchen done |
| ~June 14 | 🎉 Living room done |
| ~June 19 | 🎉 Bathroom done |
| ~June 25 | 🎉 Bedroom done |
| ~July 2 | 🎉 Office studio done |
| ~July 5 | 🎉 Garage done |
| ~July 9 | 🎉 Backyard done |
| **July 10, 2026** | 🎉🎉 **First full-house vacuum** — the big celebration |
| Then bi-weekly | Maintenance vacuums (7/24, 8/7, 8/21) |

Aquarium is already set up (see Tank tab master log) — no longer a pending milestone.

---

## 7. Aquarium Project (Living Room)

- LECA prepared May 13 (rinsed, soaked, boiled ×2)
- Prusament PETG needed for the aquarium lid print (3D printer: Bambu/Prusa-style, bi-metal hotend)
- May 29 schedule items include `aq_buy_livestock` and `aq_drip_acclimate`
- Full LECA prep steps + livestock list tracked in the schedule app

---

## 8. Plants — see `docs/plant_inventory.md`

Full inventory + watering cheat sheet lives in `docs/plant_inventory.md`. **Last refreshed from photos: May 29, 2026** (greenhouse + outdoor). Headlines:

- **Heirloom Tomato:** large green beefsteaks set; some catfacing → keep watering consistent, needs a sturdier cage.
- **Husky Cherry Tomato:** small green cherries forming, healthy.
- **Jalapeño:** recovered, strong new growth.
- **Strawberry:** flowering + first fruit, healthy.
- **Basil ×2, Parsley, Rosemary, Dill, Raspberry:** all healthy.
- **Dianthus:** full bloom, needs deadheading. **Candytuft:** bloom finishing, shear back. **Daisy:** still stressed, likely drainage — consider repotting.
- **🌱 Peppermint:** RECOVERING (Jun 6) — new growth from the cut-back stems; HOLD lifted, keep evenly moist. Photo refreshed in-app.
- **Indoor 4 (Philodendron, Royal Tea Ivy, Fittonia, Jade):** NOT re-shot May 29; still on fungus-gnat dry-out protocol. Send pics to refresh.

**Active garden rules:** indoor plants on gnat dry-out protocol (water only when bone dry, empty saucers); greenhouse watered mornings only.

---

## 9. Other Docs

- `docs/nicotine_quit_plan.md` — personal gradual-taper quit plan.
- `README.md` — original project overview (May 23).

---

## 10. Open Threads / To-Do for Next Chat

### 🤝 HANDOFF — for the next chat (as of Jun 5, 2026)

**What this app is:** single-page Home & Garden dashboard (`index.html` ~2,600 lines + `data.js` for data), Firebase Realtime DB for state, hosted on GitHub Pages at **mrosyrus21.github.io/home** (repo github.com/mrosyrus21/home). It's a PWA (installable + offline).

**Current live commit:** `3be37cb` (SW cache at hg-v77). Always `git fetch`/reset to origin/main before doing anything — the repo is VERY actively edited by parallel chats (Aquarium, Recipes, etc.).

**DEPLOY PROCESS (this chat is the deployer; only deploy on "go live"/"GO LIVE"):**
1. Fresh clone or `git reset --hard origin/main` (NEVER edit the big files via the OneDrive House mount directly — it has truncated/corrupted them before).
2. Make edits with python string-replace (unique anchors). Keep data in `data.js`, app logic in `index.html`.
3. Validate: `node --check data.js` AND extract the big inline `<script>` → `node --check`. Render-test changed functions where possible.
4. Bump `sw.js` CACHE (hg-vN+1) and the `data.js?v=` query (timestamp) so clients refresh. Restamp `const LAST_DEPLOY` (America/Denver).
5. Token: read `home-and-garden-project/.deploy-token` (`tr -d ' \t\r\n'`), push with `https://mrosyrus21:TOKEN@github.com/...`. ALWAYS mask `github_pat_*`/`ghp_*` in output. Use a fetch+rebase retry loop (parallel chats cause push rejections).
6. Mirror `index.html`/`data.js`/`sw.js` to the House folder after pushing.
7. Verify live in Chrome (load + check console + exercise tabs). Chrome extension drops a lot — retry.

**KEY RULES / PREFERENCES (memory):** Be concise (what changed, commit, done). Ask before heavy tasks unless told "Go". Keep the app CURRENT — prune completed Do-Nows/old-dated panels (daily scheduled task `garden-staleness-check` 9:05am flags these). Schedule HTML: no unescaped inch marks in double-quoted strings; syntax-check before deploy. Warn when chat is filling up.

**MULTI-CHAT COORDINATION (important, recurring pain):** Aquarium/Recipes chats edit the same files. They sometimes mirror to House but don't push (House can be ahead, sometimes TRUNCATED — check `tail -c 16 index.html` ends with </html> and `node --check` before trusting/deploying a House copy). When their content conflicts with prior decisions (e.g. re-introducing scratched katydid/spider), reconcile onto a clean origin/main clone rather than deploying their copy verbatim.

**RECENT STATE (done & live):** PWA install/offline; plant photos fixed (fittonia, croton, husky cherry, philodendron); Health tab = meds (Litfulo + magnesium + esomeprazole) + Get-Outside guide (sleep-setup/pain-log/doctor-notes REMOVED); harvest = condition-based for strawberries/tomatoes, others dropped; pain-log Firebase sync; weather-driven watering; Litfulo refill countdown; evening check-in + auto-backup scheduled tasks; data.js refactor; Kitchen Inventory seeded (v1 + v2 items); Food tab (List/Recipes/Inventory subtabs); Tank rebuilt into PER-TANK tabs (Blue Dream cube / Tall / Wide / Dry terrarium) with history at bottom; bug project = ladybugs in bioactive tub + garden (spider+katydid SCRATCHED); fly feeder removed; June-2 shrimp panels refreshed to June-4; **Recipe Box** on Food→Recipes (cute recipe-box UI I built — kraft box + red gingham + cream index cards; the Recipes chat then renamed it “Cy’s Recipe Box”, added a custom title font, and now owns the recipe content + dish photos).

**OPEN / TODO:**
- TANK PHOTOS: the per-tank tabs have no real photos. The tank shots are NOT reachable from the deployer chat (uploads folder = plant photos only). They live in the Aquarium chat — get them uploaded to the working chat or have Aquarium wire them.
- Recipes chat is filling the Recipe Box; new recipes need a dish photo each (Wikimedia Commons FilePath, closest match) — the box auto-renders them as index cards.
- Tank dimensions (saved): tall 11×16×11 in (~8.4 gal), wide 19×11×10 in (~9 gal), Blue Dream cube 11.2×11.4×11.1 in (3.7 gal), dry/rounded terrarium 10×12×7 in (~3.6 gal), divided cube 15×10×7 in (~4.5 gal).
- Junk to delete (couldn't from sandbox): House/_*.jpg contact sheets, _uploads_contact.jpg, _app_photos_check.jpg, _icon_preview.png, plants/_archive_old/*.


### ✅ LIVE — 🪴🌱 REPOT DAY pivot + GRACEFUL TIME-AWARE PACK (Jun 12 2026, commit `fb0719b..3be37cb`, ONE commit [shared w/ FLIP PACK + FINANCE FIX below], SW **hg-v76→v77**, data.js `?v=20260612190000`, stamp "Jun 12 · 1:23 PM")
**Deployed on "go live."** TWO of the four packs in commit `3be37cb` (the other two are the chats' staged packs below, shipped in the same commit/SW bump):

**🪴 REPOT DAY (Cyrus's call — replaces the 4-week BTI war):** Sun **Jun 14** task `gnat_nuke` relabeled to REPOT DAY — all 5 indoor plants SAME day, supply run in the label (potting mix · cactus mix · perlite · orchid bark · coco coir), TIME_META dur 1.5–2 hrs. `PEST_PLAN[0]` rewritten to the repot-first doctrine (bare-root, scrub pots+saucers, sealed-bag mix only, same pot size, gravel cap, old soil out/solarized, traps ~2 wks, BTI first 2–3 waterings) + **NEW `PEST_PLAN[1]` "Repot mixes — custom soil per plant"** (jade cactus+perlite 1:1; philo/croton 2:1:1 aroid; ivy 3:1; fittonia 2:1:½ w/ coco — expect ~4–5 day watering after, retune on request). Jun 21 `gnat_sand` → post-repot insurance (BTI any due pot + trap swap, 15 min); Jun 28 `gnat_check2` → victory check. All 10 BTI war-footing lines across `WATER_INFO.when` + `PLANTS.freq` (5 indoor plants ×2) → repot pointer + post-repot insurance wording; fittonia carries the new-mix cadence note. The 3 gnat ids added to `NO_PROGRESS`.

**🌱 GRACEFUL PACK (he loved "window passed — a snack still counts"; asked for more):** (1) per-meal `passed` lines — MEAL_ANCHORS got a `passed:` field + the shared `mealAnchorHtml` fallback now reads `A.passed||classic`: lunch "a late lunch still resets the afternoon", dinner "late and light beats nothing 🌙", breakfast keeps the classic. (2) **Call DT past noon** (undone card): "Noon's passed — call anyway. A late call beats a broken streak." (fresh `new Date()` — `nowM` is TDZ-unsafe at that point in `renderToday`). (3) **Zoey walk** after noon: timeLabel→"Anytime" + "an afternoon or evening sniffy walk counts double"; after 6pm: "Evening walk = her favorite anyway — cooler pavement, all the day's smells 🐾". (4) **Garden care strip** after noon: timeLabel→"Early evening" + "early evening (5–7) is the next best slot: leaves dry before dark, no midday scorch" (respects the mornings-only greenhouse rule). (5) **Water tile** after 6pm: 0 logged → "one bottle with dinner still counts — start the count now 💧"; partial → "Evening top-up — N to go." **Pattern note for future chats: time-aware grace lives at render time only (`nowM`/`new Date().getHours()`), zero state writes — keep adding moments like these, he explicitly loves them.**

**Verified (whole commit as a unit):** node --check ×5 surfaces post-restamp; jsdom real-path render at **Jun 13 09:30 / 15:00 / 20:30 + Jun 14 noon** — 9+6+5+12 case asserts ALL PASS (right text at the right hour, no past-noon text in the morning, water default before 6pm) + the full 30-assert regression suite re-PASSED; SET_CALLS=2 every run == baseline (0 new Firebase writes — Rule 13 clean). **Verified origin `main` == `3be37cb`; House mirror sha256-identical ×3 (index `f288c080…`, data `f40c8a2f…`, sw `c28bab4b…`), ends `</html>`.** Live HTTP check not possible from sandbox (precedent); SW bump forces refresh. ⚠️ Hand-delete when convenient: `staged_flip_pack_jun12/`, `staged_finance_fix_jun12/`, `staged_tv_triage_jun12/` (all shipped/superseded; sandbox can't delete from the House mount).

### ✅ LIVE (in `3be37cb`, same commit as above) — was: 🟡 STAGED — 📺🔍 FLIP PACK: TV-triage + flip-leads reminders + daily morning flip-scan card (Jun 12 2026, from the FB Marketplace flipping chat) — deployed exactly as staged (4/4 edits, anchors matched once each); jsdom-verified: card+5 links Jun 13 & 14, leads Jun 13 only, Timeline row, tv-triage standing
**What/why:** Cyrus's fix-and-flip side hustle is spinning up (free 50-inch TV in hand; Denver Craigslist research done Jun 12). He asked for all of it on his app schedule. THREE pieces, FOUR edits, ONE deploy: **(1)** 📺 standing reminder — after-work TV triage (persists until checked once; thyme-plant pattern). **(2)** 🎯 dated reminder **Jun 13** — the four $0 Denver leads spotted Jun 12 (dated, not standing — leads churn in hours and self-expire; fine if deploy slips past Jun 13, they'd be dead anyway). **(3)** 🔍 **daily** morning "flip scan" card ~7:15 AM with five tappable Denver Craigslist searches — date-keyed daily (moneycheck/DMV pattern: clears at midnight, returns every morning) + a Timeline row, links open in new tabs without toggling the card (`event.stopPropagation()`, DMV-extra pattern). Sources: `FB_Marketplace_Flipping_Playbook.md` + `TV_Flip_Fix_Plan.md` in his "Organizing Information and Future Projects" folder.

**TWO files changed — exact code staged verbatim in `home-and-garden-project/staged_flip_pack_jun12/` (`data_js_edits.js` + `index_html_edits.js`), anchors summarized here:**
- **EDIT 1 (`data.js`):** extend `REMINDERS` — string-replace on the unique thyme-plant anchor at the array's end; adds `rem_tv_triage` (standing) + `rem_flip_leads` (date 2026-06-13). Existing dated+standing renderer picks both up automatically.
- **EDIT 2 (`data.js`):** new `FLIP_SCAN` config const (label, "~7:15 AM", 5 `[label,url]` Denver Craigslist links, speed-tip line) inserted immediately after the `BURN_CARE` const's closing `};`, before `const FINANCE`.
- **EDIT 3 (`index.html`):** flip-scan render block in `renderToday()` — inserted after the 🔥 BURN CARE block's closing brace, before the `// 🔔 dated + standing reminders` comment (≈ line 1714). Order **284** (top of the pre-8am reminder cluster, just above 285 moneycheck; does NOT outrank Call DT). Done-state `laundry["rem-flipscan-"+today]` via `toggleReminder('flipscan',true)`; active card = `reminderCardHtml` with links in `extra`.
- **EDIT 4 (`index.html`):** one Timeline line — `tlRem` push for today gated on `!reminderDone('flipscan',true)`, inserted directly after the existing moneycheck `tlRem` push (≈ line 2008).

**Verified (Jun 12, sandbox node):** all four edits `node --check` clean and behavior-tested as a unit — **11/11 asserts PASS**: card renders at order 284 with all 5 links; links carry `stopPropagation` (tap a link ≠ check the card); done-state flips to the ✓ strip and suppresses the Timeline row; date-keying returns it next day; Jun 12 render shows tv_triage but NOT flip_leads, Jun 13 shows both; **no unescaped inch marks** anywhere ("50-inch"/"42-inch" spelled out per the standing rule); not money reminders → no `account` fields. **Non-destructive:** pure config + render-time code, zero Firebase seeds/writes (state only written when Cyrus taps).

**Safe deploy steps (standard):** fresh clone / `git reset --hard origin/main` (House mirror NOT to be trusted as source) → apply the four edits from `staged_flip_pack_jun12/` → `node --check data.js` + extract both `index.html` inline scripts → `node --check` → jsdom render-test Today pinned Jun 13 + Jun 14 if feasible (flip-scan card present both days, leads card Jun 13 only) → bump `sw.js` CACHE hg-v(N+1) + `data.js?v=` timestamp + restamp `LAST_DEPLOY` → push with the `.deploy-token` (mask it) → mirror to House → verify live. Built against live commit `fb0719b` (SW hg-v76); if main has moved, re-apply on current origin/main — the thyme-plant / BURN_CARE / moneycheck anchors are stable.

**After it ships:** Cyrus runs the TV triage and reports the model number + symptom in the FB Marketplace chat (it finds the exact part + teardown video next). The 7:15 flip-scan card is the app-side twin of the 7:00 AM `daily-flip-deal-hunt` chat briefing. The one-time 5:30 PM background ping `tv-triage-afterwork` stays DISABLED (his rule: reminders live in the app, not background pings).


### ✅ LIVE (in `3be37cb`, same commit as above) — was: 🟡 STAGED — 💰 FINANCE FIX: Xfinity leverage line AT&T→CenturyLink, 5 spots in data.js (Jun 12 2026, from the Financial Stability chat) — deployed: 5/5 anchors applied, zero "Internet Air" remains in data.js, Finance tab render shows CenturyLink (jsdom-verified)
**What/why:** The live app still coaches the WRONG leverage line for the Xfinity retention call — "AT&T Internet Air is $47" — in 5 places. That line was superseded Jun 8: AT&T Internet Air is 5G wireless (weak lever); the real published wired-competitor price is **CenturyLink Simply Unlimited ~$55/mo** (he's NOT switching — pure negotiation). His call script (`Financial Stability/Xfinity Call Script.html`) and `FINANCE_STATE.md` §3 already say CenturyLink; the app contradicts them, and the call is queued as a Financial Monday (`mon_xfinity`) — he'd walk into the call with the wrong script. ONE file changes (`data.js`), five string replacements, no index.html edit needed.

**Exact edits — runnable script at `home-and-garden-project/staged_finance_fix_jun12/apply_finance_fix.py`** (applies all 5; aborts file-untouched unless every anchor matches exactly once; fails if any "AT&T Internet Air" remains). ⚠️ Read the .py host-side (Read tool) or sanity-check `wc -c` ≥ 2300 and that it ends with `main()` — the sandbox mount served a TRUNCATED view of this file right after it was written (the known OneDrive issue). The 5 anchors, all unique in `data.js`:
1. TASKS `xfinity_call` note (~line 57): `Leverage: AT&T Internet Air is only $47/mo and there's no fiber at your address — you're a flight risk they'll want to keep.` → `Leverage: CenturyLink Simply Unlimited is ~$55/mo at your address (real published price — you're NOT switching, it's pure negotiation). You're a flight risk they'll want to keep.`
2. TASKS `xfinity_call` steps[] (same object): `Drop the leverage: 'AT&T Internet Air is $47/mo. I'd rather stay for the lower latency, but not at this price.'` → `Drop the leverage: 'CenturyLink Simply Unlimited is $55 a month at my address. I'd rather stay for the lower latency, but not at this price.'`
3. FINANCE.todosSeed `ft_xfinity` label (~572): `(AT&T Internet Air is $47)` → `(CenturyLink is ~$55)`
4. FINANCE.mondays `mon_xfinity` note (~613): `Leverage: AT&T Internet Air is $47/mo at your address.` → `Leverage: CenturyLink Simply Unlimited is ~$55/mo at your address — you're not switching, it's the price-match lever.`
5. FINANCE.advice "magic words" card (~640, escaped quotes are literal file bytes): `\"AT&T Internet Air is $47 at my address.\" That is the leverage line.` → `\"CenturyLink Simply Unlimited is $55 a month at my address.\" That is the leverage line.`

**Verified (Jun 12, sandbox node):** script run on a copy of the live `data.js` (fb0719b House mirror): 5/5 applied → `node --check` PASS → zero `Internet Air` mentions remain, CenturyLink present on all 4 affected lines (line 57 carries two of the five). Pure display text — no Firebase seeds/writes, no render-logic change, no new state keys.

**Safe deploy steps (standard):** fresh clone / `git reset --hard origin/main` (House mirror NOT the source) → run `python3 apply_finance_fix.py data.js` (or hand-apply the 5 pairs above — this entry is authoritative if the .py is unreadable) → `node --check data.js` → bump `sw.js` CACHE hg-v(N+1) + `data.js?v=` timestamp + restamp `LAST_DEPLOY` → push with `.deploy-token` (mask it) → mirror to House → verify live. Built against `fb0719b`; the anchors are stable text and survive if main has moved. **Deployment waits for "GO LIVE" — never push before Cyrus says it.**

**Also done outside the app (no deploy needed):** `FINANCE_STATE.md` synced to the live FINANCE block on Jun 12 — family loan $9,300→**$10,000** (incl. +$700 Zelle borrowed Jun 6 for rent), Savor card added, Farmer's Dog ($215.05, cancel ~6 wks out) + Car Wash + PlayStation Plus added to the cancel ledger, and the **Jun 15–16 ≈$428 autopay cluster before the Thu Jun 18 payday** flagged. Heads-up that several money reminders (daily 8am check-in; Jun 14/17 Game Pass; Jun 18 Disney+ cancel) are DISABLED in scheduled tasks — Cyrus is deciding on re-enabling; the Jun 11 Car Wash/Crunchyroll verify never fired and is overdue.


### ✅ LIVE — 🌾 Harvest → pick-when-you-cook + 🦟 GNAT WAR v2 (Jun 12 2026, commit `01741a0..fb0719b`, ONE commit, SW **hg-v75→v76**, data.js `?v=20260612153000`, stamp "Jun 12 · 9:37 AM")
**Deployed on "go live."** Cyrus's feedback: harvest cadence was way too aggressive (only rosemary ever picked + basil trimmed; nothing else eaten) and the fungus gnats survived the dry-out + Mosquito Bits round.

**🌾 HARVEST (data.js + harvest renderer in index.html):** All five herb HARVEST_INFO entries (basil1, basil2, rosemary, parsley, dill) converted to **ongoing on-demand cards** — no more permanent "Ready now" calendar nags (removing their `date` also drops them from the ✂️ tracker, since `hOpen` excludes ongoing). New optional per-card fields **`badge` / `hdrNote` / `signsLabel`** with renderer support added in the harvest sub-tab (`h.badge||"🌿 Pick when ripe"` etc. — future entries can use them). Chips rewritten: parsley/rosemary "pick as you cook — no schedule", dill "pick fronds as you cook · REQUIRED: snip flower heads on sight", basil "REQUIRED: pinch every 1–2 weeks — keeps it producing", mint "once back: pick as you cook". **Required-for-the-plant items kept and labeled as such:** basil pinch (trimDays 12, badge "✂️ Pinch 1–2 wks — required") and dill flower-bud patrol (trimDays 7). Tending cadences eased where they were disguised harvests: parsley trimDays 14→**21** (+ trim text now separates plant-health tending from cooking picks), rosemary 14→**30** (shaping only). **Every herb card now carries a keep-fresh / freeze / dry preservation guide** in `fact` (rosemary dries best — bundle/jar, or freeze sprigs; parsley fridge-bouquet ~2 wks or oil/water cubes; dill water cubes + paper-bag seed harvest; basil counter-glass/pesto-cubes guide was already in). Philosophy banner added at the top of the Harvest sub-tab. Strawberry/tomato/jalapeño/raspberry ripeness cards untouched.

**🦟 GNAT WAR v2:** Doctrine added as the FIRST entry of `PEST_PLAN` (index.html, Plants→Care) — why round 1 failed (BTI only stays active ~48 hrs and a one-off tea can't outlast the 3–4-week egg→adult cycle; dry topsoil just pushes larvae toward the moist root core) + the v2 stack (BTI tea at EVERY watering ×4 weeks, sticky traps laid FLAT on the soil, one-time 1:4 hydrogen-peroxide drench, half-inch sand/aquarium-gravel caps on every pot, saucer/drain hygiene, repot any pot still hatching after 4 weeks). The BTI-tea-every-watering line was appended to **both** `WATER_INFO.when` AND `PLANTS.freq` for all 5 indoor plants (watering cards display `wi.when||p.freq` — when wins, so both needed). Three dated garden tasks added (TASKS + SCHEDULE rows + TIME_META order 432): **gnat_nuke Jun 14** (peroxide drench + flat traps), **gnat_sand Jun 21** (gravel caps + BTI week 2), **gnat_check2 Jun 28** (trap check week 3) — all Sundays, each row went 2→3 tasks, inside the weekend cap of 3.

**Non-destructive (Rule 13):** pure config/render text — no seeds, no migrations; jsdom SET_CALLS identical to a baseline `01741a0` control run (2, both from pre-existing autoCarry on the empty test snapshot) = **0 new Firebase writes**; his trimmed/harvested/watered state keys unchanged. **Verified:** `node --check` clean on data.js, sw.js, recipes.js + both index.html inline blocks (re-checked post-restamp); jsdom real-path render (firebase stub → `ST.on('value')` → `showApp()` → `renderAll()`) pinned **Jun 14, Jun 21, Jun 28**: every tab no-crash, 23/23 asserts (gnat tasks render on their dates, banner + new badges present, all old cadence chips gone, zero "Ready now" nags, strawberry ripeness intact, BTI lines on Care+Watering). `restamp.py` external-source stamp, 46s skew. **Verified origin `main` == `fb0719b`; House mirror sha256-identical ×3 (index.html `c780ef63…`, data.js `e4d29386…`, sw.js `23f62d69…`), House index ends `</html>`.** Live HTTP byte-confirm not possible from sandbox (github.io fetch times out — precedent); SW v75→v76 bump forces client refresh.

### ✅ LIVE — 💰 Daily pre-8am "Check money situation" reminder (Today + Timeline, taps to Finance) (Jun 11 2026, commit `7acf168..01741a0`, ONE commit [shared w/ the color-contrast fix], SW **hg-v74→v75**, stamp "Jun 11 · 7:58 AM")
**Deployed on "GO LIVE."** New daily morning reminder in `renderToday()` (`index.html`): a dedicated date-keyed block placed just before the breakfast anchor — **order 285**, i.e. in the pre-8am reminder cluster above Call DT and ahead of the 8:30 breakfast card. Card is gold **#FACC15** "🔔 Reminder · ~7:45 AM / 💰 Check money situation" with a one-line nudge. **Tapping the card body → `switchTab('finance')`** (jumps to the 💰 Finance tab); tapping the circle checks it off via `toggleReminder('moneycheck',true)`. **DAILY + date-keyed:** done-state lives at `laundry["rem-moneycheck-"+todayKey()]`, so it clears at midnight and returns each morning (same mechanism as 🌱 Water-Mom / 🔥 Burn-care). Also surfaced on the **Timeline** (today's row) via a `tlRem` push gated on `!reminderDone('moneycheck',true)`. **Non-destructive:** pure render-time code — adds ZERO Firebase seeds/writes (state is only written when Cyrus taps), so no guard flag is needed and nothing of his is overwritten. *(If it should be one-time rather than daily, say so and I'll gate it.)*

### ✅ LIVE — 🎨 Adjacent-card color-contrast fix (app-wide audit) (Jun 11 2026, commit `7acf168..01741a0`, ONE commit [shared w/ the money reminder], SW **hg-v74→v75**, stamp "Jun 11 · 7:58 AM") — supersedes the Jun 11 OPEN handoff
**Deployed on "GO LIVE" — the OPEN item is done.** Problem: too many Today cards/reminders reused the SAME accent and sat right next to each other, so neighbors blended and got missed. Fix: a reusable adjacency pass **`_dedupeAdjColors(items)`** (+ an `_accentOf` extractor + in-scheme `_ADJ_PALETTE`) added next to `strip()` in `index.html`. It walks a rendered list and, wherever an item shares its left-border accent with the item just before it, nudges THAT item to the next palette color that differs from both neighbors — staying inside the existing scheme (adjacency contrast, not a redesign). Wired into `renderToday()` over the **`active` and `pile`** flow arrays (the post-sort pass the handoff pointed at). **Whole-app audit (per the spec):** the real same-color-adjacent blending was all in the Today flow (the reminder cluster all #38BDF8, the rollovers all #F59E0B, plus the Wednesday Call-DT/TG double-red) — now fixed. The other list renderers were audited and intentionally **NOT** recolored: 🌿 Garden watering/harvest cards and 💰 Finance up/down figures encode color as **semantic status** (green = good, red = needs-water / down) — recoloring would corrupt meaning; 🛋️ Rooms and 🖨️ Print use per-room / per-vibe **identity** colors that are already distinct between neighbors. So the fix lives exactly where the reported problem is, without breaking semantic color elsewhere. **Verified via real jsdom render** (full Firebase-snapshot → showApp → renderAll path, dates pinned **Jun 10 [Wed, TG showing] + Jun 11**): every tab renders with **no crash**, **zero adjacent same-color cards** across the Today active+pile sequences, the money card renders pre-breakfast (active idx 1 vs breakfast idx 10) and on the Timeline, and **0 Firebase writes during render** (non-destructive). `node --check` clean on index.html / data.js / recipes.js / recipes.html / sw.js. **Rule #13 non-destructive deploy:** the original base `f09153e` went stale mid-build — a new commit **`7acf168`** (the 9 DRAFT recipes, SW→v74) landed on origin first; I re-applied my edits onto `7acf168` and bumped **v74→v75** rather than overwriting it, so Cyrus's recipe work is fully preserved. **Verified: origin `main` == `01741a0` + House mirror sha256-identical** — index.html `f12b8336…`, sw.js `fd067344…`, recipes.js `45990ae0…`, recipes.html `3c76171d…`, data.js `5a590f36…`.

### ✅ LIVE — 🌿 Refined harvest logic: basil weekly–biweekly pinch + full technique/preservation; fruiting plants ripeness-based (Jun 11 2026, commit `c8f7430..f09153e`, ONE commit, SW **hg-v71→v72**, data.js `?v=20260610130000`, stamp "Jun 11 · 3:52 AM")
**Deployed on "GO LIVE" — supersedes the interim basil/strawberry spacing from `c8f7430`.** All in `data.js` (config + HARVEST_INFO), surfaced on the 🌾 Harvest sub-tab. **BASIL (basil1+basil2):** cadence eased to **weekly–biweekly** (`trimDays` 10→**12**); `harvestChip` → "✂️ pinch ~every 1–2 weeks · cut above a leaf pair"; HARVEST_INFO `how` rewritten with the healthiest-plant technique (start ~6–8 in/several leaf sets; **cut/pinch JUST ABOVE a leaf pair/node** so it branches into two; harvest top-down, **never >~⅓** then recover; **pinch flower buds** the moment they appear or leaves turn bitter; light harvest ~every 1–2 weeks, more once bushy under ⅓); `fact` replaced with **PRESERVATION** guidance (short-term: stems in a glass ~1 in water on the **COUNTER, never the fridge** — cold blackens basil — loosely tented, ~1 week; longer: **freeze in olive-oil ice cubes or as pesto**, blanch whole leaves 2s+ice first; drying loses most flavor; frozen-in-oil/pesto keeps best). **FRUITING plants → ripeness-based, not calendar (removed the "every N days" nags):** 🍓 **Strawberry** (+pot) `harvestChip` "pick only when fully red — light right now" + HARVEST_INFO `how` "only a few ripening at a time, occasional pick, take only fully red"; 🍅 **Tomato** "still green — none yet; pick when fully colored & slightly soft"; 🌶️ **Jalapeño** "just flowering — peppers later; pick firm & full-size (green)"; 🍇 **Raspberry** "next year — not fruiting this season". (Cherry tomato + other plants untouched; fruiting plants' `trimDays`/care cadence untouched — only harvest guidance changed.) **Non-destructive:** pure config/text edits, no Firebase writes, his `trimmed`/`harvested` state preserved. **Verified via real jsdom render assert** (Today regression: no crash + burn-care + Water Mom evening still present; Harvest tab: all basil technique/preservation + every fruiting ripeness note render; old fixed-calendar cadences absent). `node --check` clean all surfaces; `restamp.py` 11s skew. **Verified: origin `main` == `f09153e` + House mirror sha256-identical (index.html `695ab1f7…`, data.js `5a590f36…`, sw.js `8755eab7…`).** Live HTTP byte-confirm not possible (`web_fetch` times out on github.io); SW bump forces refresh.

### ✅ LIVE — 🔥 Burn care + 🌱 Water Mom's plants → evening + 🌿 harvest spacing (Jun 10 2026, commit `78c8fbf..c8f7430`, ONE commit, SW **hg-v70→v71**, data.js `?v=20260610120000`, stamp "Jun 11 · 3:47 AM")
**Deployed on "GO LIVE."** THREE changes shipped together in ONE commit on a fresh clone of origin `78c8fbf`: **(1)** 🌱 "Water Mom's plants" retimed from a morning daily item (order 352) to a **daily EVENING task ~8 PM after dinner** (order 1255, label "~8:00 PM"), still date-keyed (`rem-watermom-`+today). **(2)** 🔥 **Burn care** daily reminder (the Health chat's staged BURN_CARE block — config const in data.js after `TG_MEETING`, render block in `renderToday()` after the DMV block): daily Jun 10→23, infection-check sub-text Jun 12–13, one-shot 🩹 silicone-gel card from Jun 24; reuses existing helpers only, Today-tab only, no SCHEDULE/capacity changes. **(3)** 🌿 interim basil/strawberry harvest spacing **— SUPERSEDED by the refined herb/ripeness logic in the next commit below.** Verified via real jsdom load-path render assert pinned **Jun 10, Jun 12, Jun 24** (all PASS: no crash; Water Mom in the evening slot after Dinner & before bedtime; burn-care card Jun 10; infection sub-text Jun 12; silicone card Jun 24). `node --check` clean all surfaces; `restamp.py` 9s skew. **Verified: origin `main` == `c8f7430` + House mirror sha256-identical (index.html `45b803f5…`, data.js `929dcb21…`, sw.js `46440935…`).** Live HTTP byte-confirm not possible (`web_fetch` times out on github.io); SW bump forces client refresh. (The Health chat's burn-care staged edits in the OneDrive working copy are now superseded — shipped.)

**TWO files changed — exact locations + exact code to re-apply on a clean clone:**

1. **`data.js`** — new config const inserted **immediately after `const TG_MEETING = {…};`** (right before `const FINANCE = {`):
```js
const BURN_CARE = {
  start:"2026-06-10",                           // first day of the daily care nag
  end:"2026-06-23",                             // last day of the daily nag (~2 weeks of healing)
  endLabel:"Jun 23",
  infectionDates:["2026-06-12","2026-06-13"],   // early infection-check days (day 2–3)
  siliconeDate:"2026-06-24"                     // from here on, nudge the silicone-gel switch until checked once
};
```

2. **`index.html`** — render block inserted in **`renderToday()`**, immediately **after the 🚙 DMV daily-reminder block** (the `if(typeof DMV!=='undefined'){…}` that ends `…toggleReminder('dmv',true)",extra)); } }`) and **before** the `// 🔔 dated + standing reminders` comment (≈ line 1666):
```js
  // ── 🔥 BURN CARE — daily nag through the care window (date-keyed → reappears each day), then a one-shot
  // silicone-gel switch once healed. Config in BURN_CARE (data.js). (added Jun 10 2026) ──
  if(typeof BURN_CARE!=='undefined'){
    if(today>=BURN_CARE.start && today<=BURN_CARE.end){
      const bcDone=!!laundry["rem-burncare-"+today];
      const inInf=(BURN_CARE.infectionDates||[]).indexOf(today)>=0;
      const bcSub=inInf
        ? "👀 Infection check today — spreading redness, thick/cloudy pus, warmth, more pain or fever → urgent care. Otherwise: keep it covered, don't pop it, thin Vaseline under a clean non-stick dressing."
        : "Keep it covered for sleep, don't pop it, thin Vaseline under a clean non-stick dressing. Clear/yellow seep is normal — sleep on your back.";
      add("burncare",286,bcDone, bcDone
        ? strip("🔥","Burn care — done for today",true,"#FB7185","toggleReminder('burncare',true)","Daily","","Reappears tomorrow through "+BURN_CARE.endLabel)
        : strip("🔥","Burn care — protect the forehead burn",false,"#FB7185","toggleReminder('burncare',true)","Daily","",bcSub));
    } else if(today>=BURN_CARE.siliconeDate){
      const siDone=reminderDone('burnsilicone',false);
      add("burnsilicone",286,siDone, siDone
        ? strip("🩹","Silicone scar gel — started",true,"#A78BFA","toggleReminder('burnsilicone')","","","Keep it up ~12 hrs/day for 2–3 months + sun protection")
        : reminderCardHtml("🩹","Burn healed & skin fully closed? Start silicone scar gel (ScarAway or bioCorneum) ~12 hrs/day for 2–3 months, plus sun protection.","","toggleReminder('burnsilicone')",""));
    }
  }
```

**What it does (behavior):**
- **Daily** 🔥 "Burn care" card on the **Today** tab, **Jun 10 → Jun 23**. Date-keyed (`laundry["rem-burncare-"+todayKey()]`) so checking it clears only today and it **reappears each morning** — same mechanism as the DMV / "Water Mom's plants" daily reminders.
- On **Jun 12 & 13** the sub-text swaps to an **infection-check** prompt (the milestone within the window).
- From **Jun 24** onward, the daily nag retires and a **one-shot** 🩹 "switch to silicone scar gel" card shows until checked once (`laundry["rem-burnsilicone"]`).
- Reuses existing helpers only (`strip`, `reminderCardHtml`, `reminderDone`, `toggleReminder`, `add`) — **no new functions, no SCHEDULE/TASKS/capacity changes**, so it can't collide with the room-cleaning capacity rules.

**Why this design (not SCHEDULE rows):** a daily health nag with a date range + auto-milestones is the DMV-style daily-reminder pattern, not a one-off dated task; putting it in `SCHEDULE` would have fought the capacity rules and off-days. Today-tab only (like DMV) — if Cyrus later wants it on the Timeline too, that's a follow-up.

**⚠️ Verification still owed at deploy (I could NOT run it here):** I confirmed helper signatures + read the staged edits back, but I did **not** run `node --check` or the jsdom real-path render harness, because the Linux sandbox can't reliably read the full 401 KB `index.html` (the OneDrive >140 KB truncation). **The deploy chat MUST, on a clean clone:** `node --check` all surfaces (data.js · recipes.js · sw.js · both index inline blocks) **and** run the jsdom `showApp()→renderAll()→renderToday()` assert with the clock pinned to Wed Jun 10 2026 — assert (a) app doesn't crash, (b) Today output contains "Burn care — protect the forehead burn", (c) checking `rem-burncare` clears it for today only. Then also sanity-check a pinned Jun 12 (infection sub-text) and a pinned Jun 24 (silicone card).

**SAFE DEPLOY STEPS on GO LIVE:** fresh `git clone` of origin → **re-apply the two code blocks above in the Linux sandbox (NEVER trust the OneDrive mount — it truncates >140 KB)** → `node --check` all surfaces → jsdom real-path render assert (Today, pinned Jun 10 + spot-check Jun 12 & Jun 24) → bump `sw.js` **hg-v70 → hg-v71** → bump the `data.js` cache-bust `?v=` in `index.html` (data.js changed) → `python3 restamp.py` → **ONE** `git commit` + `git push origin main` (token from `home-and-garden-project/.deploy-token`) → mirror `index.html`+`data.js`+`sw.js` into House sha256-identical → verify origin `main` == new HEAD + mirror sha256 → flip this entry to ✅ LIVE with the commit hash.

### ✅ LIVE — 🪲 Dry Terrarium update (ladybugs out · 🕷️ jumping spider in · more basil+strawberry) (Jun 10 2026, commit `f6d54d5..78c8fbf`, ONE commit, SW **hg-v69→v70**, stamp "Jun 10 · 11:50 AM")
**Deployed on "GO LIVE."** The staged patch (`staged_dry_terrarium_jun10/dry_terrarium_jun10.patch`) was applied onto a fresh clone of origin `f6d54d5` via `git apply` (clean, one hunk +9/−3, index.html only), re-verified, SW bumped hg-v69→**hg-v70**, externally restamped (54s skew), ONE commit pushed `f6d54d5..78c8fbf`. **Verified: origin `main` == `78c8fbf` + House mirror sha256-identical (index.html `efc296c1…`, sw.js `6ff4069e…`).** Re-ran the jsdom real-path render assert on the applied patch — all PASS (no crash; jumping spider + size ref + ladybugs removed + basil/strawberry + inhabitants panel + Jun 10 date + katydid kept + stale ladybug text gone). Live HTTP byte/200 not confirmable from here (`mcp__workspace__web_fetch` times out on github.io); SW bump forces client refresh. The `staged_dry_terrarium_jun10/` folder is now superseded (shipped) — safe to delete.

**WHAT CHANGED in `index.html` — exact location:** `renderShopping()` Tank-tab function, inside `else if (tankSub === "dry")` (the Dry Terrarium section, one diff hunk at ≈ lines 2417–2441; +9/−3):
- **Status banner** (was `'Status — Stocked & settled (updated Jun 6)'` / "1 stubborn ladybug left"): re-dated **"(updated Jun 10)"**, title now **"🕷️ Jumping spider added · 🐞 ladybugs all gone"**, body says all ladybugs released (none left), a **small jumping spider (smaller than the katydid nymph)** joined, and **more basil + more strawberry cuttings** added to the greens; fake-plant molting perch + almond leaves/water kept.
- **NEW panel "🐾 Current inhabitants & greens (Jun 10)"** inserted right after the banner (before `const dDaily`): rows for 🦗 **Katydid nymph** (resident, kept), 🕷️ **Jumping spider** (new — small, *smaller than the katydid nymph*), 🪲 **Cleanup crew** (springtails + isopods), 🐞 **Ladybugs — removed, all released to the garden**, 🌿 **Greens — more basil + more strawberry cuttings (Jun 10)**.
- **Daily-care "leaves" item:** "Refresh the food: basil + sugar-water cotton balls + soaked raisins" → **"Refresh the greens: basil + strawberry cuttings + sugar-water cotton balls + soaked raisins"**.
- **Setup note:** removed "🐞 1 stubborn ladybug still inside…" → now **"🐞 Ladybugs all removed — released to the garden. 🕷️ Small jumping spider added (smaller than the katydid nymph)."** (katydid-enclosure note kept).
- **Untouched:** katydid nymph + all other inhabitants/plants, other tanks, all logic/styling (reused existing `banner`/`panel`/`kv`/`tdaily` helpers).

**Why:** Cyrus removed the ladybugs and added a small jumping spider + more basil/strawberry to the dry terrarium; the section still showed the Jun 6 "1 stubborn ladybug" status.

**Where the staged edit lives:** patch at `home-and-garden-project/staged_dry_terrarium_jun10/dry_terrarium_jun10.patch` (+ README) — applies cleanly onto a fresh clone of origin `f6d54d5` with `git apply`.

**VERIFICATION already done (standing rule):** real load path in jsdom (Firebase snapshot → `showApp()` → `renderAll()`, clock pinned Wed Jun 10 2026), then rendered the Dry tank view. Assertions — **PASS app does NOT crash on startup; PASS "small jumping spider"; PASS "smaller than the katydid nymph"; PASS ladybugs "removed — all released to the garden"; PASS "more basil + more strawberry cuttings"; PASS "strawberry cuttings" in greens/daily-care; PASS "Current inhabitants" panel; PASS banner "(updated Jun 10)"; PASS "🦗 Katydid nymph" kept; PASS stale "1 stubborn ladybug" text removed.** `node --check` clean on all surfaces (data.js · recipes.js · sw.js · both index inline blocks). *(At deploy: SW bumped hg-v69→v70 + externally restamped — done.)*

**SAFE DEPLOY STEPS on GO LIVE:** fresh `git clone` of origin → `git apply home-and-garden-project/staged_dry_terrarium_jun10/dry_terrarium_jun10.patch` (or re-apply the edits above) → `node --check` all surfaces → jsdom real-path render assert (dry view) → bump `sw.js` **hg-v69 → hg-v70** → `python3 restamp.py` (external Denver time) → **ONE** `git commit` + `git push origin main` (token from `home-and-garden-project/.deploy-token`) → mirror index.html+sw.js into House sha256-identical → verify origin `main` == new HEAD + mirror sha256 → flip this entry to ✅ LIVE with the commit hash. (Heads-up: `mcp__workspace__web_fetch` times out on github.io, so live byte-confirm isn't possible from here — the SW bump forces client refresh.)

### ✅ LIVE — 🦐 Daily ammonia chore softened → quick "glance at the always-on alert card" (Jun 10 2026, commit `61cf576..f6d54d5`, ONE commit, SW **hg-v68→v69**, stamp "Jun 10 · 11:09 AM")
**"Update schedule" + GO LIVE.** Cyrus approved softening the daily ammonia check now that the permanent always-on alert card is installed — no more strip-test-and-log chore, just a quick visual confirmation.

**TWO changes:**
1. **App (`index.html`) — exact location:** `renderShopping()` Tank-tab function, inside `if (tankSub === "bluedream")`, inserted right **after the "⚗️ Right Now (June 10)" panel and before the "🎯 Neocaridina Targets" panel** (≈ line 2344). Added a **daily-care panel** mirroring the dry-terrarium pattern: `panel('🧼 Daily care — N of 1 today', tdaily('bdamm',"👀 Glance at the ammonia alert card — confirm it's still showing safe / 0 ppm") + italic note "…a 5-second look — not a strip test. Resets every morning")`. Uses the existing `tdaily()` helper + `toggleTankDaily()`, date-keyed `laundry['tankd-bdamm-'+todayKey()]` so it **resets daily** like the Today items. No logic/styling changes — reused existing helpers. **No other tanks touched.**
2. **Background scheduled task `aquarium-ammonia-daily-check`** (C:\Users\cyrus\Claude\Scheduled\…, daily 8:08 AM) — reworded its prompt + description from "test ammonia/nitrite with the API kit + 30–40% water change until the cycle recovers" to a **5-second glance at the always-on card (confirm SAFE/0)**, with the full water-change/Prime protocol kept ONLY as a fallback IF the card ever shows color. Daily cron unchanged. (Updated via the scheduled-tasks tool, not a file in this repo.)

**MANDATORY real-execution verification (standing rule):** ran the full real load path in jsdom (Firebase snapshot → `showApp()` → `renderAll()`, clock pinned Wed Jun 10 2026), then rendered the Blue Dream tank via the real `renderAll`. Assertions — **(a) PASS app does NOT crash on startup; (b) PASS Today view rendered (30,626 chars); (c) PASS reworded "Glance at the ammonia alert card" item renders; (d) PASS it's inside a daily-care panel (resets daily).** `node --check` clean on all surfaces (data.js · recipes.js · sw.js · both index inline blocks) before & after restamp. `restamp.py` clean (25s skew).

**Safe deploy steps (repeatable):** fresh clone of origin → edit `index.html` in the Linux sandbox (NEVER the OneDrive mount, truncates >140KB) → `node --check` all surfaces → jsdom real-path render assert → bump `sw.js` (hg-v68→**hg-v69**) → `python3 restamp.py` → ONE `git commit` + `git push origin main` (token from `home-and-garden-project/.deploy-token`) → mirror index.html+sw.js into House sha256-identical. **Verified: origin `main` == `f6d54d5` + House mirror sha256-identical (index.html `a59c2d00…`, sw.js `bef355bd…`).** Live HTTP byte/200 confirm not possible from here — `mcp__workspace__web_fetch` times out on github.io every time; SW bump hg-v68→hg-v69 force-busts stale caches, Pages auto-builds from origin. **(Cyrus was separately verifying the served page in Chrome this round.)**

### ✅ LIVE — 🦐 Blue Dream tank: ammonia 0 ppm + permanent always-on ammonia alert card installed (Jun 10 2026, commit `bbe3485..61cf576`, ONE commit, SW **hg-v67→v68**, stamp "Jun 10 · 10:56 AM")
**"Update schedule" + GO LIVE.** Cyrus installed a **permanent always-on Seachem-style ammonia alert card** (continuous monitor, not a one-off strip) in the Blue Dream shrimp cube; it currently reads **0 ppm (safe)**. The tank section was still showing the 6/4 "recovering from a 1.0 ammonia spike" status, so it was stale.

**WHAT CHANGED in `index.html` — exact location:** all inside the `renderShopping()` Tank-tab function, `if (tankSub === "bluedream")` block (≈ lines 2334–2380):
- **Header date** (shared tank header): "Last updated June 5, 2026" → **"June 10, 2026"**.
- **Status banner** (was amber `245,184,48` "Status — June 4 ⚠️→✅" recovering): now **green `74,212,144` "Status — June 10 ✅ · Always-on monitor"**, body states the permanent Seachem-style always-on ammonia alert card is installed and reads **0 ppm ammonia (safe)**, spike fully resolved, shrimp active/eating.
- **Param chip row:** NH3 chip `1.0`/warn → **`0`/ok (green)**.
- **"⚗️ Right Now" panel:** "(June 4) … down to 1.0 and holding" → **"(June 10) … Ammonia 0 ppm — safe"**, notes the always-on card replaces one-off strips.
- **"🎯 Neocaridina Targets" Ammonia row:** `0 ppm (1.0 — recovering)` → **`0 ppm (0 ✅ — always-on alert card)`**.
- **"🧰 Equipment On Hand" → Testing:** prepended **"permanent always-on ammonia alert card (Seachem-style, installed Jun 10)"**.
- **"📋 Full Parameter Log":** added row **`6/10 · NH3 0 · NO2 0 · NO3 5 · pH 7.6 · Hard 125 · Alk 74 · "Ammonia back to 0 ✅ — permanent always-on ammonia alert card installed (continuous monitor)"`**.
- **NOT touched** (per scope): the daily ammonia-check task, other tanks (Tall/Wide/Dry), all other tank logic/styling.

**Why no logic change:** purely content/status in the render string; styling helpers (`banner`/`panel`/`chip`/`kv`) reused unchanged.

**MANDATORY real-execution verification (standing rule):** ran the full real load path in jsdom (Firebase snapshot → `showApp()` → `renderAll()`), clock pinned Wed Jun 10 2026, then rendered the Blue Dream tank view (`currentTab='shopping'; tankSub='bluedream'; renderShopping()`). Assertions — **(a) PASS app does NOT crash on startup; PASS banner "Status — June 10"; PASS "permanent Seachem-style always-on ammonia alert card"; PASS "0 ppm ammonia (safe)"; PASS Right Now "Ammonia 0 ppm — safe"; PASS Targets "(0 ✅ — always-on alert card)"; PASS 6/10 log row; (b) PASS stale "(1.0 — recovering)" / "Status — June 4" removed.** `node --check` clean on all surfaces (data.js · recipes.js · sw.js · both index inline blocks) before & after restamp. `restamp.py` clean (48s skew).

**Safe deploy steps used (repeatable):** fresh `git clone` of origin → edit `index.html` in the Linux sandbox (NEVER the OneDrive mount — it truncates >140KB) → `node --check` all JS surfaces → jsdom real-path render assert → bump `sw.js` cache (hg-v67→**hg-v68**) → `python3 restamp.py` (external Denver time) → **ONE** `git commit` + `git push origin main` (token from `home-and-garden-project/.deploy-token`) → mirror index.html+sw.js back into House sha256-identical. **Verified: origin `main` == `61cf576` + House mirror sha256-identical (index.html `ede69c3a…`, sw.js `a14d50c6…`).** Live HTTP byte/200 confirm NOT possible — `mcp__workspace__web_fetch` times out on github.io every time (recurring infra issue); the **SW bump hg-v67→hg-v68** force-busts stale PWA caches and Pages auto-builds from origin. Hard-refresh / reopen the PWA to pull hg-v68.

**💡 SUGGESTION flagged to Cyrus (NOT done):** now that ammonia is a continuous always-on reading, the daily "check ammonia" scheduled task could soften from a test-and-log chore to a quick **"glance at the alert card"** confirmation. Left unchanged pending his OK.

### ✅ LIVE — 🛠️ "3D print: headrest cup holder for Zoe" added as a rollover task (Jun 10 2026, commit `23c1db3..bbe3485`, ONE commit, SW **hg-v66→v67**, stamp "Jun 10 · 10:51 AM")
**Deployed on standing GO-LIVE authorization.** New 3D-print idea — a cup holder that clamps onto a car-seat headrest post — added as a **rollover task** (the engine built earlier: stays on Today and carries forward daily until checked, so it persists until he prints it). Seeded **idempotently** via a dated guard flag `flags['rollover-seed-headrest-v1']` (same pattern as the divider-tank seed): `rollovers['ro-headrest-cupholder']={ label:"🛠️ 3D print: headrest cup holder for Zoe", created: todayKey() }`, written once and never duplicated on reload. Chose the rollover (Today) home over the Print-tab wishlist because the ask was explicitly "stays until he prints it." **MANDATORY real-execution verification (the standing rule):** ran the full real load path in jsdom (Firebase snapshot → the headrest seed migration writing state → `showApp()` → `renderAll()` → `renderToday()`), clock pinned to Wed Jun 10 2026, realistic state (divider seed already done, headrest seed pending). Assertions: **(a) PASS — app does NOT crash on startup; (b) PASS — Today output literally contains `"headrest cup holder for Zoe"` (rendered inside a rollover card); (c) PASS — divider-tank rollover still present (no regression).** `node --check` clean on all surfaces (data.js · recipes.js · sw.js · both index inline blocks) before and after restamp. `restamp.py` clean (33s skew). **Verified: origin `main` == `bbe3485` + House mirror sha256-identical (index.html `330088ab…`, sw.js `a6b1f6e5…`).** Live HTTP byte/200 confirm STILL not possible — `mcp__workspace__web_fetch` times out on the github.io host every time (recurring infra issue); the **SW bump hg-v66→hg-v67** forces clients to drop stale caches, and Pages auto-builds from origin. Hard-refresh / reopen the PWA to pull hg-v67.

### ✅ LIVE (force-redeploy) — 🔁/🌱/🗑️ features: render PROVEN correct via real-execution harness; SW **hg-v65→v66** cache-bust (Jun 10 2026, commit `2beed12..23c1db3`, ONE commit, stamp "Jun 10 · 8:14 AM")
**Why:** Cyrus reported the A–E items not appearing on Today and suspected dead code (early return / outside flow / item-cap). **Disproven by directly executing the real `renderToday()` in a jsdom harness** (not an isolated logic sim): loaded the full page DOM, stubbed Firebase/fetch, pinned the clock to Wed Jun 10 2026, and ran (1) `renderToday()` directly, (2) the FULL real path — Firebase snapshot → the divider-tank seed migration writing state → `showApp()` → `renderAll()` → `renderToday()`, and (3) steady-state with a populated Firebase + a 2-day-old rollover. **All three produced a ~30K-char Today view literally containing `"Add a task that rolls over"`, `"Water Mom's plants"`, `"Clean and set up divider tank"`, and `"Carried over · day 3"`, with no thrown error.** (The add-card and Water-Mom items are UNCONDITIONAL `add()` calls inside `renderToday`, before the flow render at the end — they cannot be hidden by state.) **Conclusion: the render code is correct; the items were not appearing because the client was serving STALE code** — the prior v65 deploy's *live* verification was never confirmed (web_fetch keeps timing out on the github.io host). **Fix:** new commit re-triggers the GitHub Pages build and **SW bump hg-v65→hg-v66** force-busts stale PWA caches (`skipWaiting` + `clients.claim` already in sw.js auto-activate the new worker). `restamp.py` clean (60s skew). **Verified: origin `main` == `23c1db3` + House mirror sha256-identical (index.html `d08a60d9…`, sw.js `20d2aa13…`).** Live HTTP 200/byte-compare STILL unverified — `mcp__workspace__web_fetch` times out on github.io (recurring infra issue). **Action for Cyrus / next chat:** hard-refresh or fully close & reopen the PWA to pull hg-v66; if it still doesn't show, verify live in a real browser (Chrome tools) — the code is proven, so any remaining issue is cache/propagation. Harness scripts saved at `/tmp/hg/harness*.js` (sandbox, ephemeral).

### ✅ LIVE — 🔁 Rollover tasks + 🌱 Water Mom's plants (daily) + 🗑️♻️ Trash/Recycle alternating-Tuesday schedule (Jun 10 2026, commit `b58303e..2beed12`, ONE commit, SW **hg-v65**, stamp "Jun 10 · 7:01 AM")
**Deployed on "GO LIVE" (Cyrus authorized: "complete these tasks and then go live").** Fresh clone of current origin `b58303e` (no drift — clone == House mirror == origin, sha256-verified before editing). ALL edits applied by a python script in the Linux sandbox (NOT the OneDrive mount, which truncates the 392KB file). `node --check` clean on all 4 JS surfaces (data.js · recipes.js · sw.js · extracted index inline block, 350K chars) AND re-checked after restamp. Two behavior sims green: rollover lifecycle (active→carried day-N→done-pile-that-day→gone-next-day→uncheck-returns) and Tuesday parity (recycle Jun 9/23, Jul 7/21; trash-only Jun 16/30, Jul 14). `restamp.py` external Denver (44s skew, selftest 8/8). ONE commit pushed `b58303e..2beed12`. **Verified: origin `main` == `2beed12` + House mirror sha256-identical (index.html `e9723d49…`, sw.js `b41e221a…`).** Live HTTP byte-compare inconclusive — `mcp__workspace__web_fetch` timed out on the github.io host (same infra flakiness noted in the hg-v59 entry); Pages auto-builds from origin and the SW bump hg-v64→**hg-v65** forces clients to refresh.

**WHAT SHIPPED (all on the Today tab; matches existing flow/`add()` + date-keyed `laundry` + `checked` patterns; NEW `rollovers` Firebase node):**
- **A — Rollover engine + UI.** New Firebase node `rollovers` = `{id:{label,created,done?}}` (loaded in the snapshot handler, saved via `saveRollovers()`). Each renders on Today via the `flow`/`add()` system and **STAYS every day until checked** — an unfinished one carries forward day after day (never silently disappears) and grows a **"🔁 Carried over · day N"** badge plus "Still here from <date>" subtext once it survives past day 1. `done` stores the date it was completed: it shows in the done pile that day, then it's gone the next; tapping a done one again clears `done` and it returns. Amber (#F59E0B) styling. Cyrus can add his own via a dashed **"＋ Add a task that rolls over until done"** card (order 325) → `addRollover()` prompt. Helpers: `addRollover` / `toggleRollover` / `removeRollover` / `rolloverCardHtml`. Rollover cards count toward the daily tally; the add button is passive.
- **B — 🌱 "Water Mom's plants"** every day (order 352, after garden care). Date-keyed `laundry["rem-watermom-"+todayKey()]` via `toggleReminder('watermom',true)` — checking clears only today, reappears tomorrow. No end date.
- **C — 🗑️♻️ Trash + Recycle**, every **Tuesday night** (order 1280, evening). Computed from `dow===2` + week-parity off the **Tue Jun 9 2026 anchor**: even weeks = "Trash + Recycle" (♻️, Jun 9/23, Jul 7…), odd weeks = "Trash" only (🗑️, Jun 16/30, Jul 14…). Date-keyed done (`rem-trash-`+today) so it returns weekly. A real repeating schedule, not one-offs.
- **D — Alarm clock:** reused the EXISTING top-pinned "Get an alarm clock" (`checked["get_alarm_clock"]`) — per Cyrus, NO duplicate card added.
- **E — "Clean and set up divider tank"** seeded as a rollover (`rollovers['ro-divider-tank']`) via a one-time idempotent migration gated by `flags['rollover-seed-v1']` (created = the day it first loads). Appears on Today and rolls until checked.
- **Untouched / regression-checked:** meal anchors, water tile (bottles), bedtime wind-down, Today's 3 / habit streaks, Call DT, TG Meeting, reminders — all keys independent; diff is 5 hunks, +84 lines, only `index.html` + `sw.js`. **SW hg-v65.**

**Follow-up for next chat:** if `mcp__workspace__web_fetch` starts working, do a live byte-compare of https://mrosyrus21.github.io/home/sw.js (expect `hg-v65`) to close out the one unverified check.

### ✅ LIVE — 🌙 Bedtime wind-down merged into ONE tap (phone-away locks reading + lights + phone) (Jun 9 2026, commit `cbdc8a5..b58303e`, ONE commit, SW **hg-v64**, data.js `?v=20260609193658`, stamp "Jun 9 · 7:36 PM")
**Deployed on "GO LIVE."** Fresh clone of current origin `cbdc8a5` (no drift), edit via python in the sandbox. `node --check` clean ×4; 6-check sim green. `restamp.py` 51s skew. **Verified origin == `b58303e` + House mirror sha256-identical (index.html, sw.js); live Pages 200.**

**WHAT SHIPPED:** the three separate Today-tab bedtime items — 📖 reading hour, 🛏️ lights out, 📵 phone-to-the-other-room (the old `add("windphone"/"windread"/"windlights")` cards using `toggleEvening`) — are merged into ONE 🌙 "Bedtime wind-down" card. **The key problem solved:** Cyrus puts the phone (which runs the app) in another room at bedtime, so he can't check things off afterward. The single card is tapped AS he sets the phone down: `toggleWindDown()` flips all three `laundry["evening-wind-phone/read/lights-"+today]` keys true in one action (and `windDownDone()` reports complete only when all three are set; an undo tap clears all three; a partial leftover state still completes on tap). Calm copy ("Phone's going to the other room — tap to lock in tonight's wind-down. Sleep well. 🌙"), tied to his 11pm bedtime / no-screens rhythm (`RY.reading`/`RY.lightsOut`), purple bedtime styling kept. The done-state keys are unchanged, so nothing else that reads them breaks; meal/water/habit work is untouched (those keys are independent). **SW hg-v64**, data.js `?v=20260609193658`.


### ✅ LIVE — 💧 Water tile: cups → BOTTLES (1 bottle = 32 oz · goal 3 ≈ 96 oz) + one-time migration (Jun 9 2026, commit `a2c510f..cbdc8a5`, ONE commit, SW **hg-v63**, data.js `?v=20260609192714`, stamp "Jun 9 · 7:27 PM")
**Deployed on "GO LIVE."** Fresh clone of current origin `a2c510f` (still the staged base — no drift), staged `index.html` applied + the one-time migration added, `git diff` confirmed touching ONLY the water helpers + water-tile block + the new migration in `index.html`. `node --check` clean ×4; sims green (10-check tile sim: 3 taps = goal; 6-check migration sim). `restamp.py` 13s skew. **Verified origin `main` == `cbdc8a5` + House mirror sha256-identical (index.html, sw.js); live Pages 200.**

**WHAT SHIPPED:**
- Water tile speaks in **bottles**: `+1 bottle` button, "Per tap: 1 bottle", "X of Y bottles" headline, bottle/oz toasts, and both edit prompts. **1 bottle = 32 oz.** Defaults: `waterGoal()` = **3 bottles** (`habits.waterGoal||3`, ≈ 96 oz), `waterServing()` = **1 bottle**. Oz context shown ("1 bottle = 32 oz · goal ~96 oz", dynamic `goal*32`). All prior behavior kept (fill animation, partial credit, undo, daily reset, 2–5pm catch-up nudge, "counts all fluids — no magic number"). Goal + per-tap still editable (in bottles/oz).
- **Today's 3** water completion now triggers at the **3-bottle** goal.
- **One-time migration `water-bottles-v1`** (in the snapshot migration chain): resets any leftover cups-era `habits.waterGoal`/`waterServing` to the bottle defaults (3 / 1), gated by `flags['water-bottles-v1']` so it runs once and never clobbers a later user choice; historical daily counts untouched.
- **SW hg-v63**, data.js `?v=20260609192714`.

Staged folder `staged_water_bottles/` superseded (shipped) — safe to delete.

### ✅ LIVE — 💧🍴💊 Evidence-based daily habits (Water · Meal windows · Today's 3) + 📞 TG Meeting + ✅ rules to-do (Jun 9 2026, commit `09118b7..a2c510f`, ONE commit, SW **hg-v62**, data.js `?v=20260609174945`, stamp "Jun 9 · 5:49 PM")
**Big build, deployed in one pass on "GO LIVE."** Fresh clone of `09118b7`; ALL edits applied by python scripts in the Linux sandbox (NOT the Edit tool). `node --check` clean on all 4 JS surfaces (data.js · recipes.js · sw.js · extracted index inline JS), **22-check behavior sim** green (water taps, 2-of-3 partial credit, forgiving streak incl. never-miss-twice + freeze tokens + consistency %, meal-window before/open/closes-soon/passed, no-guilt copy), and an inline rendered preview QA'd before push (saved at `outputs/habit_preview.html`). `restamp.py` 40s skew. Verified **origin == `a2c510f`** + House mirror sha256-identical (index.html, data.js, sw.js); live Pages 200.

**WHAT SHIPPED (all on the Today tab + Timeline, mobile-first, cohesive with the app's glassy/serif style):**
- **💧 Water tile** — big tappable glass that visibly FILLS (animated `hg-water-fill` height transition) toward a personalized, EDITABLE goal (default 8; `waterSetGoal`/`waterSetServing`; copy says it counts ALL fluids, no magic number). +serving per tap, undo, daily reset (keyed `habits.water[date]`). Partial credit always (shows "5 of 8", never failure). ONE gentle catch-up nudge only mid-afternoon (2–5pm) if under half.
- **🍴 Meal windows** — `MEAL_ANCHORS` extended with `winStart`/`winEnd` (breakfast 7–9, lunch 12–1, dinner 6–8); `mealAnchorHtml()` rewritten: one-tap "I ate" anywhere in the window = a hit; the reminder fires near the window's CLOSE ("…window closes in N min"), not its start; after close = gentle "a snack still counts" (no guilt). Gentle late-night-eating nudge after ~9:30pm.
- **✨ Today's 3** (centerpiece) — Water · Vitamins · Meals trio with PARTIAL CREDIT (2 of 3 = a visually distinct green "good day"). FORGIVING streak engine (`habitStats`): never hard-resets — "never miss twice" + 1–2 **freeze tokens that regenerate weekly** (`ensureFreeze`), and it shows the **streak ring** + **consistency %** ("X of 30 days") + **total completions** + freezes-left alongside the number, so one miss never zeroes motivation. Compassionate, factual copy after a miss ("One missed day doesn't slow you down — today's the one that counts"). NO red-X walls, no guilt language anywhere (sim-asserted). A **weekly meal-regularity dot row** (last 7 days, colored by score, today ringed).
- **🔗 Cross-linking** — Today's 3 puts Water/Vitamins/Meals in one place; breakfast/meal cards surface the 💊 vitamins + 💧 water prompts together ("eat → pills → water").
- **📞 TG Meeting** — recurring every Wednesday 6:30 PM, rendered with the exact loud CALL-DT treatment (same gradient/border/`dtCallPulse` glow) on Today (every Wed) + Timeline; `const TG_MEETING` in data.js; date-keyed done (`rem-tg-…`) so it returns weekly.
- **✅ "Have Claude rebuild Claude's rules in settings menu"** — new undated priority/do-first to-do (`claude_rules` in `TASKS`, room "priority").
- **New state:** `habits` Firebase node (water counts/goal/serving + freeze tokens), loaded in the snapshot handler, saved via `saveHabits()`. **SW hg-v62**, data.js `?v=20260609174945`.

Staged folders `staged_tg_meeting/` + `staged_meal_anchors/` are now superseded (both shipped) — safe to delete.

### ✅ LIVE — 🥗 Tian Provençal photo FIXED (Cyrus's own photo) + credit corrected (Jun 9 2026, commit `74015b2..09118b7`, ONE commit, SW **hg-v61**, recipes.js `?v=20260609172351`, stamp "Jun 9 · 5:23 PM")
**Deployed on "GO LIVE".** The old `recipe_tian_provencal.jpg` was the wrong dish — a crusted zucchini *tart* (the `Joannaberg4` Wikimedia image, byte-identical to the squash-flatbread photo). Fix landed in two steps: (1) earlier commit `74015b2` swapped in a verified **CC0** crustless tian from Wikimedia Commons; (2) **this commit `09118b7`** replaced THAT with **Cyrus's own provided photo** (a real crustless Provençal tian — shingled zucchini/eggplant/tomato/squash coins in a fluted baking dish, basil + parmesan, NO crust/dough; 1000×667, 158,729 bytes) and **corrected the photo credit**, which had been left falsely claiming "Wikimedia Commons · CC0" — now reads **"Photo: provided by user"** (no false attribution). Old images archived to `House/_archive/tian-photo-userswap-20260609/` (CC0 one) and `archive/old-recipe-photos/` (original tart). Wired to `r_box_tian_provencal` → shows on BOTH the in-app Recipe Box and public `recipes.html` (shared `recipes.js`). I verified by **opening the image file** that it's a genuine crustless tian before shipping. `node --check` clean ×5 (recipes.js · data.js · sw.js · extracted index inline JS · extracted recipes.html inline JS); parse harness 49 recipes, photo path intact, credit carries NO CC0/Wikimedia claim. `restamp.py` external Denver time (61s skew, selftest 8/8). ONE commit `09118b7` pushed `74015b2..09118b7`. **Verified: origin `main` == `09118b7` + House mirror sha256-identical (index.html, recipes.html, recipes.js, sw.js, data.js, images/recipe_tian_provencal.jpg = `383216b7…`, 158,729 bytes); `recipes.html` returns 200.** SW bump hg-v60→**hg-v61** + recipes.js `?v` bump (index.html ×1, recipes.html ×1) force clients to drop the cached tart — hard-refresh to see the new photo.


### ✅ LIVE — 🍽️ Meal anchors that nag + 💊 pill bundle (Jun 9 2026, commit `a7e491d..6d8bc10`, ONE commit, SW **hg-v59**, data.js `?v=20260609133048`, stamp "Jun 9 · 1:30 PM")
**Deployed on "GO LIVE"** — Cyrus confirmed BOTH flags: keep the new times (lunch 12:00, dinner 7:00) and keep the pill bundle on breakfast. Fresh clone of current origin `a7e491d` (still the build base — no drift), staged edits applied, `git diff` confirmed touching ONLY the RHYTHM/MEAL_ANCHORS block in data.js and the meal/timeline blocks in index.html. `node --check` clean ×4 (data.js · recipes.js · sw.js · extracted index inline JS), functional sim 11/11. `restamp.py` (47s skew). ONE commit `6d8bc10` pushed `a7e491d..6d8bc10`. **Verified: origin `main` == `6d8bc10` + House mirror sha256-identical (index.html, data.js, sw.js); live `sw.js` + `recipes.html` return 200.** (Live index byte-compare skipped — web-fetch infra flaky on the big file, as in prior deploys; Pages auto-builds from origin and the SW bump to hg-v59 forces clients to refresh.) Staged folder `staged_meal_anchors/` superseded — files tombstoned to 119-byte placeholders (the OneDrive mount blocked programmatic delete; delete the folder manually if you like).

**WHAT SHIPPED:**
- **Three meal anchors, "eat on a clock":** 🍳 breakfast **8:30** · 🥗 lunch **12:00** · 🍽️ dinner **7:00 PM** (hard-by 8:00). Times live in `RHYTHM` + the new `const MEAL_ANCHORS` in data.js (id/emoji/label/at/min/upOrder/pills/by/cue). Same `toggleMeal()` / `laundry["meal-<id>-"+today]` done-state.
- **Reminders NAG (supportive, never guilt):** new `function mealAnchorHtml(A,done,nowM,dow)` before `renderToday()`. Before its time = quiet strip at the normal slot; once **due** (now ≥ anchor min) = loud card pinned to the top of Today (sort order 6) that persists until checked; **overdue** (≥45 min) = gentle escalate (amber→orange + "a little late, no worries" / "even something small counts"). No guilt/"you failed" language (harness-asserted).
- **💊 Pill bundle:** Litfulo + vitamins re-homed onto breakfast — in the morning the pill card sits directly under the breakfast anchor (sort 7, "take with breakfast"), and the breakfast nag says "Pills go with it 💊". One trigger: eat → pills.
- **Timeline:** a compact "🍽️ Daily meal rhythm — eat on a clock" banner at the top of the Timeline (🍳 8:30 · 🥗 12:00 · 🍽️ 7:00 · by 8:00 · 💊 pills with breakfast), so breakfast shows there too.
- **sw.js** → **hg-v59**; data.js `?v=20260609133048`.

### ✅ LIVE — 🌿 thyme reminder + 🍴 Recipe Box category-landing browse redesign (Jun 9 2026, commit `dea1166..13cdef7`, ONE commit, SW **hg-v56**, recipes.js + data.js `?v=20260609001127`, stamp "Jun 9 · 12:14 AM", 49 recipes)
**Built + deployed in one pass on "build the thyme reminder + recipe browse redesign, GO LIVE."** Fresh clone of `dea1166` into the Linux sandbox; ALL edits via python (NOT the Edit tool). `node --check` clean ×5 (data.js · recipes.js · sw.js · extracted index.html inline JS · extracted recipes.html inline JS). 27-check harness + DOM-stub render sims on BOTH surfaces green: thyme seed `standing:true`/no date/no account, shows on Today, survives day-rollover, on Timeline today-row, clears when checked; category mapper buckets all 49 into 9 non-empty tiles, drill-in shows only that category's cards + back, Doggy split (2 dog recipes in-app; disabled "in the app" tile on the public page), search flat across the whole box + clearing returns to tiles, weekly featured intact, recipes.html share-safe (no Firebase/localStorage/curtain). `python3 restamp.py` external Denver time (1s skew, selftest 8/8). ONE commit `13cdef7` pushed `dea1166..13cdef7`. **Verified: origin `main` == `13cdef7` + House mirror sha256-identical (index.html, recipes.html, recipes.js, sw.js, data.js).** ⚠️ Live-CDN re-fetch of `recipes.html` timed out (web-fetch infra flaky, as in prior deploys) — re-confirm `github.io/home/recipes.html` 200 with the category tiles + `sw.js` = `hg-v56` next session; Pages auto-builds from origin.

**WHAT SHIPPED:**
- **🌿 thyme reminder** — new standing errand seed in `data.js` `REMINDERS`: `{ id:"rem_thyme_plant", standing:true, emoji:"🌿", text:"Get a thyme plant" }` (same shape as the Petco/ammonia standing errand). Auto-renders on **Today** (no 💳 pill) and now on the **Timeline** today-row via a new standing branch in `renderTimeline()`'s `tlRem` map (`r.standing && !reminderDone(r.id)` → attaches to `today`). Stays until checked; done-state in `laundry` via `reminderKey`/`toggleReminder` (`"rem-"+id`).
- **🍴 Recipe Box browse redesign** (both `index.html` Recipe Box AND public `recipes.html`) — single flat list replaced by a category landing: **🧑 Hooman Food** category tiles (Italian 🍝3 · French 🥐1 · Mexican 🌮5 · Breakfast 🍳2 · Seafood 🦐5 · Pasta 🍜1 · Sides & Veg 🥗5 · Desserts 🍰4 · American/Mains 🍔23) + a **🐶 Doggy Food** tile (Zoey's 2 dog recipes in-app; disabled "in the app" on the public page). Tap a tile → that category's cards + **← All categories** back control. New `recipeCategory(r)` mapper (priority: italian→french→mexican→breakfast→seafood[+tuna]→pasta→sides/veg→desserts[+baking]→American/Mains; first match wins). KEPT: global search (spans ALL recipes incl. dogs in-app; clearing returns to tiles), the weekly auto-rotating featured spotlight (`weeklyFeatured`, hidden while searching), the grandma's-kitchen cream/gingham theme, the Caveat title, and the "Cy's Recipe Box" name. Nav held in transient `recipeCat`/`selCat` (no Firebase). SW `hg-v55→hg-v56`; `data.js?v`+`recipes.js?v` → `20260609001127` (index ×2, recipes.html ×1). recipes.js data unchanged.

**TASK 1 — 🌿 thyme reminder (in-app, Today + Timeline; standing rule).** Add an **active, open-ended errand reminder** "🌿 Get a thyme plant" — shows on **Today now and stays until checked off** (NOT date-gated), and appears on the **Timeline** near-term. **No account** (garden errand).
- This is the SAME pattern as the existing **standing Petco ammonia-tester errand** (see §10 PART 1, commit `75956bb`). Grep `data.js` for the Petco/ammonia reminder seed in the `REMINDERS` array and copy its shape exactly — it's the model for a no-date, no-account, stays-until-checked errand. Add one new seed, e.g. `{ id:"rem_thyme_plant", emoji:"🌿", text:"Get a thyme plant" }` with whatever flag the Petco errand uses to render as standing (no `account`, no hard date). Done-state lives in `laundry` via the `reminderKey`/`toggleReminder` one-shot (`"rem-"+id`) like the others.
- Render is automatic once seeded: `renderToday()` emits it via `reminderCardHtml()` (no 💳 pill since no account) and `renderTimeline()` injects it via its `tlRem` map. If the standing errand renders on Today via a different branch than dated ones, mirror that branch. **Verify by sim:** card shows on Today, has no account pill, survives a day rollover (doesn't disappear), and shows on Timeline; checking it off clears it.

**TASK 2 — 🍴 Recipe Box browse redesign (BOTH the in-app Recipe Box AND public `recipes.html`).** Replace the single long list with a **category-landing → drill-in** browse, keeping search + weekly featured + the grandma's-kitchen theme + "Cy's Recipe Box" name.
- **Files / functions to read first:** in `index.html` → `renderRecipeBox()` (the `view-grocery`, `foodSub==='recipebox'` view) and `recipeListHtml()`; the shared helpers `weekIndex()` / `weeklyFeatured(list)` and the `RECIPES_BOX.filter(!dog&&!draft)` featured pick. In `recipes.html` → its list render + `recipeCard(r,featured)` + the featured spotlight + the search input. (Dog recipes live in `data.js`'s `RECIPES`/Zoey set and are NOT loaded by recipes.html.)
- **Top-level split:** **🧑 Hooman Food** (all human recipes) and **🐶 Doggy Food** (Zoey's dog recipes — the `dog:true` ones, in-app only). On `recipes.html`, dog recipes aren't loaded → show **Hooman Food only** (fine; keep the framing — either hide the Doggy tile or show it disabled "in the app").
- **Category landing FIRST:** show clickable **category tiles** (not the full list). Tap a tile → render only that category's recipe cards + a **"← All categories"** back control that returns to the tiles. Use a transient nav variable (no Firebase) for the selected category, e.g. `recipeCat` in index.html and a local `selCat` in recipes.html; default null = show tiles.
- **Hooman sub-categories — derive from existing tags/names** with a single `recipeCategory(r)` mapper (priority order, first match wins): **Italian** (`italian`), **French** (`french` — NEW, the Tian) , **Mexican** (`mexican`), **Breakfast** (`breakfast`/name), **Seafood** (`seafood`/`fish`/`shrimp`/`salmon`/`trout`), **Pasta** (`pasta`/name), **Sides & Veg** (`side`/`vegetarian`/`veg`/`salad`), **Desserts** (`dessert`/`sweet`), else **American / Mains**. Only emit tiles for non-empty categories; show a per-tile count. (49 recipes; spot-check the mapping covers them all — log any that fall through to Mains.)
- **Search stays global:** the search box filters across **ALL** recipes regardless of selected category (name / ingredient / tag / blurb, as today). While the box has text → show flat results (ignore category selection). **Clearing the box → return to the category tiles.**
- **Keep the weekly featured spotlight** at the very top (above the tiles), unchanged logic (`weeklyFeatured(RECIPES_BOX.filter(!dog&&!draft))`), hidden while searching — same on both surfaces.
- **Theme/name unchanged:** grandma's-kitchen cream/gingham theme, Caveat title, "Cy's Recipe Box".
- **Verify by sim/harness:** tiles render with correct counts; tapping a category shows only its recipes + back returns to tiles; Hooman/Doggy split correct (Doggy only in-app); search filters across all and clearing restores tiles; featured still rotates weekly and matches on both surfaces; recipes.html has NO curtain/Firebase/localStorage (share-safe) and still returns 200.

**Style:** mobile-clean, match the existing Recipe Box. No new libraries. Don't touch other tabs/Finance. After building: `node --check` ×5, ONE commit, **SW→hg-v56**, `?v` bump (×3), restamp, push, mirror, verify, flip this entry to LIVE.


### ✅ LIVE — 🍴 Cy's Recipe Box: NEW French recipe "Tian Provençal" (Jun 8 2026, commit `a652451..dea1166`, ONE commit, SW **hg-v55**, recipes.js + data.js `?v=20260608234500`, stamp "Jun 8 · 11:56 PM")
**Built + deployed in one pass on "add ONE new FRENCH recipe, GO LIVE."** Fresh clone of `a652451` in the Linux sandbox; recipe appended to `RECIPES_BOX` via python (NOT the Edit tool). `node --check` clean on recipes.js · data.js · sw.js · extracted index.html inline JS · extracted recipes.html inline JS. Parse harness: **49 recipes** (was 48), new one last, `dog:false / draft:false` so it passes recipes.html's `!r.dog && !r.draft` filter; tagged `french`; US units (no metric tokens); photo file present. `restamp.py` (55s skew, selftest 8/8). ONE commit `dea1166` pushed `a652451..dea1166`. **Verified: origin `main` == `dea1166` + House mirror sha256-identical (index.html, recipes.html, recipes.js, sw.js, image); recipes.html still 200.** ⚠️ Live-CDN re-fetch of `recipes.js?v` timed out (web-fetch flaky/infra) — re-confirm `github.io/home/recipes.html` shows the Tian card and `sw.js` = `hg-v55` next session; Pages auto-builds from origin.

**WHAT SHIPPED** — single new recipe in `recipes.js` (appears on BOTH the in-app Recipe Box under Food→Recipes AND the public `recipes.html`; grandma's-kitchen theme styles it automatically):
- **`r_box_tian_provencal` — "Tian Provençal"** 🥗 · tags `["box","french","provençal","side","lunch","vegetarian"]` (clearly FRENCH — distinct from the authentic-Italian set) · "15 mins prep · 45 mins cook" · serves 1–2 · source **"Classic Provençal"** · blurb flags it as a classic French Provençal tian, not Italian. 9 ingredients (½ zucchini + ½ yellow squash + 1 small tomato in thin ~⅛-inch coins, ½ onion, 2 garlic, 2 Tbsp olive oil, salt+pepper, 1 tsp thyme/herbes de Provence, optional grated Parmesan), all US units; 7 steps (375°F · sauté onion+garlic as a base · slice thin even coins · shingle upright in alternating rows · oil+season+Parmesan · foil-bake 25 min then uncover 15–20 + optional broil · rest).
- **Photo:** `images/recipe_tian_provencal.jpg` — reused the **already-in-repo, license-verified Wikimedia shingled-zucchini Provençal photo** (`Joannaberg4 · CC BY-SA 4.0 · Wikimedia Commons`) — this is the same dish the project earlier identified as the "French/Provençal tian" (the dropped flatbread image). **NOTE for next session:** a brand-new image could not be downloaded (image fetch isn't available in the cowork sandbox — web tools return only text/binary, and curl/wget are disallowed), so the closest verified in-repo shingled-zucchini photo was used. It's a shingled-zucchini *tart* (has a pastry rim), not a pure crustless tian; **swap to a true tian photo when an image download is possible** if desired.

### ✅ LIVE — 💸 Side Income sub-tab in Finance (Jun 8 2026, commit `6cf4e8f..a652451`, ONE commit, SW **hg-v54**, stamp "Jun 8 · 11:39 PM")
**Built + deployed in one pass on "build the Side Income tab, GO LIVE."** Fresh clone of `6cf4e8f` into the Linux sandbox; ALL edits via python (NOT the Edit tool). `node --check` clean on extracted index.html inline JS · data.js · recipes.js · extracted recipes.html inline JS · sw.js. Functional harness: gating simulated across 3 states (empty → only Phase 1 open; listings>0 → Phase 2 unlocks; Phase 1+2 shipped → Phase 3 unlocks); segment renders with no runtime crash; **verified the One Number / Overview math contains NO `fin.side` reference** (committed-outflow formula untouched). `restamp.py` external Denver time (60s skew, selftest 8/8). ONE commit `a652451` pushed `6cf4e8f..a652451`. **Verified: origin `main` == `a652451` + House mirror sha256-identical (index.html, sw.js); recipes.html still 200.** ⚠️ Live-CDN sw.js version not text-confirmable (web-fetch returns it as binary) — re-check `github.io/home/sw.js` = `hg-v54` next session; Pages auto-builds from origin.

**WHAT SHIPPED (all in `index.html`, single-file app — data.js/recipes.js/recipes.html untouched, so no `?v` bump; only SW→hg-v54 to refresh the shell):**
- **New `["sideincome","💸 Side Income"]` sub-tab** registered in the finance subs array (after 🧠 Coach). `switchFinSub`/`finSub`/pills handle the rest; `fin.side` loads/saves automatically via the existing snapshot + `saveFin()`.
- **4 helpers** added by the other fin toggles: `sidePhaseToggle(n)` (toggles `fin.side.phase[n]`, toasts "Phase n shipped — next phase unlocked"), `sideProdToggle(n)` (`fin.side.prod[n]`), `sideEditListings()` + `sideEditEarned()` (both `prompt()`-based with numeric validation, copy of the `updateDmvAppt()` pattern).
- **D) Green banner** up top: "Print, shoot, and list ONE thing — the moss ledge."
- **A) 3-phase roadmap (checkable, GATED):** 🟢 Phase 1 (now, wk 1–2) Sell 3D prints on Etsy + local FB · 🟡 Phase 2 (wk 3–6) Freelance writing/admin on Upwork · 🔵 Phase 3 (month 2+) Build sites/apps for local businesses ($500–5k/job). Later phases render **🔒 locked/greyed** until the prior gate is checked — Phase 2 unlocks when listings are live (`fin.side.phase[1]` OR `fin.side.listings>0`), Phase 3 unlocks when one gig is landed (`fin.side.phase[2]`). The gating rule is shown in plain text.
- **B) 5 starter prints (checkable, each priced):** Magnetic moss/plant ledge ~$13 (⭐ start here) · Shrimp/isopod hide ~$9 · Betta floating house ~$17 (costs ~$5.76) · Coral frag rack ~$18 · Net-pot/grommet bundle ~$12. All PETG/water-safe. Shows the **pricing rule:** full cost (filament + electricity + ~$0.16/hr machine wear + labor + failure buffer) **× 3–4**.
- **C) Earnings tracker:** tap-to-edit `# listings live` + `total earned to date` (stored in `fin.side`, `prompt()`-edited), an "N of 5 starter prints listed" progress line, a note that the money goes to **$500 buffer first, then the debt snowball (•6605)**, and a ⚠️ card stating it is **kept SEPARATE from the real cashflow "One Number"** (aspirational; not added to committed-outflow math).
- **Style:** mobile-clean, matches Coach/To-dos (Playfair/Lora, `section-label`, the shared card style, Y/G/R palette). No new libraries; all existing Finance sub-tabs untouched.


### ✅ LIVE — 🧺 Kitchen Inventory: seeded the 11 stuffed-shells staples in-stock (Jun 8 2026, commit `6fbee0d..6cf4e8f`, ONE commit, SW **hg-v53**, stamp "Jun 8 · 10:28 PM")
**⚠️ HONEST STATUS NOTE:** this "inventory seed" was referenced in the previous two asks ("fold into the same commit … + inventory") but was **NOT actually done** in commits `7dd7021`/`6fbee0d` — those only did the photo audit, unit conversion, the new/real recipe, and the weekly featured rotation. It is done **now** in its own commit `6cf4e8f`. Before this commit the only inventory seeds in the code were `inventory-seed-v1` (squash/greens) and `inventory-seed-v2` (Cyrus's Jun 4 items); none of these 11 were present.

**WHAT SHIPPED** — a one-time dated Firebase seed migration in `index.html` (same guarded `flags['…']` pattern as the existing seeds), `flags['inv-seed-shells-jun8-v1']`:
- Adds these **11 items to Kitchen Inventory as in-stock**, capitalized labels, `invKey`-normalized keys: **Jumbo pasta shells** 🐚 (key `jumbo pasta shell`), **Ricotta** 🧀, **Mozzarella** 🧀, **Parmesan** 🧀, **Marinara** 🥫, **Italian sausage** 🌭, **Onion** 🧅, **Garlic** 🧄, **Pesto** 🌿, **Fresh basil** 🌿, **Lemon** 🍋. (If a key already existed it's just flipped to `inStock:true`; nothing duplicated.)
- **Clears matching active grocery-List items:** for each seeded item it marks `grocPurchased[id]=true` on any `GROC_BUILTIN` or `grocCustom` row whose normalized name **exactly** matches the item's key — the same exact-key semantics as the app's own `buyGroc()`/`addToGrocery()`. Exact match (not loose) is deliberate so there are NO false positives (e.g. "Onion" will NOT clear the builtin "Green onion"; "Lemon" will NOT clear a "Lemon juice" entry). Whatever of the 11 is currently on his live list gets ticked off; the rest of the list is untouched.
- Runs once client-side on next load (writes `inventory` + `grocPurchased` + the done-flag in a single `ST.update`, then returns). Recipe-derived grocery items already in stock are additionally auto-hidden by the existing `grocStockLoose` filter.

**Verified:** `node --check` clean (sw.js + extracted index.html inline JS); migration logic unit-simulated in Node (11 items seeded in-stock; sample list with Ricotta/Marinara/Garlic → marked purchased; "Lemon juice" and builtin "Green onion" correctly left alone). `restamp.py` (8s skew). ONE commit `6cf4e8f` pushed `6fbee0d..6cf4e8f`; origin HEAD == `6cf4e8f`; House mirror (index.html, sw.js) sha256-identical. data.js/recipes.js/recipes.html unchanged (so their `?v` left at `20260608220000`; only SW bumped to hg-v53 to refresh the shell). ⚠️ Live-CDN fetch not re-confirmed (web-fetch flaky) — re-check `github.io/home/sw.js` = `hg-v53` and that the 11 items show in 🧺 Inventory next session (note: the seed runs in the live app against Firebase on first load — it will populate when he opens the app).


### ✅ LIVE — 🍴 Cy's Recipe Box: stuffed-shells → REAL published recipe + 🔁 WEEKLY auto-rotating featured pick (Jun 8 2026, commit `0afd13b..6fbee0d`, ONE commit, SW **hg-v52**, recipes.js + data.js `?v=20260608220000`, stamp "Jun 8 · 10:22 PM")
**Built + deployed in one pass on two folded asks: (1) make "Sausage & Pesto Stuffed Shells" a REAL chef/credible-source recipe, and (2) auto-rotate the featured spotlight WEEKLY on both surfaces.** Fresh clone of `0afd13b` → sandbox Python edits (NOT the Edit tool). `node --check` clean on recipes.js · data.js · sw.js · extracted index.html inline JS · extracted recipes.html inline JS. 13-check harness green. `restamp.py` (64s skew, selftest 8/8). ONE commit `6fbee0d` pushed `0afd13b..6fbee0d`. **Verified: origin `refs/heads/main` == `6fbee0d` + House mirror sha256-identical (index.html, recipes.html, recipes.js, sw.js).** ⚠️ Live-CDN fetch not re-confirmed (web-fetch flaky) — re-check `github.io/home/sw.js` = `hg-v52` next session.

**PART 1 — REAL RECIPE (replaces my earlier hand-drafted version).** `r_box_sausage_pesto_stuffed_shells` "Sausage & Pesto Stuffed Shells" now carries the actual, well-reviewed recipe **transcribed faithfully (as the chef wrote it, US units) from Brianna May's "Baked Stuffed Shells with Meat Sauce" on Bites with Bri** (5★, 11 ratings) — https://biteswithbri.com/baked-stuffed-shells/ . `source` field → **"Bites with Bri (Brianna May)"**; the note credits the source + link and her own spicy/creamy/make-ahead variations. 12 ingredients (1 lb Italian sausage, yellow onion, 5–6 garlic, 24 oz marinara + ¼ cup water, 15 oz whole-milk ricotta, 1 egg, ⅓ cup basil pesto, 2 cups mozzarella divided, 1 cup Parmesan, ¾ tsp kosher salt, 20 jumbo shells) · 6 steps (boil shy of al dente at 400°F → brown sausage → onion+garlic+marinara simmer → ricotta/pesto/egg/cheese filling → stuff & layer in a 9×13 → foil-bake 25–30 min, then broil 1–2 min). Serves 6. **Photo + name + emoji + tags kept** (photo still Famartin's baked-shells, CC BY-SA 4.0). NOTE on the user's earlier ingredient list: that was just what he had ON HAND — the recipe is the source's as-written, not force-fit to his pantry (pesto happens to be authentic to Bri's recipe).

**PART 2 — 🔁 WEEKLY AUTO-ROTATING FEATURED (no Firebase, pure date math).** The spotlight at the top of the Recipe Box now changes every week automatically. Two tiny shared helpers added to BOTH `index.html` and `recipes.html`:
```
function weekIndex(){ return Math.floor((Math.floor(Date.now()/86400000)-4)/7); } // continuous week counter; advances each Monday (UTC)
function weeklyFeatured(list){ if(!list||!list.length) return null; var i=((weekIndex()%list.length)+list.length)%list.length; return list[i]; }
```
- **How it picks:** `weekIndex()` = whole weeks since Mon 1970-01-05 (UTC), so it's constant Mon→Sun and ticks up every Monday. The featured recipe = `list[weekIndex() % list.length]`, cycling deterministically through the whole box, wrapping forever. No state, no upkeep. (This week's pick: **Instant Pot Mashed Potatoes**.)
- **Same pick on both surfaces:** both now draw from the SHARED `RECIPES_BOX` pool, filtered `!dog && !draft` (48 recipes) — `index.html` was switched off `RECIPES` (which also contains the data.js beef-tenderloin/Zoey, absent on the public page) to `RECIPES_BOX` so the two pages always agree. Because `recipes.js` merges `RECIPES_BOX` objects into `RECIPES` by reference, the in-app grid's "exclude the featured from the list" (`r!==feat`) still works.
- **In-app:** `renderRecipeBox()` and `recipeListHtml()` both call `weeklyFeatured(RECIPES_BOX.filter(!dog&&!draft))` (the "★ This week's pick"/"⭐ Featured recipe" spotlight). The old static `featured:true` flag (beef tenderloin) is now ignored for the spotlight.
- **Public `recipes.html`:** added a featured-card variant (`recipeCard(r,featured)` — taller image + "★ This week's pick" label) and a spotlight at the top of the list (hidden while searching), excluded from the grid below; also fixed a latent `list.map(recipeCard)` index-as-2nd-arg gotcha by mapping with an explicit `recipeCard(r,false)`.
- Verified by simulation: stable across all 7 days of a week, advances each week, both surfaces identical, cycles distinct recipes.

**index.html exact changes:** added the two helpers; both featured picks switched to `weeklyFeatured(RECIPES_BOX.filter(!dog&&!draft))`; `data.js?v`+`recipes.js?v` `20260608210000→20260608220000` (×2); `LAST_DEPLOY`→"Jun 8 · 10:22 PM". `recipes.html`: helpers + featured card + spotlight; `recipes.js?v→20260608220000`. `sw.js`: `hg-v51→hg-v52`. (data.js unchanged except — nothing; only its `?v` reference in the HTML.)

**SAFE DEPLOY (done):** fresh clone `0afd13b` → sandbox Python → SW/`?v` bumps → `node --check` ×5 → 13-check harness + rotation simulation → `restamp.py` → ONE commit `6fbee0d` → push → origin HEAD + House mirror verified sha256-identical. (Next session: confirm github.io serves `hg-v52`, recipes.html 200 with the rotating spotlight, and the real Bites-with-Bri shells recipe in both views.)


### ✅ LIVE — 🍴 Cy's Recipe Box: NEW recipe "Sausage & Pesto Stuffed Shells" (Jun 8 2026, commit `7dd7021..0afd13b`, ONE commit, SW **hg-v51**, recipes.js + data.js `?v=20260608210000`, stamp "Jun 8 · 10:03 PM")
**Built + deployed in one pass on "add a NEW recipe … GO LIVE."** Fresh clone of `7dd7021` into a sandbox scratch dir (`/tmp/homeclone2` — OneDrive can't host a `.git`); recipe object appended to `RECIPES_BOX` via sandbox Python (NOT the Edit tool). `node --check` clean on recipes.js · data.js · sw.js · extracted index.html inline JS · extracted recipes.html inline JS. Parse check: **48 recipes** (was 47); new one is last, `dog:false / draft:false` so it passes recipes.html's `!r.dog && !r.draft` filter. `restamp.py` (61s skew, selftest 8/8). ONE commit `0afd13b` pushed `7dd7021..0afd13b`. **Verified: origin `refs/heads/main` == `0afd13b` (exact tree) + House-folder mirror sha256-identical (index.html, recipes.html, recipes.js, sw.js, new image).** ⚠️ Live-CDN fetch still couldn't be confirmed this run (web-fetch tool timing out, infra) — re-confirm `github.io/home/sw.js` = `hg-v51` and `…/recipes.html` 200 next session.

**WHAT SHIPPED** — single new recipe in `recipes.js` (so it appears on BOTH the in-app Recipe Box under Food→Recipes AND the public `recipes.html`, which share `recipes.js`; the grandma's-kitchen theme styles it automatically — no per-recipe CSS):
- **`r_box_sausage_pesto_stuffed_shells` — "Sausage & Pesto Stuffed Shells"** 🐚 · tags `["box","italian"]` · "25 mins prep · 45 mins cook" · serves 4-6 · source "Cy's Kitchen". 12 ingredients (jumbo shells, 1 lb Italian sausage, ricotta, mozzarella, Parmesan, pesto, egg, 24 oz marinara, basil…) all in US units; 7 steps (boil shy of al dente → brown sausage+onion+garlic → ricotta/pesto/egg filling → stuff & layer in a 9×13 with marinara + cheese → foil-bake 375°F 25 min, uncover 10–15 min → rest, finish with basil + pesto drizzle). Added a make-ahead note.
- **Photo:** `images/recipe_sausage_pesto_stuffed_shells.jpg` — license-verified Wikimedia `2019-03-11 … Jumbo stuffed shells … after baking` by **Famartin (CC BY-SA 4.0)**, a full baking dish of stuffed shells with marinara + melted mozzarella (matches the dish). Web-optimized to 800×600 (center-cropped 4:3, ~100 KB). `photoCredit: "Photo: Famartin · CC BY-SA 4.0 · Wikimedia Commons"`.

**WHAT CHANGED IN index.html (exact):** `data.js?v` + `recipes.js?v` bumped `20260608200000 → 20260608210000` (2 occurrences) so the new recipe loads past cache; `const LAST_DEPLOY` restamped `"Jun 8 · 10:03 PM"`. `recipes.html`: `recipes.js?v` → `20260608210000` (1). `sw.js`: `hg-v50 → hg-v51`. No JS-logic changes; app stays curtain-protected, recipes.html stays recipes-only.

**SAFE DEPLOY (done):** fresh clone `7dd7021` → sandbox Python (append recipe + optimize image) → SW/`?v` bumps → `node --check` ×5 → parse/filter harness → `restamp.py` → ONE commit `0afd13b` → push → verified origin HEAD + House mirror sha256-identical. (Next session: confirm github.io serves `hg-v51` + recipes.html 200, and that the new shell recipe renders in both the app card grid and recipes.html.)


### ✅ LIVE — 🍴 Cy's Recipe Box: photo-accuracy audit (7 mismatched photos replaced) + ALL recipe amounts converted to US customary units (Jun 8 2026, commit `d4a10d4..7dd7021`, ONE commit, SW **hg-v50**, recipes.js + data.js `?v=20260608200000`, stamp "Jun 8 · 2:00 PM")
**Built + deployed in one pass on "Focused QA + fix, GO LIVE" (with a folded-in second ask: convert metric → US).** Fresh clone of `d4a10d4` into a sandbox scratch dir (`/tmp/homeclone` — the OneDrive mount can't host a `.git`); ALL edits via sandbox Python (NEVER the Edit tool, which truncates the OneDrive-synced app files). `node --check` clean on recipes.js · data.js · sw.js · extracted index.html inline JS · extracted recipes.html inline JS. A 21-check harness green (47 recipes parse, 7 new images valid & ≤800px & archived, no metric tokens left, all photo credits present, Zoey portions converted, SW hg-v50, both `?v` bumped). `python3 restamp.py` external-time stamp (28s skew, selftest 8/8). ONE commit `7dd7021` pushed `d4a10d4..7dd7021`. **Verified: origin `refs/heads/main` == `7dd7021` (our exact tree, so origin blobs are byte-identical) and the House-folder mirror is sha256-identical to the sandbox.** ⚠️ Live-CDN fetch (github.io sw.js / recipes.html) could NOT be confirmed this run — the web-fetch tool timed out (180s, infra, not a block); the commit is on origin and Pages auto-builds, so a hard-refresh should show hg-v50. Re-verify `https://mrosyrus21.github.io/home/sw.js` shows `hg-v50` and `…/recipes.html` returns 200 next session.

**WHAT SHIPPED (applies to BOTH the in-app Recipe Box under Food→Recipes AND the public `recipes.html`, which share `recipes.js`):**

**🖼️ PART 1 — PHOTO-ACCURACY AUDIT.** Audited every recipe photo (47 in RECIPES_BOX + 3 in data.js incl. Zoey's 2 dog recipes = 50) against that recipe's actual ingredient list via labeled contact-sheets. **7 photos prominently showed a MAJOR component NOT in the recipe and were replaced** with license-verified, web-optimized (800×600, center-cropped 4:3, ~60–100 KB) Wikimedia photos; each old photo archived to `archive/old-recipe-photos/<name>.replaced-20260608.jpg` and `photoCredit` updated:
- **Instant Pot Award-Winning Chili** — old photo showed **hard-boiled eggs in a tomato broth, no beans/ground beef** (looked like a menudo/egg soup). → `Chili con carne` by cyclonebill (CC BY-SA 2.0): ground beef + kidney beans.
- **Old-Fashioned Beef Stew** — old photo had **corn + green beans + peas** (recipe has only carrots/potatoes/pearl onions/celery). → `Irish Beef Stew` by A Healthier Michigan (CC BY-SA 2.0): beef, potatoes, carrots.
- **Oven-Baked Baby Back Ribs** — old photo buried the ribs under **sweet-potato fries + cornbread + rice** (none in recipe). → `Pork-ribs 02` by Pannet (CC BY 4.0): clean glazed rack.
- **Pan Fried Butterflied Trout** — old photo was **trout amandine, covered in sliced almonds** (no almonds in recipe). → `Pan-fried trout with lemon butter` by Armineaghayan (CC BY-SA 4.0): trout + lemon + herbs.
- **Pan-Seared Salmon** — old photo was salmon on a salad bed with **avocado + tomatoes + sauce** (recipe is salmon + capers + lemon). → `Salmon fried in pan` by gran (CC BY-SA 3.0): salmon searing in the pan.
- **Creamy Garlic-Dill Chicken Thighs** — old photo had prominent **green beans + cauliflower** + a different fried-chicken-in-red-sauce. → `Chicken thighs at the girl and the fig` by Missvain (CC BY 4.0): plated thighs in a creamy/herb sauce.
- **Romaine & Spinach Side Salad** — old photo was a loaded salad with **hard-boiled eggs + feta + bacon + tomatoes** (recipe is just romaine/spinach + Dijon-lemon vinaigrette + optional parmesan). → `Mixed green salad` by FranHogan (CC BY-SA 4.0): leafy greens.
- **The other 43 photos were judged accurate and KEPT** (minor garnishes — a lemon wedge, blueberries on pancakes, a side of rice/salad that reads as a normal accompaniment — were left alone per the "only fix MAJOR misleads" rule). All "Cy's Recipe Box" makeover work (rename, grandma's-kitchen theme, Italian rewrites, 100% coverage) is intact.

**📏 PART 2 — METRIC → US CUSTOMARY.** Converted every metric measurement in recipes.js + data.js to cooking-friendly rounded US units; where a quantity was given as both, kept ONLY the US one. The serving-size scaler is unaffected (same leading-number/fraction format it already scales). Recipes changed:
- **Gordon Ramsay's Buttermilk Fried Chicken** — `500 ml (2 cups)` → `2 cups` buttermilk; `1 litre (4 cups)` → `4 cups` oil; `300 g (2¼ cups) plain white flour` → `2¼ cups all-purpose flour`; steps `170C`→`340°F`, `75C (165F)`→`165°F`, `170C (340F)`→`340°F`, `1.5cm depth`→`½-inch depth` (×2).
- **Mole Poblano** — `3.5 ounces Mexican chocolate 100 grams` → `3.5 ounces Mexican chocolate` (dropped the redundant grams).
- **Pork & Broccoli Stir Fry** — `200 g pork` → `7 oz pork`; step `2-3 cm high layer` → `1-inch high layer`.
- **Steak with Chimichurri** — `180g-200g Sirloin Steak` → `6-7 oz Sirloin Steak`.
- **Frittata di Zucchine** — `40 g Parmigiano-Reggiano` → `½ cup grated Parmigiano-Reggiano`.
- **Pasta alla Salsiccia e Zucchine** — `200 g` pasta → `7 oz`; sausage `about 250 g` → `about ½ lb`; `60 ml` wine → `¼ cup`; `40 g Parmigiano` → `½ cup finely grated Parmigiano`.
- **Zoey's One-Pot + Beef & Pumpkin Pot (data.js)** — `Portion 460 to 480 g per day` → `Portion about 1 lb (16 oz) per day` (×2). (kcal left as-is — it's an energy unit, not a measurement to convert; eggshell-bake `300°F` and `½ tsp/lb` were already US.)

**WHAT CHANGED IN index.html (exact):** `data.js?v` and `recipes.js?v` bumped `20260608191500 → 20260608200000` (2 occurrences) so the converted data + new credits load past the cache; `const LAST_DEPLOY` restamped to `"Jun 8 · 2:00 PM"` (America/Denver, external time). `recipes.html`: its `recipes.js?v` bumped to `20260608200000` (1 occurrence). `sw.js`: `hg-v49 → hg-v50`. No functional/JS-logic changes to index.html or recipes.html — the app stays curtain-protected; recipes.html stays curtain-free, recipes-only.

**SAFE DEPLOY (done):** fresh clone `d4a10d4` → sandbox Python edits (recipes.js + data.js + 7 images + archive) → SW/`?v` bumps → `node --check` ×5 → 21-check harness → `restamp.py` → ONE commit `7dd7021` → `git push d4a10d4..7dd7021` → verified origin HEAD == 7dd7021 + House mirror sha256-identical. (Next session: confirm github.io serves `hg-v50` + recipes.html 200.)


### ✅ LIVE — 🍴 Cy's Recipe Box: rename + grandma's-kitchen theme · authentic Italian rewrites · 100% photo coverage · 11 photo fixes (Jun 8 2026, commit `932d72e..d4a10d4`, ONE commit, SW **hg-v49**, recipes.js + data.js `?v=20260608191500`, stamp "Jun 8 · 1:23 PM")
**Built + deployed in one pass on "Big recipe makeover, GO LIVE" (plus three mid-flight corrections, all folded in).** Fresh clone of `932d72e` into a sandbox scratch dir (NOT the OneDrive mount — it can't host a `.git`); ALL edits via sandbox Python (never the Edit tool). `node --check` clean on data.js · recipes.js · index.html inline JS · recipes.html inline JS. 20-check harness green. `restamp.py` external-time stamp (skew 58s). Verified **origin == HEAD** (`d4a10d4`), **House mirror byte-identical** (sha256 on index.html · recipes.html · data.js · recipes.js · sw.js · deploy-github.py), and **live returns HTTP 200** on `/`, `/recipes.html`, `/recipes.js`, `/sw.js`, and a new image. Live content re-fetched and confirmed (new name, theme, recipes, removals, hg-v49 all present — not stale).

**WHAT SHIPPED (applies to BOTH the in-app Recipe Box under Food→Recipes AND the public `recipes.html`):**
- **RENAMED → "Cy's Recipe Box"** everywhere it's titled: the in-app lid header (`index.html`, recipesHtml lid, now in a handwritten **Caveat** script font) + both `📇/📖 Cy's Recipe Box` section labels; and `recipes.html` `<title>` + `<h1>` (also Caveat). NOTE: name is "Cy's" — the *grandma* angle is the THEME only, never the name.
- **🧓 GRANDMA'S-KITCHEN THEME (recipe surfaces ONLY — Today/Finance/etc. untouched):** warm cream/tan/butter palette, vintage index-card look (kept the cream `#F5ECD7` cards + red gingham "tape"), a subtle **red-gingham/parchment background** behind the Recipe Box view (`.gk-surface`) and on the detail overlay, Caveat script title accent. In `index.html` the detail overlay (`#recipe-page`) recolors via inline CSS-var overrides (`--text/--text2/--amber/...` → warm browns) so the ported inline styles all go warm with no JS changes; light text in the list (match-count, 🐕 Zoey's Kitchen divider, dog subtitle) was darkened for the cream bg. `recipes.html` `:root` palette + body background swapped to warm gingham. Mobile-clean, readable.
- **🇮🇹 AUTHENTIC ITALIAN rewrites (in recipes.js → both pages):**
  - "Sausage & Squash Pasta" → **"Pasta alla Salsiccia e Zucchine"** (salsiccia squeezed from the casing & browned, zucchine, garlic, white-wine deglaze, emulsified glossy with starchy pasta water + Parmigiano-Reggiano — *la mantecatura*; no jar/tomato sauce). Kept the id `r_box_sausage_squash_pasta` so the existing To Cook entry still resolves.
  - "Sausage & Squash Flatbread" → **DROPPED** (the shingled-squash dish he saw is a French/Provençal *tian*, not authentic Italian). Replaced with **"Frittata di Zucchine"** (new id `r_box_frittata_di_zucchine`) — classic Italian zucchini frittata, uses his eggs + both squashes, antipasto/lunch.
  - **"Roasted Garlic-Parmesan Zucchini, Squash and Tomatoes" → DELETED entirely** (per correction) from recipes.js + both pages.
- **🖼️ 100% PHOTO COVERAGE — 50/50 recipes** (47 in RECIPES_BOX + 3 in data.js incl. Zoey's 2 dog recipes) now carry a real, license-verified, web-optimized (≤800px) photo with attribution. **6 gaps filled** (breakfast quesadilla, romaine+spinach salad, the new pasta + frittata, and Zoey's One-Pot + Beef&Pumpkin homemade-dog-food bowls). **11 bad/wrong photos replaced** and the old ones archived to `archive/old-recipe-photos/`: corned beef & cabbage, carnitas, pork & broccoli, **chile verde (now pork in green salsa)**, mole poblano, **Award-Winning Chili (now AMERICAN red bean-and-beef chili with toppings — was a wrong Mexican-style dish)**, shredded beef, **pan-seared asparagus (was a soup)**, **pork chops (now just chops)**, plus two found in the full audit: **wisconsin five-cheese bake** (was an ambiguous/unappetizing shot → golden baked mac) and **smashed red potatoes** (was plain white mash → crispy smashed RED potatoes). All photos eyeballed on contact sheets for dish-correctness + appetizing-ness; every `photoCredit` cites a verified PD/CC0/CC BY/CC BY-SA license + author. (One orphan file `images/recipe_roasted_garlic_parmesan_zucchini_squas.jpg` remains unused after the recipe deletion — harmless.)
- **To Cook cleanup:** new one-time Firebase migration `jun8-flatbread-out-v1` pulls the dropped flatbread (`ck-seed-flat2`) and any roasted-zucchini entry (`ck-seed-dinner`) out of the live cookQueue.
- **SW → hg-v49**; `recipes.js?v` + `data.js?v` bumped to `20260608191500` in index.html and recipes.html so the new data loads past the cache.


### ✅ LIVE — 🍴 Public shareable recipes.html (the promised page — now real) (Jun 8 2026, commit `f774de3..932d72e`, ONE commit, SW **hg-v48**, stamp "Jun 8 · 12:49 PM")
**Built + deployed in one pass on "GO LIVE."** Fresh clone of `f774de3`; new file written in the sandbox. `node --check` ×4 clean (data.js · recipes.js · sw.js · **recipes.html inline JS**), 16-check harness green, share-safety grep clean. `restamp.py` (21s skew). Verified **origin == HEAD** (`932d72e`), **House mirror byte-identical** (sha256 on recipes.html · index.html · sw.js · deploy-github.py), and **the live URL returns HTTP 200**: https://mrosyrus21.github.io/home/recipes.html (was 404, now serves the page).

**WHAT SHIPPED**
- **NEW `recipes.html` at the repo root** → serves at `https://mrosyrus21.github.io/home/recipes.html`. Self-contained public page; **added to the deploy script's staged-files list** (`deploy-github.py`, right after index.html) so it actually ships, and to the **SW SHELL** cache (`sw.js`) so it's offline-available.
- **Single source of truth:** loads the SAME `./recipes.js?v=…` (`RECIPES_BOX`) the app uses — no duplicated recipe data, stays in sync automatically. Falls back to `RECIPES` if ever defined, but data.js is NOT loaded.
- **Human recipes only:** filters `!r.dog && !r.draft` → Zoey's 🐕 dog recipes (which live in data.js, never loaded here) and any draft/pending items are excluded. 48 recipes shown.
- **Features:** search box (filters by name / ingredient / tag / blurb) + the app's serving-size stepper with the ingredient quantity scaler ported verbatim (same fraction-aware scaling). Index-card aesthetic + mountain background reused from the app; header "🍴 Recipe Box" + live recipe count.
- **Share-safety (verified):** NO password curtain, NO Firebase, NO localStorage/sessionStorage, NO finances/health/today/reminders — recipes ONLY. Opens with zero login. The main app `index.html` is untouched functionally (only the standard deploy stamp changed) and stays curtain-protected.
- **SW** → **hg-v48** (+ caches `./recipes.html`).


### ✅ LIVE — 🖨️ Print → Ideas: two collapsible reference backlogs (Jun 8 2026, commit `ce63a2d..f774de3`, ONE commit, SW **hg-v47**, data.js `?v=20260608124027`, stamp "Jun 8 · 12:40 PM")
**Built + deployed in one pass on the request to "keep these in the Print tab for future ideas."** Fresh clone of `ce63a2d`; edit via python in the sandbox (NOT the Edit tool). `node --check` ×4 clean, 12-check harness green (both lists present + existing Print content confirmed untouched). `restamp.py` (35s skew). Verified **origin == HEAD** (`f774de3`) + **House mirror byte-identical** (sha256 on index.html · sw.js).

**WHAT CHANGED (index.html, the `_wish` Ideas panel only):** appended a "📚 Reference backlogs — saved for later, not active to-dos" divider and two native `<details>` collapsibles just above the Ideas footer:
- **🖨️ SV06+ Printer Upgrades (blue PLA)** — 23 chips (filament/runout guide, spool holder + bearing roller, duster/filter clip, PTFE/drybox passthrough, X&Y drag chains, cable clips/strain reliefs, under-base drawer, tool/nozzle organizers, tool tray, scraper holder, glare visor, screen tilt mount, knob caps, webcam mount, LED bar bracket, belt tensioner, Z top brace, damping feet, PEI handle/tab, purge bucket, bed-level test squares, Sovol badge, drawer pulls) + a ⚠️ "hotend-adjacent parts stay PETG" note.
- **🏡 Household / Hobby print ideas** — grouped: 🪲 Bug terrarium · 🐟 Aquarium out-of-water parts · 🌱 Garden · 🐕 Zoey · 🍳 Kitchen · 🎲 Desk/fun.

Both are collapsed by default (`<details>`/`<summary>`, no new JS state) so they read as a backlog, not active to-dos. The Print-of-the-Day, vibe sections, "Pimp My SV06" upgrade tiers, To-Do dryer/silica items, and Ideas footer are all unchanged. **SW hg-v47**, data.js `?v=20260608124027`.


### ✅ LIVE — 🍝 Recipe swap: 2 planned dishes out, today's 2 real dishes in, salad made permanent (Jun 8 2026, commit `bf3e345..ce63a2d`, ONE commit, SW **hg-v46**, data.js `?v=20260608121431`, stamp "Jun 8 · 12:15 PM")
**Built + deployed in one pass on "GO LIVE"** (incl. a mid-job correction: the Romaine & Spinach salad was KEPT and made permanent, not removed). Fresh clone of `bf3e345`; all edits in the sandbox via python (NOT the Edit tool). `node --check` ×4 clean, 20-check harness green. `restamp.py` (12s skew). Verified **origin == HEAD** (`ce63a2d`) + **House mirror byte-identical** (sha256 on index.html · recipes.js · sw.js).

**WHAT CHANGED**
- **`recipes.js` (Recipe Box):** REMOVED `r_box_sausage_squash_spaghetti` ("Hot Italian Sausage Spaghetti in Red Sauce") and `r_box_squash_flatbread` ("Squash Ribbon Flatbread"). The salad `r_box_romaine_spinach_salad` is now **PERMANENT** — dropped `"draft": true` and the "DRAFT — pending approval" note prefix. ADDED two finalized recipes: **`r_box_sausage_squash_flatbread` "Sausage & Squash Flatbread"** (lunch, serves 1–2, bake 400°F ~12–15 min) and **`r_box_sausage_squash_pasta` "Sausage & Squash Pasta"** (dinner, serves 1–2, squash browned hard + crushed-tomato/pesto). Both carry the shared prep note: "Brown ALL the sausage once (crumbled), split ⅓ lunch / ⅔ fridged dinner; caramelize 1 onion low & slow ~15–20 min and split between both." Box now 48 recipes.
- **`index.html` (one-time migration `jun8-recipeswap-v1`):** removes the spaghetti + old-flatbread entries from `cookQueue` (keeps `ck-seed-salad1` — salad stays queued), queues **flatbread → LUNCH** and **pasta → DINNER** for **2026-06-08**; adds **🍯 Hot honey** to the grocery list (`grocCustom`, food-only); corrects **🥚 Eggs back to IN-STOCK** in `inventory`. (The Recipes-project `SCHEDULE_INVENTORY_HANDOFF.md` wasn't mounted, so the grocery + eggs changes were applied directly, as the handoff permitted.)
- **`sw.js`** → **hg-v46**; data.js `?v=20260608121431`.


### ✅ LIVE — 🔔 Added Jun 13 car-wash cancellation-verify reminder (Jun 8 2026, commit `75956bb..bf3e345`, ONE commit, SW **hg-v45**, data.js `?v=20260608120917`, stamp "Jun 8 · 12:09 PM")
**One-reminder add on "GO LIVE."** Fresh clone of `75956bb` in the sandbox; the only change is a new entry in `data.js` `REMINDERS`: `{ id:"rem_carwash_verify", date:"2026-06-13", emoji:"🫧", text:"Check that Super Star Car Wash actually got cancelled (no $24 charge)", account:"Capital One 360 checking" }` — placed right after the Jun 11 car-wash+Crunchyroll check (both kept). It auto-surfaces on **Today** on Jun 13 (checkable, with the 💳 account pill) and on the **Timeline** on its date — no render changes needed, the PART-1 reminders engine handles it. `node --check` ×4 clean, 8-check mini-harness green (9 reminders total, new seed date+account verified, both render paths still carry the account), `restamp.py` (26s skew). Verified **origin == HEAD** (`bf3e345`) and **House mirror byte-identical** (sha256 ×3). **SW hg-v45**, data.js `?v=20260608120917`.


### ✅ LIVE — 🔔 In-app REMINDERS (Today + Timeline) + 🚙 DMV daily + PART 1–4 finance edits (Jun 8 2026, commit `2316638..75956bb`, ONE commit, SW **hg-v44**, data.js `?v=20260608112359`, stamp "Jun 8 · 11:25 AM")
**Built + deployed in one pass on "GO LIVE."** Fresh clone of `2316638` in the Linux sandbox; ALL edits via python (NOT the Edit tool — it truncates the OneDrive-synced files). `node --check` ×4 clean (data.js · recipes.js · sw.js · extracted index.html inline JS), an 89-check extended harness green, PLUS a functional render sim proving reminders surface on **BOTH** Today (date===today) and Timeline (on their dates), money ones carrying their account, and the DMV daily reappearing next day + retiring after the appt. `python3 restamp.py` (42s skew, America/Denver). Verified **origin == HEAD** (same commit object `75956bb`) and **House mirror byte-identical** (sha256 on index.html · data.js · sw.js). Live Pages reachable (first sw.js fetch 200); the CDN byte-compare couldn't finish because the fetch tool timed out — Pages rebuilds from `main` automatically and the SW bump to **hg-v44** forces clients to refresh.

**✅ CONFIRMED: reminders render on BOTH Today and Timeline.**

**WHAT SHIPPED**
- **PART 1 — REMINDERS:** new `REMINDERS` array (8 seeds) + `DMV` config at the top of `data.js`'s FINANCE area; done-state lives in `laundry` (`"rem-"+id` one-shot; `"rem-dmv-"+todayKey()` for the daily). `renderToday()` emits each reminder on its date as a checkable `add(...)` card via a shared `reminderCardHtml()` — **every money reminder shows its account as a 💳 pill**; `renderTimeline()` injects each on its matching date (account inline) via a new `tlRem` map merged into the day projection. Seeds: Jun 11 verify Car Wash+Crunchyroll (Cap One 360 checking) · Jun 14 play Game Pass · Jun 15 cancel Game Pass · Jun 15 verify Uber One · Jun 17 verify Game Pass cancelled · Jun 18 cancel Disney+ (Merrick •4735) · Jun 19 verify Disney+ (Merrick •4735) · standing Petco ammonia-tester errand (no account).
- **PART 2 — DMV daily:** recurring daily Today card through `dmvApptDate()` (default 2026-06-25, editable via `laundry["dmv-appt"]`), date-keyed so checking clears only today and it reappears tomorrow (= "no cancellation slot, try again"); "📅 Appt: … · Got an earlier slot? Update the appt →" affordance calls `updateDmvAppt()`; the Jun 25 appointment renders on the Timeline; the daily retires once `todayKey()` passes the appt.
- **PART 3:** finance subtabs reordered to lead with ✅ To-dos + `finSub` default flipped to `"todos"`; removed `ft_savor`; added 🟡 `ft_affirm` "Figure out Affirm (Amazon) payments"; Print 📋 To-Do shows "🎉 Got the Sovol filament dryer!" + weekly checkable "💧 Check & recharge the dryer's silica" (`togglePrintSilica()`, iso-week keyed).
- **PART 4:** Debts tab no longer renders the cancel list (Overview still sums `FINANCE.cancels`; the "verify below" pointer was dropped); Bills loan rows now show TOTAL owed — family **$10,000**, Nissan/Lendmark/student/IRS = **"unknown — tap to set →"** (taps to ❄️ Debts) — wired via `billTotalHtml` + `LOAN_DEBT` map; 5 new 🟡 `ft_total_nissan/lendmark/student/irs/family` to-dos with `ft_gather` folded out; Smart Start split into "🚙 Smart Start — maintenance" (7th) and "🚙 Smart Start — lease" (21st), no "1 of 2"; `psn` removed from Bills + `trk_psn` removed from 📅 Track, new 🟠 `ft_psn` "Cancel PlayStation Plus (annual, ~$177.18)" (🟠 header amount unchanged).

Note: the handoff's "❄️ Debts cancel list" phrase in `mon_gamepass` did not exist in `2316638` (that card has no note) — nothing to fix.

### ✅ LIVE — 💰 Finance ✅ To-dos: removed the Home Depot/May-statements audit to-do (audit FINAL) (Jun 8 2026, commit `4102432..2316638`, ONE commit, SW **hg-v43**, data.js `?v=20260608180000`, stamp "Jun 8 · 10:09 AM")
**One-line removal on "GO LIVE."** Fresh clone of `4102432` in the sandbox; deleted `ft_audit` ("Send the Home Depot card + May checking statements to finish the subscription audit") from the 🟡 tier — Cyrus considers the subscription audit complete and does not want to be asked for statements again. No replacement task; `ft_po` is now the last 🟡 item. Everything else identical to 4102432. `node --check` ×4 clean, harness green, `restamp.py` (44s skew). Verified origin + House mirror byte-identical; Pages built. **SW hg-v43**, data.js `?v=20260608180000`.

### ✅ LIVE — 💰 Finance ✅ To-dos: finalized subscription audit — cancel list + 📅 annual-renewals tracker (Jun 8 2026, commit `e19484e..4102432`, ONE commit, SW **hg-v42**, data.js `?v=20260608170000`, stamp "Jun 8 · 10:01 AM")
**Built + deployed in one pass on "GO LIVE".** Fresh clone of `e19484e` in the Linux sandbox, all edits via python/sed (NOT the Edit tool — it truncates the OneDrive-synced files). `node --check` ×4 clean, 82-check harness green, `python3 restamp.py` (16s skew). Verified origin raw blobs byte-identical, House mirror byte-identical (sha256), Pages built at `4102432`.

**WHAT CHANGED (all `data.js` FINANCE + `index.html` renderFinance):**
- **🟠 Cancel list (~$144/mo)** — now exactly the active set, each checkable w/ amount: `ft_gamepass` $31.11 · `ft_carwash` Super Star Car Wash $24.00 · `ft_disney` $21.69 · `ft_crunchy` $15.18 · `ft_uberone` $9.99 (keeps the DashPass-redundancy note) · **NEW `ft_claudepro`** "Cancel leftover Claude Pro — billed twice ($21.03 ×2), the OLD plan still charging on top of Claude Max". Removed (confirmed gone, no recent charge): ChatGPT, DoorDash DashPass, EverAI, Epoch.com AIPX, Earth Breeze — now a tidy **"✅ Confirmed gone" footer** under the tier (new tier-level `footer` render).
- **CORRECTIONS:** Claude = **Claude MAX $100/mo (KEEP)** — `keeping` line updated; the $21.03×2 is the old Pro to cancel (above). PlayStation = **annual $177.18, renews ~early March** → moved into a new **"📅 Track — annual renewals"** display-only tier (`info:true`, non-checkable, not counted in the open tally) with Mint Mobile (annual, keep, "confirm amount"); `bills` psn/mint entries corrected to match.
- **🟡 ADDED** `ft_audit`: "Send the Home Depot card + May checking statements to finish the subscription audit" (those accounts aren't in the data — any subs on them are unaudited).
- **📞 Mondays → 11** (dropped `mon_chatgpt`/`mon_epoch`/`mon_dashpass`); `MON_FT` relinked; single-source-with-Mondays preserved (`finTaskDone`); `mon_sweep` still the only standalone.
- **Render additions:** tier-level `footer` (muted note) + `info:true` tiers (display-only rows, no checkbox, skipped from open-count). Kept: Prime date-gate (Jun 30), Savor reframe, card-overhaul `steps[]`, 🟢 buffer + •6605, add-input, tier badges. **SW hg-v42**, data.js `?v=20260608170000`.

**DEPLOY (safe path done):** fresh clone `e19484e` → sandbox python edits → `node --check` ×4 → harness → `restamp.py` → ONE commit `4102432` via `.deploy-token` → push → verified origin + House mirror byte-identical + Pages built.

### ✅ LIVE — 💰 Finance ✅ To-dos rework: real itemized subscriptions · Prime date-gate · Savor reframe · card-overhaul plan (Jun 8 2026, commit `b23df85..e19484e`, ONE commit, SW **hg-v41**, data.js `?v=20260608160000`, stamp "Jun 8 · 9:38 AM")
**Built + deployed in one pass on Cyrus's "GO LIVE".** Fresh clone of `b23df85` in the Linux sandbox, all edits via python/sed there (NOT the Edit tool — it truncates the OneDrive-synced files). `node --check` ×4 clean, harness green (97 + 6 scoped checks), `python3 restamp.py` (6s skew). Verified: origin raw blobs byte-identical, House mirror byte-identical (sha256), GitHub Pages built at `e19484e`.

**WHAT CHANGED & WHY (all in `data.js` FINANCE + `index.html` renderFinance):**
- **🟠 Itemized subscription purge (~$196/mo)** — dropped the "pull a statement / verify each canceled" tasks (`ft_statement`,`ft_verify` removed). Now each real sub is its own checkable line WITH amount: `ft_gamepass` $31.11 · `ft_chatgpt` $20.00 (keeping Claude) · `ft_disney` $21.69 · `ft_carwash` Super Star Car Wash $24.00 · `ft_crunchy` $15.18 · `ft_epoch` Epoch.com AIPX $14.95 · `ft_everai` $12.99 · `ft_uberone` $9.99 · `ft_dashpass` $9.99 (Uber One/DashPass flagged redundant) · `ft_earthbreeze` (buy as-needed). Each new `ft_` id is relinked in `MON_FT` to its 📞 Mondays card.
- **📦 Amazon Prime date-gated** — new `showFrom:"2026-06-30"` field; hidden in the To-dos until Jun 30, then appears in 🔴 Do First as "Cancel the Amazon Prime trial before it bills ~Jul 5" (deadline pill `2026-07-05`). Render gates by `showFrom<=todayKey()` in both the open-count and the tier map; empty tiers are skipped. The separate Jul 2 schedule-app reminder is untouched.
- **💳 Savor de-doomed** — removed all interest-compounding / 30%-APR / "$0 rule" framing. Debt entry → `apr:""`, `note:"keep current — autopay on"`, badge text "🟢 Working card — autopay on" (kept `keepZero` so it isn't shown "PAID"); the debt sub-line now renders APR conditionally. Both Coach tips reframed to "the working card — keep autopay on so it never falls behind." New 🔴 Do First item `ft_savor`: "Set Savor to autopay (at least the minimum) so no bill ever falls behind."
- **🛒 Amazon Fresh → 🔴 Do First** (`ft_fresh`, note: no Fresh charge found in statements — cancel anyway per his call).
- **Removed entirely:** duplicate-Claude (`ft_dupclaude` + `mon_claude` + the `dupclaude` cancel-list entry — he handled it) and **Kalshi** (`ft_kalshi` + its Coach tip — done with the app).
- **🟡 Card-overhaul plan** — `ft_cardreset` relabeled to "Credit-card overhaul plan — ditch the bad cards + get one good one WITHOUT tanking your score" with a 5-item `steps[]` sub-list (keep old paid-off cards OPEN · pay down utilization first, snowball •6605 → up · then ONE 0%-APR balance-transfer / low-APR card · one application at a time · no card-number reset). `mon_cardreset` retired. New render support for `steps[]` (arrow sub-lines) and item `note`.
- **Kept:** 🟡 Xfinity / family loan / Farmer's Dog / `ft_po` "Figure out payments for PO"; 🟢 $500 buffer + •6605 snowball; the add-your-own input; tier badges; single-source-with-Mondays (`finTaskDone`). **Mondays = 14** (verify cards reworded to "Cancel X — $amt"; `mon_sweep` the only standalone). **SW hg-v41**, data.js `?v=20260608160000`.

**DEPLOY (safe path done):** fresh clone `b23df85` → sandbox python edits → `node --check` ×4 → harness → `python3 restamp.py` → ONE commit `e19484e` via `.deploy-token` → push → verified origin + House mirror byte-identical + Pages built.

### ✅ LIVE — 💰 Finance ✅ To-dos: prioritized MASTER money list + 📞 Mondays single-source consolidation (Jun 8 2026, commit `b23df85`, pushed `b68138c..b23df85` — ONE commit, SW **hg-v40**, data.js `?v=20260608153000`, stamp "Jun 8 · 9:17 AM")
**GO LIVE was pre-granted in the Jun 7 handoff; built + deployed in one pass.** Fresh clone of `b68138c` in the Linux sandbox, all edits done there. `node --check` ×4 clean, a 106-check harness green, `python3 restamp.py` external-time stamp (9s skew). Verified: origin raw blobs byte-identical, GitHub Pages build confirmed `built` at `b23df85`, House mirror byte-identical (sha256 match, no truncation).
⚠️ **Build note for next time:** the OneDrive-synced Edit-tool writes truncated `data.js` and `index.html` mid-file twice during this build. Recovered by restoring from `git HEAD` and patching via python in the sandbox. **Lesson: edit the app files in the sandbox/clone (python/sed), then mirror to House with `cp` + a `sha256sum` equality check — don't trust the Edit tool on the large synced files.**

**WHAT CHANGED & WHY**
- **`data.js` `FINANCE.todosSeed`** — flat 10-item array replaced by a 5-tier structure `[{tier,col,items:[{id,label,deadline?}]}]`. Tiers: 🔴 Do first `#E0506A` (`ft_prime`[deadline 2026-07-02] · `ft_savor`[NEW] · `ft_latefee`) · 🟠 Subscription purge ~$157/mo `#FB923C` (`ft_statement`[NEW] · `ft_verify` · `ft_carwash`[NEW] · `ft_dupclaude`[NEW]) · 🟡 Big structural savers `#FBBF24` (`ft_xfinity` · `ft_family` · `ft_zoeyfood`[kept prereq] · `ft_farmersdog`[NEW] · `ft_cardreset`[NEW] · **`ft_po`[NEW] "🧾 Figure out payments for PO"** — Cyrus's post-handoff addition, exact "PO" shorthand kept) · 🟢 Foundation `#4AD490` (`ft_buffer`[NEW] · `ft_kalshi`[NEW] · `ft_snowball`[NEW]) · 🔵 Also open `#7EB8F0` (`ft_fresh` · `ft_progress` · `ft_gather` · `ft_autopay` — legacy seeds, nothing dropped). Reused ids preserve existing Firebase `fin.todoDone` check state.
- **`index.html` `renderFinance()` To-dos branch (~line 2984)** — rebuilt to iterate the tiers: uppercase color-coded tier label + colored left-border card, `finTodoRow` per item (red ⏰ deadline pill on `ft_prime`), open-then-done within each tier, a "✍️ Your additions" group, and the add-your-own `#finTodoInput` box **unchanged**.
- **NO DUPLICATE STATE (hard req met):** new `const MON_FT` link map + `finTaskDone(id)` (done if `fin.todoDone[ft]` **OR** `fin.monday[mon]`). ✅ To-dos is the single master checkable surface; the 15 overlapping 📞 Mondays cards now render a "📋 Tracked on your ✅ To-dos list" pointer (taps through to the To-dos sub-tab) **instead of an independent checkbox** — only `mon_sweep` keeps its own. The Mondays queue (`monOpen`/`fmNext`) and the Monday **Today** card now read `finTaskDone`; the Today checkbox routes through `finTaskToggle` (→ the master To-do for linked items); `finTodoToggle` also refreshes the Today card. Pacing / queue-advance intact, verified by harness.
- **`sw.js`** cache → `hg-v40`; **`index.html`** data.js cache-buster → `?v=20260608153000`.

**WHERE / DEPLOY STEPS DONE (the safe path):** fresh clone `b68138c` → sandbox python/sed edits → `node --check` ×4 → 106-check harness → `python3 restamp.py` → **ONE** commit `b23df85` via `.deploy-token` → `git push origin main` → verified origin byte-identical + Pages `built` + House mirror byte-identical. Source for finance content stays `home-and-garden-project/FINANCE_STATE.md`; future amount/bill/debt edits go in the `const FINANCE` block at the END of `data.js`.

- [x] **Finance tab build — DONE, LIVE in `2c8b497`** (see entry below). Source stays `home-and-garden-project/FINANCE_STATE.md`; future amount/bill/debt edits go in the `const FINANCE` block at the END of `data.js`. Check-off/balance state lives in Firebase node `fin`.
- [ ] **CORRECTION (Jun, from Financial Stability chat) — Xfinity retention leverage:** the call should cite **CenturyLink ~$55/mo**, NOT "AT&T Internet Air $47" (AT&T only had 5G Internet Air; CenturyLink is the more credible wired-competitor name to drop, and he's NOT actually switching). Update the Xfinity item/script in the `const FINANCE` block of `data.js` accordingly, redeploy on GO LIVE. Also note corrections: PlayStation is **$177.18 annual** (renews ~March), not $85.99; Mint Mobile amount unverified (no charge in any provided statement); Claude = $100 Max plan KEEP, the two $21.03 are old Pro charges to cancel.

### ✅ LIVE — 🌱 Per-plant frequency chips + 📱 mobile header fix + 🕐 deploy-stamp tz pipeline (Jun 7 2026, commit `b68138c`, pushed `3a663ca..b68138c` (one amend for restamp.py, single commit), SW **hg-v39**)
**GO LIVE pre-granted in the Jun 7 handoff; two mid-build additions folded into the SAME single commit on Cyrus's GO LIVE re-confirm.** Fresh sparse clone of `3a663ca` in the Linux sandbox (`/sessions/<session>/hgwork2` — /tmp full). ONE commit: `index.html` + `data.js` + `sw.js` + **new `restamp.py`**, data.js `?v=` → `20260607140500`.
1. **🌱 Frequency chips (the handed-off NEXT JOB):** all 21 `PLANTS` entries gained display-only `light` / `waterChip` / `harvestChip` fields per the researched cadence table (`days` stays the engine truth; harness asserts every waterChip's numbers contain the plant's `days` — "daily" ⇔ days:1). `renderGarden()` renders them via new `chip`/`chipRow` helpers (wrap-friendly pills, color-coded): ❤️ Care = light chip on every plant card · 💧 Watering = cadence chip on every card · 🌾 Harvest = chip on the 9 HARVEST_INFO cards **+ new display-only "🌱 Harvest outlook — everything else" list** for the other 12 plants (the Harvest sub-tab only ever rendered HARVEST_INFO plants — this is how ristra's "🌶️ not yet — first harvest ~10–12 weeks after transplant" and mint's not-yet line show without inventing harvest cards or new state). Zero behavior changes; indoor foliage plants honestly say "🌿 foliage — not harvested".
2. **📱 Mobile header fix (Cyrus mid-build report: "header spacing messed up again"):** in the ≤560px block — the util-pill row (incl. the live-temp Forecast pill) had **no flex-wrap; with flex-end justify it overflowed off the LEFT edge** on narrow phones → now wraps centered, pills keep tap height; weather card can now **shrink** (`.hdr-right{flex:0 1 auto;min-width:0}` + `.hdr-weather{max-width:100%}` + wx rows downsized 10–10.5px) so a worst-case 104°/100%-rain/28-mph/88%-humidity card fits a 360px screen beside the clock; `.tab` font 9px so the 6 main tabs fit. NOTE: headless Chromium (360+390px, real Google fonts, worst-case mocked weather) could NOT reproduce an overflow on the OLD header — the fix is structural/defensive (wrap + shrink make overflow impossible), so if his phone still shows something off, get a screenshot — it may be a different element.
3. **💊 Vitamins & Meds mobile check (Cyrus "couldn't find it"):** verified rendering in real Chromium at 360/390px — `#vitamins-meds` label renders, the daily-lineup card sits directly under the Litfulo card, Health tab has no horizontal overflow (screenshot taken). It's BELOW the Litfulo hero + refill tracker — likely just needs a scroll past the refill buttons; nothing was broken or hidden.
4. **🕐 Deploy-stamp pipeline FIXED FOR REAL (the "Jun 7 · 7:49 AM" stamp was wrong — actual time was Jun 6 ~7:49 PM MDT; the old conversion mangled the UTC date-rollover):** new **`restamp.py` committed to the repo root** — external UTC from GitHub's HTTP Date header → `zoneinfo("America/Denver")` → 12-hour stamp; **always runs an 8-case selftest first** (incl. THE bug case: 2026-06-07T01:49Z must stamp "Jun 6 · 7:49 PM"; plus noon/midnight edges, MST winter, DST spring-forward gap), then **asserts the written stamp is within 5 min of a second, independent external fetch — deploy MUST abort if it exits non-zero**. All future deploys: `python3 restamp.py` instead of hand-stamping. This deploy stamped **"Jun 7 · 8:13 AM"** (independent check agreed, 36s skew; sandbox UTC 14:13 ✓ = 8:13 AM MDT).
**Verify trail:** node --check ×4 (data.js, sw.js, inline ×2) + post-restamp re-check; **184-check jsdom harness** (21 plants × 3 fields, every chip renders on all 3 sub-tabs, waterChip↔days agreement per plant, ristra/mint not-yet lines, done/recovering states keep chips, all 10 renderers, vitamins label + lineup-under-Litfulo order, mobile CSS rules present, SW v39 + ?v= bump); **20-check real-Chromium layout harness** (playwright headless-shell, 360px + 390px, stateful Firebase stub with migration re-fire, worst-case weather mock w/ CORS headers, real Google fonts): no horizontal scroll, zero header/navbar overflowers, greeting col + weather card no overlap, 4 pills on-screen ≥24px tall, eyebrow on top line, 21 light chips fit, vitamins section verified — fixed files 20/20 (A/B: original 3a663ca fails only the chips-missing checks). restamp.py selftest 8/8. Push `3a663ca..b68138c`; origin blobs byte-identical ×4 (API raw); live 200 ×3 byte==pushed, verify ×2; **House mirror updated ×4 (incl. restamp.py at House root next to deploy-github.py)**. Screenshot `mobile_header_390.png` in session outputs.
**For Cyrus:** chips are live on Plants→Watering/Harvest/Care. The header stamp now reads true Denver time — it said "Jun 7 · 8:13 AM" at a moment your wall clock read 8:13 AM Sunday. If your phone's header still looks cramped anywhere, send a screenshot — the fix covered every overflow path we could measure, but your exact phone/font combo is the judge. ⚠️ Old `staged-finance-hg-v38/` + `staged-vitamins-hg-v36/` still need hand-deleting (sandbox can't delete from the House mount).

### ✅ LIVE — 💰 Car wash cancel + card-reset decision + 🌱 peppermint recovery (Jun 7 2026, commit `3a663ca`, pushed `29886aa..3a663ca`, SW **hg-v38**)
**Built + committed locally in the build session's sandbox (moved to `/sessions/<session>/hgwork` — /tmp filled up with undeletable prior-session clones); push deliberately HELD.** ONE commit (amended to fold in the mint update): `index.html` + `data.js` + `sw.js` + `images/mint_20260606.jpg` + archive move, data.js `?v=` bumped to `20260606174945`. Byte-identical fallback copies + commit message in **`home-and-garden-project/staged-finance-hg-v38/`** (live House mirror untouched — still matches origin `29886aa`).
1. **✂️ Cancel list:** new entry `carwash` "Car wash subscription", **`amt:0`** so cancelAll/cancelGot/cancelLeft and the One Number stay honest while the amount is unknown; note "amount unknown — pull it off the statement when you cancel, then update this entry". **One small render tweak in `renderFinance`:** amount column shows **"$? — check statement"** (11px) when `amt` is falsy instead of `$0.00`; known amounts render exactly as before.
2. **📞 Financial Mondays queue → 16 items**, both new ones slotted after the existing 13, before the final sweep (dynamic Monday dates auto-assign — they take the next open Mondays): **`mon_carwash`** "🫧 Cancel the car wash subscription" (note: grab the real amount off the statement, update the cancel-list entry) and **`mon_cardreset`** "💳 Decide: card reset to kill small subscriptions?" — note explicitly framed as **a decision, not advice, no answer pre-picked**: FOR = replacing the card number nukes ALL small autopays at once (fast zombie-charge kill) · AGAINST = breaks the autopays he WANTS (utilities re-entered one by one), some subs survive via the card networks' auto-updater, and balances are untouched · alternative stated = the queue is already killing one a week. Renders on the Mondays sub-tab and the Monday Today card like every other item; circle stays manual.
3. **🌱 Peppermint recovery (Cyrus's photo, Jun 6):** fresh new growth confirmed pushing up from the old cut-back stems (visually verified). Photo replaced: `images/mint_20260606.jpg` (EXIF-uprighted, 1000×563 q62, ~99 KB) wired into `PLANT_INFO.mint.photo`; the May 31 die-back photo moved to `archive/mint_20260606/mint_20260531_pre_recovery.jpg` + README (in the repo and mirrored under `home-and-garden-project/archive/`). Care copy flipped everywhere the HOLD lived: PLANTS `freq` → **"🌱 Recovering — new growth Jun 6; keep evenly moist, it's coming back. Gentle morning water"**, watering panel `when`/`thirst` → recovery guidance ("the HOLD is over"), `trim` → let new shoots establish, then pinch tips. The `overwater:true` risk badge deliberately KEPT (mint is still overwater-prone — "evenly moist, never soggy"). Zero `mint_20260531` references remain.
**Verify trail:** node --check ×4 (inline pre/post-restamp, data.js, sw.js); **25-check finance harness** + **20-check mint harness** (data flips exact, old language gone, photo file >50 KB + archived pair present, garden tab renders the recovery note + new photo, overwater badge kept, finance items intact post-amend, all 10 renderers) (cancels=11 w/ carwash last amt 0, Σ unchanged $371.98, "$? — check statement" renders while $215.05/$9.99 etc. stay money-formatted, mondays=16 in order carwash→cardreset→sweep, balanced FOR/AGAINST note markers, Mondays sub-tab renders both, queue-advance surfaces carwash then cardreset on the mocked Monday Today card w/ manual toggle, all 10 renderers) + regressions: curtain 30/30, work-items 13/13, vitamins 44/44. Stamp "Jun 6 · 5:50 PM" (external, 0s skew — restamp again at GO LIVE).
**GO LIVE executed Sun Jun 7.** Restamped "Jun 7 · 7:49 AM" (external, 0s skew), amended (final hash `3a663ca`), node --check ×3, pushed `29886aa..3a663ca`. Verified: origin blobs byte-identical ×4 (API raw, incl. the mint photo), live 200 ×4 byte==pushed, verify ×2, House mirror byte-identical ×4. ⚠️ Hand-delete the obsolete `staged-finance-hg-v38/` (and the older `staged-vitamins-hg-v36/`) — the sandbox cannot delete from the House mount.

### ✅ LIVE — 🔒 Password curtain + ⭐ Priority room fix + real-lock to-do + 2 one-off work items (Jun 6 2026, commit `29886aa`, pushed `ea24b0d..29886aa`)
**On GO LIVE.** Same sandbox sparse clone (now at `ea24b0d`). ONE commit: `index.html` + `data.js` + `sw.js` (**hg-v37**), data.js `?v=` bumped to `20260606145933`. Stamp "Jun 6 · 3:03 PM" (external GitHub Date header, 1s skew).
1. **🔒 Password curtain:** full-screen gate before the app — mountain time-of-day background (reuses `TODAY_BG[todaySeg()]` with a dark overlay), one centered password field (Playfair "Home & Garden" + 🏔️), Enter submits. Wrong → gentle shake (`curtainShake` keyframes) + clears + refocuses. Right (trim/lowercase tolerant) → `localStorage["hg_curtain_ok"]="zoey"` so the **device is remembered** — typed about once per device, ever. **`CURTAIN_PASSWORD = "zoey"`** is a clearly-marked constant at the very top of the inline script (5-second change; changing it auto-re-locks every device since the flag stores the password value). **Honest comments in code + HTML: privacy curtain, NOT security** — the page, scripts, and Firebase data remain publicly fetchable; the real lock is the Firebase Auth project (now queued, see #2). Fails OPEN if localStorage is unavailable. Curtain z-index 10000 (above the fb-loading veil); `curtainInit()` runs at the end of the inline script.
2. **⭐ Priority to-do + Rooms fix:** new `TASKS.auth_lock` "🔒 Big project: real app lock — Firebase Auth + locked database rules" (level **hard**, note spells out: curtain is cosmetic, data publicly readable, fix = Auth sign-in + locked DB rules; password constant location referenced). **Bug found & fixed while wiring it:** the `priority` room existed in `data.js` ROOMS/TASKS but was **missing from `renderRooms`' order list — the whole ⭐ Priority list (vacuum bags, GoodRx, Xfinity script, etc.) never actually rendered anywhere.** "priority" now leads the Rooms tab. The daily one-task rotation (`oneTaskForDate`/`todaysPlan`/`RHYTHM_ROOM_ORDER`) was deliberately left untouched — priority items are errands, not the house rotation (harness-asserted).
3. **💼 Today one-offs (windows auto-expire after Sun Jun 7):** "Finish work training" (Sat copy: "Tonight — or tomorrow"; Sun: last-call) and "🕐 Do timecard — ready for Bill by Monday" (Sat: "By Sunday night"; Sun: "Today — Bill needs it Monday"). Ordinary manual `checked[]` circles (`work_training_jun6` / `timecard_jun8`), date-gated `2026-06-06..07`, gone Monday regardless of state.
**Verify trail:** node --check ×4 (inline ×2 post-edit/post-restamp, data.js, sw.js); **30-check curtain harness** (locked device: curtain visible + bg wired + autofocus; wrong pw shakes/clears/stays locked/no flag; right pw — incl. " ZoEy " — sets flag + opens; remembered device hidden on load; stale flag stays locked; localStorage-blocked fails open; auth_lock in data + renders w/ note when ⭐ Priority expanded; all 10 renderers) + **13-check work-items harness** (Sat/Sun/Mon mocked: presence, per-day copy, check-off strips, Monday auto-expiry, rest-day + Financial Monday intact, Rooms order ⭐-first w/ vacuum bags visible + 7 rooms, `oneTaskForDate` never picks priority) + **44-check vitamins regression** all green. Push `ea24b0d..29886aa`; origin blobs byte-identical ×3 (API raw); live 200 ×3 byte==pushed, verify ×2; House mirror byte-identical ×3.
**For Cyrus:** the password is **zoey** (lowercase, but caps/spaces are forgiven). First open on each device asks once; after that it remembers. To change it later: top of index.html's main script → `CURTAIN_PASSWORD`.

### ✅ LIVE — 💊 Vitamins & Meds section (Health tab) + Sunday case-refill link-up (Jun 6 2026, commit `ea24b0d`, pushed `2cf52a9..ea24b0d`, SW **hg-v36**)
**GO LIVE executed Jun 6.** Was staged as local `60d81ab`; on GO LIVE: re-restamped "Jun 6 · 2:56 PM" (external GitHub Date header, 1s skew), amended (stays ONE commit `ea24b0d`: `index.html` + `sw.js`, no `?v=` bumps needed), node --check ×2 post-restamp, pushed `2cf52a9..ea24b0d`. Verified: origin blobs byte-identical ×2 (API raw), live 200 ×2 byte==pushed (~40 s CDN), House mirror byte-identical ×2. ⚠️ The sandbox cannot delete from the House mount — leftover `home-and-garden-project/staged-vitamins-hg-v36/` (staging fallback, now obsolete) is safe to delete by hand.
1. **💊 Health tab:** "💊 Medications" section is now **"💊 Vitamins & Meds"** (`id="vitamins-meds"`). Litfulo hero card + refill tracker kept unchanged. New **"🕐 The daily lineup — times matter"** card, 4 tappable rows (each a manual check, `laundry["well-<key>-<date>"]` via existing `toggleWellness`; **existing `eso`/`litfulo` keys reused — zero duplicated state**): 🌅 8:00 AM wake = Esomeprazole ALONE, 30–60 min pre-breakfast (bottle by the coffee maker) · 🍳 ~8:45 breakfast = Litfulo + Fish Oil 1200mg + Biotin 5000mcg (+ Zinc 50mg with a live **"today ✓"** flag on Mon/Wed/Fri, "(not a zinc day today)" otherwise) — Litfulo shows an auto **"📦 pending pickup"** chip while no refill is logged (`!litfulo.startDate`; logging a refill removes it, no code change needed) · 🥪 Noon = Metamucil gummies + BIG glass of water, kept away from morning meds · 🌙 10:00 PM screens-off = Melatonin 3mg + L-theanine ("training wheels for the sleep rebuild — revisit once the cycle holds"). Amber standing note: **skip biotin 2–3 days before any blood draw** (skews labs; Litfulo has lab monitoring) + run the lineup past the pharmacist at Litfulo pickup. Bottom italic note: shelved as redundant — Triple Omega 3-6-9 · Flaxseed Oil · Hair/Skin/Nails gummies.
2. **📦 Case-loading guide card** "Sunday night: load the case — 2 min" (`id="vitamin-case-guide"`, the deep-link anchor): 7 numbered steps — all-7 breakfast slots fish oil + biotin (step text adapts: "+ 1 Litfulo" once a refill is logged) · M/W/F add zinc · esomeprazole stays by the coffee maker · melatonin bottle lives with the book (bottle = the 10 PM reminder) · Metamucil stays in the kitchen (gummies melt in cases) · bottle under ~a week → grocery list · labs this week → pull biotin from the next 2–3 days of slots.
3. **📅 Sunday Today item** (Sundays only, order 1290, before the 10 PM wind-down): "💊 Refill the vitamin case" — card **body** tap = new `goVitaminCase()` (switchTab to wellness + smooth-scroll to `#vitamin-case-guide`); **circle** tap (stopPropagation) = manual check (`well-vitcase-<date>` — auto-resets each Sunday by date key). Checked → standard done strip in the pile. Inserted in `renderToday()` right above the Zoey-removed comment; `goVitaminCase` lives right after `toggleWellness`.
**Verify trail (done at build):** clone HEAD verified `2cf52a9`; node --check ×4 (inline pre/post-restamp + sw.js ×2); **44-check vm harness** (TZ=America/Denver) across 3 mocked dates — Sat Jun 6 (all 10 renderers; section/anchor/lineup rows/timing text; pending chip; NOT-zinc-day; labs+pharmacist note; 7 guide steps; shelved note; refill tracker kept; NO Sunday item; fiber+melatonin toggle/untoggle persist), Sun Jun 7 6 PM (card + pill + sub render; goVitaminCase switches tab AND scrolls the anchor; circle wired with stopPropagation; check-off → strikethrough done strip; dinner-proposals pointer coexists), Mon Jun 8 (zinc "today ✓" in title; refill-logged kills the pending chip + guide says "+ 1 Litfulo"; no Sunday item; rest-day + Financial Monday intact); goVitaminCase defined exactly once; sw.js hg-v36. Stamp currently "Jun 6 · 2:38 PM" (external GitHub Date header, 1s skew).
**Deployed.** (The staged-folder fallback path was never needed.)

### ✅ LIVE — Three automations: grocery auto-build · Sunday dinner proposals · Monday balance fold-in (Jun 6 2026, commit `2cf52a9`, pushed `93687c7..2cf52a9`)
**On GO LIVE.** Fresh shallow sparse clone of `93687c7`. ONE commit: `index.html` + `sw.js` (**hg-v35**). Stamp "Jun 6 · 2:20 PM" (external-time verified, 1s skew). **Standing boundary honored: NOTHING auto-disappears or auto-checks — every item keeps its manual tap.** Tonight's extra ingredients (dough/parmesan/lemon) deliberately NOT added — the feature only fires on NEW queueing, no retro-fill (harness-verified migrations never call it).
1. **🛒 Groceries auto-build:** new `grocFromRecipe(rid)` + `ingFood()` parser (strips qty/units/prep/parentheticals) + `GROC_STAPLE_WORDS` skip-list (items made only of salt/pepper/water/oil/olive words) + loose contains-matching vs Kitchen Inventory (`grocStockLoose`) and the list (`grocListLoose`, builtin+custom, de-dupe). Fires from `addCook()` and the Sunday ✓ Queue; adds missing food items to `grocCustom` labeled **"(from: 🍝 <recipe>)"**; toast reports the count. Add-only. `buyGroc` now strips the from-tag so inventory names stay clean (only behavioral change to existing code).
2. **📅 Sunday dinner proposals** (`isSundayEve()` = Sunday ≥3 PM): To Cook tab tops with "This week's dinner ideas" — up to 5 box dinners ranked by `invMatchOf` (on-hand/total non-staple ingredients; excludes dog, draft, side/breakfast/salad/dessert tags, already-queued, swapped-away). Each card: on-hand chip, tap-through to the recipe, **day select + ✓ Queue** (queues dinner, triggers #1), **↻ swap** (transient `sunSkips`, resets on reload). Sunday Today flow gets a small passive pointer card (top 2 names → taps to Food→To Cook). Nothing queues without his tap.
3. **💳 Monday balance fold-in:** the 📞 Financial Monday Today card gains "Update balances — 1 minute, 4 numbers" → stepper (`finMonBalIdx` transient, `FIN_MON_BAL`: 360 Checking → 360 Savings → SAVOR → Quicksilver •6605), one type-in each saved via existing `finSet` (**dated**, feeds the same trends as tap-to-edit), skip button per step, ends "✓ Balances logged — now the call. (The circle stays yours to tap.)" — `fin.monday` untouched by the stepper.
**Verify trail:** node --check ×4 + extracted inline (post-restamp); **42-check vm harness** across 3 mocked dates — Sat Jun 6 (parser unit tests incl. staples; grocFromRecipe adds exactly the 3 missing, from-tags, in-stock skipped, zero auto-checks, re-run de-dupes to 0, purchased dupe re-adds; buyGroc strips tag → clean inventory key; NO proposals/Monday card on Sat; all 10 renderers), Sun Jun 7 6 PM (section + 5 cards + chips, ranked desc, drafts/dog/sides excluded from proposals, ↻ removes, ✓ queues dinner + groceries, queued rid leaves pool, Today pointer card), Mon Jun 8 (card + button, 4-step walk: checking saved dated 123.45 → savings skipped stays unset → savor/c6605 dated, done note, fin.monday EMPTY after full walk, toggle still rendered manual, no proposals on Mon), hg-v35. Push `93687c7..2cf52a9`, origin blobs byte-identical ×2, live 200 ×2 byte==pushed, House mirror ×2.

### ✅ LIVE — Sauce split (spaghetti RED-only · flatbread PESTO-only) + flatbread photo (Jun 6 2026, commit `93687c7`, pushed `de3bd1a..93687c7`)
**On GO LIVE.** Fresh shallow sparse clone of `de3bd1a` (same `--filter=blob:none` + sparse trick — /tmp is still nearly full). ONE commit: `index.html` + `recipes.js` + `sw.js` (**hg-v34**) + `images/recipe_squash_flatbread.jpg`. Stamp "Jun 6 · 1:46 PM" (external-time verified, 0s skew). recipes.js `?v=` bumped. **Both new recipes STAY `draft:true` — permanent only when Cyrus says so (approval = remove the draft key on `r_box_squash_flatbread` + `r_box_romaine_spinach_salad`).**
1. **🍝 Spaghetti = RED ONLY:** renamed **"Hot Italian Sausage Spaghetti in Red Sauce"** — pesto removed entirely (ingredients + steps + blurb; culinary logic in the blurb: tomato acid cuts the sausage fat). 4 steps, 6 ingredients. Plate note: "the rich RED half of tonight's plate." **Photo kept** — the John Herschell CC BY-SA 2.0 sausage-in-red-tomato-sauce shot is an even better match now.
2. **🥖 Flatbread = PESTO ONLY:** pesto moved INTO the base — "Spread a THIN, even layer of pesto" on the dough **before** shingling the squash ribbons (step 3 → shingle *over the pesto* step 4); rationale in blurb/note: red sauce would sog the thin ribbons, basil + summer squash + parmesan is the classic. Optional tiny fresh-pesto drizzle after baking kept (step 6). Plate note: "the herby GREEN half." Garlic-oil base dropped; oil now just brushes the tops.
3. **📷 New Commons photo for the flatbread:** curl+Commons-API pipeline, 6 candidates contact-sheeted, license verified from `extmetadata` — **"Nathan's tart.jpg" by Joannaberg4, CC BY-SA 4.0**, visually confirmed (mandoline-thin zucchini shingled over golden flat dough, zero red) → optimized 450×337 q72 (36 KB) `images/recipe_squash_flatbread.jpg`, `photo` + `photoCredit` wired on the draft recipe.
4. **🥗 Salad untouched** (still DRAFT). Draft chips render unchanged in all 3 places; migration `jun6-dinner3-v1` untouched.
**Verify trail:** node --check ×4 + extracted inline (post-restamp); **19-check harness** — main (renamed, ZERO pesto matches, no squash, red sauce + sausage + 4 steps, plate note, NOT draft, Herschell photo kept), flatbread (draft:true, pesto ingredient, zero red-sauce/tomato, pesto-spread step index < shingle step index, drizzle + 425°/18–22 kept, plate note + sog rationale, photo + credit wired, file >10 KB), salad untouched, hg-v34, chips + migration intact in index. Push `de3bd1a..93687c7`, origin blobs byte-identical ×4 (API raw), live 200 ×4 byte==pushed, House mirror byte-identical ×4. Scratch `_flatbread_contact.jpg` / `_flatbread_final.jpg` may appear in session outputs — safe to delete.

### ✅ LIVE — Tonight's 3-part menu (DRAFT chips) + 📞 Financial Mondays + greenhouse seasonal rethink (Jun 6 2026, commit `de3bd1a`, pushed `871a181..de3bd1a`)
**On GO LIVE.** Fresh shallow sparse clone of `871a181` (sandbox /tmp nearly full — used `--filter=blob:none` + sparse-checkout of the 4 files; future chats copy this). ONE commit: `index.html` + `data.js` + `recipes.js` + `sw.js` (**hg-v33**). Stamp "Jun 6 · 1:35 PM" (external-time verified vs GitHub Date header, 1s skew). Both `?v=` bumped to `20260606133530`.
1. **🍝 Tonight's dinner refined into a 3-part menu (DRAFT status — Cyrus's approval pending for permanent save):** main `r_box_sausage_squash_spaghetti` reworked — renamed **"Hot Italian Sausage Spaghetti with Pesto-Kissed Red Sauce"**, zucchini/yellow squash REMOVED from ingredients+steps (4 steps now), note points to the flatbread. Two NEW recipes with **`draft:true`**: **`r_box_squash_flatbread`** "Squash Ribbon Flatbread" (mandoline-thin zucchini+yellow squash shingled over pizza dough/puff pastry, garlic oil, parm, 425°F 18–22 min, optional pesto drizzle) + **`r_box_romaine_spinach_salad`** "Romaine & Spinach Side Salad" (Dijon-lemon vinaigrette, optional parm). RECIPES now 51. **Draft chip renders in 3 places:** recipe card ("🕒 DRAFT · pending approval" badge), recipe detail page (meta chip), To Cook card (small 🕒 DRAFT pill). Migration **`flags/jun6-dinner3-v1`** queues `ck-seed-side1` + `ck-seed-salad1` for 2026-06-06 dinner next to the existing spaghetti seed. **⚠️ APPROVAL = flip `draft:true` off (or delete the key) on those two recipes in recipes.js — nothing else.**
2. **📞 Financial Mondays — new 6th Finance sub-tab** (`["mondays","📞 Mondays"]` between Debts and To-dos): paced queue, ONE retention call or cancellation per Monday so he's never overwhelmed. Data: **`FINANCE.mondays`** (14 items, end of the FINANCE block in data.js); done state **`fin.monday[id]=dateKey`** (`finMondayToggle`); **dates assign dynamically** — first open item = next Monday (`finMondayKey(offset)`), so completing one auto-advances the rest; deadline field renders an amber "jump the line" warning if the computed Monday slips past it. **Queue order set (priority: time-sensitive first):** Jun 8 property-mgmt late fee → Jun 15 Xfinity retention → Jun 22 family renegotiation ($400→$200) → Jun 29 Amazon Prime trial + Fresh (deadline Jul 2) → dup Claude charge $21.03 → Game Pass verify → **~Jul 20 cancel The Farmer's Dog** → Disney+/ChatGPT/Crunchyroll/Epoch+EverAI/Uber One/DashPass statement verifications (one a week) → final sweep. **Today flow:** on Mondays only (`dow===1`, order 330) a "📞 Financial Monday" card shows this week's single item with its note + check circle (same `finMondayToggle`); after check-off it becomes a done strip ("pool league is guilt-free 🎱") and does NOT surface the next item until next Monday.
3. **🌡️ Greenhouse rethink — it's EMPTY in hot June ON PURPOSE:** the hot-day per-plant advisory "open the greenhouse flap so it doesn't cook" REMOVED from `wxWaterNote` (greenhouse branch deleted); Today wind card reworded ("the empty greenhouse is a ready-made wind shelter" — no flap). Care-page **Greenhouse Plan rebuilt**: new **status card** (current mode + SWITCHES-BY-MONTH chip + 3 active suggestions + research footnote) above the 4 season rows. Modes by month: **SUMMER (May–Aug) = Solar Workhorse, kept empty** — drying/curing rack (free solar dehydrator: herbs ~1 day, garlic/onions ~2 wks on mesh), fall-seedling station from ~Jul 15 under **30–50% shade cloth (50% at CO altitude)**, sweet-potato slip/cutting propagation (75–85°), solarize used potting soil in clear bags; **FALL (Sep–Oct) = season extension** (the real window, greens in before ~Oct 5–15 first frost); **WINTER (Nov–Feb) = cold frame/overwinter** (rosemary + tender pots, freeze-thaw shield); **SPRING (Mar–Apr) = seed starting** (~May 15 last frost). Research cited in-app: Ceres Greenhouse Solutions + CSU PlantTalk (unshaded CO greenhouse >110° — shade or repurpose), SARE project FNC13-933 (greenhouse solar dehydrator), Sustainable Market Farming (fall brassicas under shade cloth, 50% at altitude; sweet-potato slips).
**Verify trail:** node --check ×4 + extracted inline (pre/post restamp); **71-check vm harness** (Sat Jun 6 ctx + mocked Mon Jun 8 ctx) — recipes (51 total, main squash-free w/ 4 steps + sausage/sauce/pesto kept, both drafts' ingredients/steps, draft chips on card+detail+To Cook and NOT on non-drafts), migration present + simulated → all 3 render queued tonight w/ exactly 2 DRAFT pills, Mondays (14 items, order, deadline math: Jun 29 ≤ Jul 2 ✓, THIS-MONDAY star, queue advance on check-off, done pile, no leakage into other sub-tabs, todos intact), Monday-key math (Sat→Jun 8; Mon→same day), Today card renders ONLY on Monday + done-strip behavior, greenhouse (zero flap strings, status card + 3 suggestions + NOW chip + 4 rows + cites + upgrades box + care buckets intact), all 10 renderers + 10 switchTab, hg-v33 + both v-bumps. Push `871a181..de3bd1a`, origin blobs byte-identical ×4 (API raw), live 200 ×4 byte==pushed, House mirror byte-identical ×4.

### ✅ LIVE — Finance sub-tabs + SAVOR $190 + Zoey one-pot redo + topper transition (Jun 6 2026, commit `871a181`, pushed `b300502..871a181`)
**On GO LIVE.** Fresh shallow clone of `b300502`, ONE commit: `index.html` + `data.js` + `sw.js` (**hg-v32**). Stamp "Jun 6 · 1:14 PM" (external-time verified, 1s skew). data.js `?v=` bumped.
1. **💰 Finance is now 5 sub-tabs** (new `finSub` state + `switchFinSub` + renderSubtabs entry): **📊 Overview** (One Number + payday countdown · Accounts · $500 buffer) · **📅 Bills** · **❄️ Debts** (snowball + cancel list) · **✅ To-dos** · **🧠 Coach** (rotating tips + the dog-food cost analysis). Mechanism: every section still computes; section boundaries emit `\u0001<tag>\u0001` sentinels and `renderFinance` filters segments by `finSub` at the end — tiny diff, no scope surgery, all interactions (tap-to-edit, check-offs) re-render within the active sub-tab.
2. **💳 SAVOR truth-up:** migration **`flags/jun6-savor-v2`** dates `fin.debt.savor = $190 @ 2026-06-06` ($190 of 3D-print supplies). Keep-zero badge auto-drops (existing guard), card behaves as real debt; data.js note now says "clear next paycheck, then back to the $0 rule". **No hypothetical print-sale income anywhere in the One Number** (decision). New **Print → Ideas** entry `print_sidehustle` ("🖨️ Print side-hustle?", aqua vibe, honest framing: functional hydro/aquarium prints sell on Etsy/locally, 2–3 listings = cheap experiment, earnings pay the card down). New Coach tip "$190 of filament is inventory, not income" connects them.
3. **🐕 Zoey's Kitchen REDONE (research-backed):** owner-sworn sustainable pattern = ONE slow-cooker dump-batch, frozen daily portions (sources: A Cup Full of Sass / The Produce Moms / Family Food on the Table crockpot recipes; Stellanspice + vet guides for lean 10–15% fat protein, sardine omega-3s, eggshell calcium ~500 mg/lb meat, and skipping spinach for seniors — oxalates). The 3 protein recipes are REPLACED by: **`r_zoey_onepot`** — "Zoey's One-Pot — Slow-Cooker Turkey & Sweet Potato" (3 lb 93/7 turkey, brown rice, sweet potato, peas/carrots, green beans-instead-of-spinach, off-heat sardines ×2 cans, eggshell ½ tsp/lb, optional eggs; HIGH 3–4 h; 660 kcal / 460–480 g/day kept; NEVER-list + senior vet note kept) and **`r_zoey_beefpot`** — "Beef & Pumpkin Pot (rotation variant)" (90/10 + pumpkin-not-pie-filling, flagged the pricey occasional batch, ~$66 vs ~$39 per the cost panel). Old ids `r_zoey_chicken/beef/pork` gone, zero stale refs.
4. **🔄 Transition tracker REMOVED from Today** (the whole order-330 block; `zoeyStart/zoeyHold/zoeyDayN/zoeyMix/toggleZoeyFeed` left in place but orphaned — `zoey`/`laundry['zoey-feed-*']` Firebase data untouched and harmless). New plan lives in the One-Pot recipe note: **slow ~1-month topper-style transition** (spoonful of homemade into each FD meal so she finishes it — she's sick of FD, especially the CHICKEN packs — ratio drifts as the shipment runs out) + 4 real chicken-pack tips (warm it slightly · 50/50 with beef/pork packs · smashed sardine or parmesan sprinkle · warm low-sodium broth drizzle).
5. **🛒 Grocery:** migration **`flags/jun6-zoeygroc-v1`** adds 7 food-only items to `grocCustom` (`gc-zoey1..7`): ground turkey 3 lb · brown rice · sweet potatoes · frozen peas & carrots · frozen green beans · no-salt sardines · eggs.
**Verify trail:** node --check ×2 + extracted inline (pre/post restamp); **63-check vm harness** — each sub-tab shows ONLY its sections + no sentinel leakage + 5 subtab buttons render, SAVOR $190 sim (badge drops, renders, note, tip, idea renders on Ideas page), Zoey (exactly 2 dog recipes, all ingredients incl. spinach-swap note, no "supplement", freezer + kcal lines, topper plan + all 4 tips + NEVER list, variant pumpkin + cost note, old ids gone, Zoey's Kitchen renders both, openRecipe works), tracker gone in both zoey states + walk intact, grocery migration + items render, To Cook dinner intact, all 7 renderers + 10 switchTab + 5 finSub, hg-v32 + v-bump. Push `b300502..871a181`, origin blobs byte-identical ×3, live 200 ×3 byte==pushed, House mirror ×3.

### ✅ LIVE — To Cook swap + new sausage & squash spaghetti recipe (Jun 6 2026, commit `b300502`, pushed `4759ea5..b300502`)
**On GO LIVE.** Fresh shallow clone of `4759ea5`, ONE commit: `index.html` + `recipes.js` + `sw.js` (**hg-v31**) + `images/recipe_sausage_squash_spaghetti.jpg`. Stamp "Jun 6 · 1:03 PM" (external-time verified, 1s skew). recipes.js `?v=` bumped.
1. **🍳 To Cook swap via one-time migration `flags/jun6-tocook-v2`:** deletes both Firebase seed entries (`ck-seed-lunch` Breakfast Quesadilla + `ck-seed-dinner` Roasted Garlic-Parmesan Zucchini) and queues **`ck-seed-dinner2` = the new recipe, TONIGHT'S DINNER (2026-06-06)**. Both old recipes stay in the Recipe Box — only the queue changed.
2. **📖 New recipe `r_box_sausage_squash_spaghetti` — "Hot Italian Sausage & Squash Spaghetti with Pesto-Kissed Red Sauce"** (serves 4, 10 min prep · 20 min cook, Homemade): 12 oz spaghetti · 1 lb frozen hot Italian sausage · zucchini + yellow squash half-moons · 24-oz jar red sauce · 3–4 Tbsp pesto stirred in OFF-heat + a dollop to serve · pasta-water gloss; the pesto-swirl note included verbatim. **RECIPES now 50.** Photo via the Commons pipeline: *"Grilled hot Italian sausage with spaghetti and a baby heirloom tomato sauce"* by **John Herschell, CC BY-SA 2.0** (license verified from extmetadata, visually checked — sausage in red tomato sauce, on-theme), optimized 450×448 q72, `photoCredit` renders on the detail page.
**Verify trail:** node --check ×2 + extracted inline (pre/post restamp); **34-check vm harness** — recipe shape (9 ingredients incl. all 6 required items, 5 steps, serves 4, photo file + credit, unique id), migration markers, simulated migration → To Cook queue renders the new dinner and the two old items are gone from the queue (scoped past the add-form `<select>`, which legitimately lists all recipes), Recipe Box card + `openRecipe` works, prior batches intact (cost panel, Zoey parked), all 7 renderers + 4 food subs, hg-v31 + v-bump. Push `4759ea5..b300502`, origin blobs byte-identical ×4, live 200 ×4 byte==pushed, House mirror byte-identical ×4.

### ✅ LIVE — Zoey parked + dog-food cost analysis + grow-lamp stand project + Blue Dream updates (Jun 6 2026, commit `4759ea5`, pushed `17422ba..4759ea5`)
**On "update schedule and go live".** Fresh (shallow) clone of `17422ba`, ONE commit: `index.html` + `data.js` + `sw.js` (**hg-v30**) + `images/hydro_top_20260606.jpg`. Stamp "Jun 6 · 12:54 PM" (external-time verified, 0s skew). data.js `?v=` bumped. ⚠️ Sandbox /tmp was nearly full of undeletable prior-session clones — used `git clone --depth 1` (all verify steps unaffected); future chats may need the same.
1. **🐕 Zoey transition PARKED (do not start yet):** the Today-tab 12-day tracker's unstarted card now reads "**⏸ Not started — use up the Farmer's Dog shipment + buy ingredients first**" with the plan in the sub-line (1+ month shipment incoming → feed through it ~4 weeks → buy ingredients → THEN tap the circle for Day 1). `zoeyStart()` untouched — manual start still works. New money to-do seed **`ft_zoeyfood`** ("Buy Zoey's homemade-food ingredients (when the Farmer's Dog shipment runs low, ~4 weeks)"). **Farmer's Dog cancel timing honestly delayed** in three places: cancel-list note ("~6 weeks out"), Coach "big domino" tip (rewritten — shipment is paid for, using it up IS the money move), and path-to-positive ("~6 weeks out: use up the shipment, then the 12-day switch").
2. **💰 New Finance panel "🐕 Farmer's Dog vs Homemade — cost analysis"** (between Cancel list and Money to-dos). Computed from his 3 dog recipes (660 kcal/day · 460–480 g/day · 10-day batches · 28-lb dog), incl. ~25% meat cooking loss, ~5% liver, rice/sweet potato, frozen veg, 2 cans no-salt sardines + 3 eggs per batch, free eggshell calcium. **FD $215.05/mo ($7.07/day) vs homemade ~$141/mo (~$4.63/day)** equal rotation → **saves ~$74/mo (~$890/yr)**; leaning chicken+pork → **~$104/mo (~$1,250/yr)**. Per-batch: chicken ≈ $39 ($3.89/d) · pork ≈ $34 ($3.39/d) · **beef ≈ $66 ($6.61/d)** — beef nearly matches FD at current prices (USDA/FRED avg ~$6.90/lb Apr 2026), so the panel says to make beef the occasional batch. Prices clearly labeled "Denver King Soopers-ish estimates, Jun 2026" (chicken thigh b/s $3.49/lb · 90/10 beef $6.49/lb · pork loin $2.79/lb) with a "true-up after the first real ingredient run" footnote. Static HTML — no new state.
3. **🧊 Print To-Do — new project card "🌱 Grow-lamp anchored stand"** (below Active Print): **HARD + WEEKEND chips**, anchored stand (metal or PLA, **NOT a ceiling mount** — he finds that ugly) to position the small grow lamp over the aquarium-top hydroponics; needs reach over the tank / stability against bumps / cable routing; candidate approaches = weighted base + vertical arm, or clamp/anchor onto the tank stand. Info card, no check-off state.
4. **🦐 Blue Dream tab:** his real photo (`images/hydro_top_20260606.jpg`, optimized 1000×563 q72 from the upload) added as a hero above the Hydroponics panel — potatoes in net baskets, one sprout cluster + one leafing. Hydroponics panel rows: **potatoes top growth started (Jun 6)** · **white sweet potato roots 4–8 inches**. Tank Hardware gains a **Cooling** row (clip-on fan, evaporative, watch evaporation/top off distilled). New **"🪟 Lid Design Plan — idea, not built"** panel: transparent lid keeps the nerites in, integrated small light, airflow gaps; hydro keeps growing on top under the grow lamp; aquarium lamp serves only the water column; cross-links the Print-tab stand project.
**Verify trail:** node --check ×3 + extracted inline (pre- and post-restamp); **59-check vm harness** — zoey parked card + manual-start path renders Day 1, ft_zoeyfood seed + renders, cancel note/tip/path ~6-weeks markers, cost panel (FD $7.07 · ~$141/~$4.63 · ~$74/~$890 · ~$104/~$1,250 · 3 per-batch figures · both disclaimers), print card (chips, no-ceiling-mount, both approaches, Active Print intact), Blue Dream (hero + file exists, top-growth, 4–8 in, fan, lid panel ×3 markers, biofilm line intact), prior terrarium batch intact, all 7 renderers + 10 switchTab + 4 tank subs + 4 print subs, hg-v30 + v-bump. Push `17422ba..4759ea5`, origin blobs byte-identical ×4 (API raw), live 200 ×4 byte==pushed (~40 s), House mirror byte-identical ×4.

### ✅ LIVE — Heartleaf photo fixed + Care page buckets + terrarium real-world update (Jun 6 2026, commit `17422ba`, pushed `9433666..17422ba`)
**On GO LIVE.** Fresh clone of `9433666`, ONE commit: `index.html` + `data.js` + `sw.js` (**hg-v29**) + `images/philodendron_20260606.jpg` (+ archive move). Stamp "Jun 6 · 12:40 PM" (external-time verified, 0s skew). data.js `?v=` bumped.
1. **🪴 Heartleaf Philodendron photo replaced** — the 6/2 photo was the wrong plant. New photo via the curl+Commons-API pipeline, license verified from `extmetadata`: **"Philodendron scandens subsp oxycardium2.jpg" by KENPEI, CC BY-SA 3.0** — classic glossy heart-shaped leaf, indoor shot; 6 candidates contact-sheeted, this one visually confirmed. Cropped (right 18% off — escalator background) + optimized 487×800 q72 (62 KB) → `images/philodendron_20260606.jpg`, wired in `data.js` `PLANT_INFO.philodendron.photo` with an attribution comment. Old photo archived to `archive/philodendron_20260606/philodendron_20260602_wrong.jpg` + README (in the repo AND mirrored under `home-and-garden-project/archive/`). ⚠️ The sandbox can't delete from the House mount — stale `House/images/philodendron_20260602.jpg` is safe to delete by hand.
2. **❤️ Care page bucketed:** three sections with subtle uppercase labels — **🔶 Needs care now** (due/never-tended) on top, **🌿 Coming up** in the middle (incl. plants with no maintenance task), **✓ Recently tended** (done today or within 2 days) at the bottom. Same urgency ratio (`turg`) sorts inside each bucket; empty buckets render no label; "Mark all as tended" button + wx banner untouched.
3. **🏜️ Dry terrarium truth-up (user's real changes today):** banner now "Stocked & settled (updated Jun 6) / 🪲 Crew fed & furnished — **1 stubborn ladybug left**" — ladybugs released to the garden except one (relocate when caught), **fake plant in as the katydid molting perch**, **more fruit flies added**, base got **almond leaves + water**, food = **fresh basil stems in sugar-water-soaked cotton balls + soaked raisins**. Daily-care food line is now "🍃 Refresh the food: basil + sugar-water cotton balls + soaked raisins". Setup panel renamed "🛠️ Setup": `dry-perches` + `dry-litterbark` labeled DONE and **checked via one-time migration `flags/jun6-terrarium-v1`** (sets `checked['dry-perches']`/`['dry-litterbark']`, Firebase-persisted, user can still uncheck). Hero caption matched. Build/Shopping/Checklist panels untouched.
**Verify trail:** node --check ×3 + extracted inline (pre- and post-restamp); **62-check vm harness** — photo wired + file exists + old ref gone, 3 care labels in order + due plant top / tended-today plant bottom / all plants render / no due-label when all tended, terrarium banner ×6 markers + new food line + old line gone + DONE labels + migration present + checked strike-through + daily 0→1 of 4, all 7 renderers + 10 switchTab + 4 tank subs + 3 garden subs + 4 food subs, stamp + hg-v29 + v-bump. Restamp from GitHub `Date:` header (0s sandbox skew). Push `9433666..17422ba`, origin blobs byte-identical ×4 (API raw), live 200 ×4 byte==pushed (CDN ~90 s), House mirror byte-identical ×4.

### ✅ LIVE — Food sub-tabs restored + wishlist split + SAVOR card (Jun 6 2026, commit `9433666`, pushed `e8cf313..9433666`)
**On GO LIVE.** Fresh clone of `e8cf313`, ONE commit: `index.html` + `data.js` + `sw.js` (**hg-v28**), stamp "Jun 6 · 11:45 AM" (server-verified), data.js `?v=` bumped.
1. **Nav restructure:** 📖 Recipe Box + 🍳 To Cook moved back INSIDE Food as sub-tabs — Food = **🛒 List · 🧺 Inventory · 📖 Recipes · 🍳 To Cook**. Top nav back to **6 single-row cells** (wrap CSS reverted), Today stays the pill. `renderRecipeBox`/`renderToCook` now target `view-grocery` gated on `foodSub`; `renderGrocery` dispatches; legacy `foodSub==="recipes"` self-heals to `recipebox`; top-level `tab-recipebox`/`tab-tocook` + views removed. Featured spotlight + recipe-only search unchanged inside the Recipes sub-tab; To Cook planner mechanics unchanged (cookQueue intact).
2. **Print wishlist split:** sub-tabs now **📋 To-Do · 💎 Wishlist · 💡 Ideas · 🛠️ Upgrades**. **💎 Wishlist = big-ticket only** — new Firebase node **`wishCustom`** (`{t,done}`; `addWish/toggleWish/removeWish`, escaped labels, add input + check + ×; empty-state hint). **💡 Ideas = the entire old wishlist unchanged** (28 ideas, five vibes, 🎲 Print of the Day, fpPct) — funPrints check state preserved, page eyebrow now says "💡 Ideas — Print Something Fun".
3. **Finance — Cap One SAVOR:** new debts entry `savor` (card:true, **keepZero:true**, $500 limit noted, ~30% APR, min $0) listed after Merrick; renders **"🟢 Open — keep it at $0"** instead of PAID/target while balance ≤ 0 (`paidOff` guarded by `!keepZero`; never snowball target at $0; tap-to-update works — a real balance drops the keep-zero badge and it behaves like any card). One-time migration **`flags/jun6-savor-v1`** dates `fin.debt.savor = $0 @ 2026-06-06`. New Coach tip "The SAVOR card is a tool, not a balance" (18 tips now).
**Verify trail:** node --check ×3 + extracted inline; **37-check vm harness** — 6 cells + single-row CSS + pill, Food 4 sub-tabs dispatch (Recipes spotlight+search scoped, To Cook renders + toggles in place, legacy id heals, List intact), wishlist (empty state, escaped row, check/remove, Ideas page = old content w/ Print of the Day, To-Do intact), SAVOR (keep-zero badge + dated, not PAID-struck, •6605 still sole target, balance>0 exits keep-zero), migration present, all 10 renderers. Push `e8cf313..9433666`, origin blobs ×4 byte-identical, live 200 ×3 byte==pushed (CDN needed ~1 min) + hg-v28 + SAVOR served, House mirror ×3.

### ✅ LIVE — Big batch: mountain bgs, finance truth-up, print, 📖 Recipe Box tab, 🍳 To Cook tab (Jun 6 2026, commit `e8cf313`, pushed `2cbdbd5..e8cf313`)
**On GO LIVE.** Fresh clone of `2cbdbd5`, ONE commit: `index.html` + `data.js` + `recipes.js` + `sw.js` (**hg-v27**) + 5 new `images/bg_*_20260606.jpg`. Stamp "Jun 6 · 11:30 AM" (server-time verified). Both `?v=` params bumped.
1. **🏔️ Mountain backgrounds, 5 time-of-day slots** (`todaySeg`: night<5 / dawn<8 / morning<11 / noon<16 / dusk<20 / night) — all LOCAL, downloaded via the curl+Commons-API pipeline, license verified per file from `extmetadata`, optimized 1280px q72, visually checked via contact sheet (one morning re-pick — first choice too dark): dawn **Maroon Bells at sunrise** (Lorie Shaull, CC BY-SA 2.0) · morning **Bear Lake reflection, RMNP** (G. Lamar, CC BY 2.0; small corner watermark, invisible under the dark overlay) · noon **Longs Peak w/ lenticular clouds** (Anne Dirkse, CC BY-SA 4.0) · dusk **Palmer Park sunset, Colorado Springs** (Mark Byzewski, CC BY 4.0) · night **Milky Way over RMNP** (NPS/Jeremy M. White, PD — his Milky Way kept, also now HEADER_NIGHT locally). Attribution comments above `TODAY_BG`. `setTabBg` fallback → `TODAY_BG.dusk`.
2. **💰 Finance truth-up (statement screenshot):** family loan start **$10,000** (+$700 Zelle Jun 6 for rent, noted); rent bill notes "June: paid Jun 5, 1 day late — late fee TBD ⚠️"; Xcel note "June: $133.16 ✓"; checking account renders an amber pending line ("$215.05 Farmer's Dog pending → effectively ~$108"); **Farmer's Dog $215.05 added to the cancel list** ("pending Zoey's transition — cancel after Day 12 🐕") — cancel-list label now dynamic (~$372 target) and path-to-positive leads with the dog line (gap −$324 → −$109 when it's verified); new Coach tip "The Farmer's Dog is the big domino"; new seed to-do `ft_latefee` (call property mgmt after the weekend). **One-time migration `flags/jun6-batch-v1`** seeds: checking $323.04 @ 2026-06-05, June paid-checks for rent/student-loan/Xcel, famloan history $10,000 @ Jun 6, + the two To Cook seeds.
3. **🧊 Print:** "✨ Just for fun" checklist REMOVED (ids dead, state harmless in funPrints); Active Print = "Desktop Hydroponics — pots / Pots are on the bed RIGHT NOW"; two new check-offs: `tpu_arrived` ("🧵 TPU filament arrives Jun 7") + `print_grommets` ("⭕ Print TPU grommets — desktop hydroponics", queued after pots).
4. **📖 Recipe Box is its own tab** (id `recipebox`, after Food): ⭐ Featured-recipe spotlight on top (recipeListHtml no longer repeats the featured card), then lid + search + cards. **Search lives ONLY here and matches recipes only** (existing scoped engine; Food tab has no search). **Food keeps List + Inventory** (recipes sub-tab removed; stale `foodSub==="recipes"` self-heals to "list").
5. **🍳 To Cook tab** (id `tocook`): planned-recipe queue in new Firebase node **`cookQueue`** (`{rid,day,meal,done}`, `saveCook()`); cards show meal+day chips, link to `openRecipe`, check off when cooked (sink + strike), × to remove; add flow = recipe select (box, sorted, no dog food) + day (next 7) + meal. **Seeded:** TODAY LUNCH = **new "Breakfast Quesadilla with Spinach & Mushrooms"** (added to recipes.js — the box had no quick inventory-match lunch; eggs/tortilla/spinach/mushrooms/green onion/yogurt, 15 min) · TODAY DINNER = existing **"Roasted Garlic-Parmesan Zucchini, Squash and Tomatoes"** (the "Italian zucchini & yellow squash" — zucchini + yellow squash + Italian seasoning + parmesan, both squashes in inventory). RECIPES now 49.
6. **Nav:** 8 uniform cells wrapping 4+4 (Plants/Tank/Food/Recipes // To Cook/Print/Health/Finance), Today stays the pill.
**Verify trail:** node --check ×5 + concat; **54-check vm harness** — all data counts (49 recipes, cancels Σ$371.98, 9 to-dos, 17 tips), 5 bg slots + files exist + 5-way seg math, finance render ($323.04 + pending line, 3 of 16 June bills paid, famloan $10,000, dog line in path, gap −$324 → −$109 on dog-verify), Recipe Box spotlight-first + single featured + scoped search (#recipe-list only), grocery self-heal + 2 sub-tabs, To Cook seeds render w/ links + check-off + add form, recipe overlay opens from To Cook, print (fun gone, pots-NOW, TPU rows, Klipper kept), all 12 renderers, 8 cells + wrap CSS, hg-v27. Push `2cbdbd5..e8cf313`, origin blobs byte-identical ×6 checked, live 200 ×9 (4 files byte==pushed + 5 images served), House mirror byte-identical (4 files + 5 images).
**Note for Cyrus:** two scratch contact-sheet files `_mtn_check.jpg` / `_mtn_morning2.jpg` may appear in the session outputs folder — safe to delete.

### ✅ LIVE — Deploy stamp fixed: real Denver time + short format (Jun 6 2026, commit `2cbdbd5`, pushed `63ee86d..2cbdbd5`)
**On GO LIVE.** Fresh clone of `63ee86d`, ONE commit: `index.html` + `sw.js` (**hg-v26**) + `deploy-github.py`.
- **Root cause of the 6-hours-off stamps:** the build sandbox's clock was skewed early this morning (it held Denver wall time labeled as UTC), so `TZ=America/Denver` conversion subtracted 6 hours again. The clock has since synced (verified 0s skew vs GitHub's HTTP `Date` header).
- **⚠️ STANDING RESTAMP RULE (in the DEPLOY_STAMP comment too): never trust a bare clock.** At every deploy, fetch real UTC from an external source (e.g. `curl -sI https://mrosyrus21.github.io/home/` → `Date:` header), convert to **America/Denver** (zoneinfo — MST/MDT automatic), cross-check the local clock, and stamp from the verified time.
- **Format change:** eyebrow shows JUST the short stamp — **"Jun 6 · 11:07 AM"**. No "Last deployed" label, no year, no "MT" suffix (`_eb.textContent=LAST_DEPLOY` now). `deploy-github.py`'s stamp f-string shortened to match (it already used ZoneInfo America/Denver).
- **Verified at push:** served stamp "Jun 6 · 11:07 AM" vs actual Denver time at verification "Jun 6 · 11:08 AM" ✓; zero "Last deployed" matches in the served blob; node --check + full 11-renderer vm pass; origin blobs byte-identical ×4; live 200 ×3 byte==pushed; House mirror byte-identical ×3 (incl. deploy-github.py).

### ✅ LIVE — Dry terrarium: stocked status + daily care checklist (Jun 6 2026, commit `63ee86d`, pushed `d3249f0..63ee86d`)
**On approval.** Fresh clone of `d3249f0`, ONE commit: `index.html` + `sw.js` (**hg-v25**), restamp "Jun 6, 2026 · 5:01 AM MT". Tank → 🏜️ Dry only.
- **Status banner → "Stocked & cycling in / 🪲 Bioactive cleanup crew active":** isopods + springtails working the coco, fruit flies as the snack layer, "springtails hide in the substrate by day — that is normal." Hero caption matched.
- **🧼 Daily care (resets every morning):** new date-keyed `laundry["tankd-<id>-<date>"]` + global `toggleTankDaily()` (sits next to `toggleReset`), local `tdaily()` row helper (round checkbox = daily, square `tchk` = one-time). Items: 🌫️ light mist (damp not soggy) · 🍃 fresh untreated leaves for the katydid (basil works) · 🪵 perch check (katydids molt hanging upside-down — vertical twigs are life-or-death) · 👀 critter headcount. Panel title shows "n of 4 today".
- **🛠️ Setup still needed:** persistent `tchk` ids `dry-perches` (vertical twigs) + `dry-litterbark` (leaf litter + bark for isopods), plus the placement note: ladybugs → garden aphid plant at dusk; katydid → its own enclosure away from predators.
- Existing Build / Shopping / Setup-Checklist panels untouched below.
**Verify trail:** node --check (sw.js + extracted inline); 25-check vm harness — banner + all 4 daily items + molt warning render, check-off bumps count + strikes through, mocked Jun 7 resets to 0 of 4, setup list + note render, old panels kept, all 4 tank sub-tabs + all 9 other renderers pass, hg-v25, new stamp. Push `d3249f0..63ee86d`, origin blobs byte-identical ×3, live 200 ×2 byte==pushed + markers served, House mirror byte-identical ×2.

### ✅ LIVE — Nav correction: Today is a PILL (Jun 6 2026, commit `d3249f0`, pushed `2c8b497..d3249f0`)
**On GO LIVE (user correction to `2c8b497`).** Fresh clone, ONE commit: `index.html` + `sw.js` (**hg-v24**), restamp "Jun 6, 2026 · 4:52 AM MT".
- **☀️ Today is now a util-pill**, FIRST in the pill row (before Forecast/Rooms/Timeline), `id="pill-today"`, ships with `pill-active` so it highlights on open; `switchTab` pill array now `["today","rooms","timeline","forecast"]`, tab array dropped `today`. Removed `tab-today` from the main row.
- **Main tab row back to 6 single-row uniform cells** (Plants/Tank/Food/Print/Health/Finance): reverted the `2c8b497` wrap CSS (`.tabs` no flex-wrap, `.tab` back to `flex:1`).
- Today remains the **default view on open** (`currentTab="today"` unchanged; view-today visible by default).
**Verify trail:** node --check (sw.js + extracted inline); 26-check vm harness — default view today, switch to finance (pill deactivates, tab activates, view swaps), switch back via pill (pill-active restored, no tab cell active), all 10 switchTab destinations render, tab-today absent, exactly 6 tab cells, single-row CSS, Finance after Health, hg-v24, new stamp. Push `2c8b497..d3249f0`, origin blobs byte-identical ×3, live 200 ×2 byte==pushed, `pill-today` served + zero `tab-today`, hg-v24 served, House mirror byte-identical ×2.

### ✅ LIVE — Finance tab + Call DT glow + magnesium out + Sleep Reset Day (Jun 6 2026, commit `2c8b497`, pushed `faf9bcd..2c8b497`)
**On GO LIVE.** Fresh clone of `faf9bcd`, ONE commit: `index.html` + `data.js` + `sw.js` (**hg-v23**), restamp "Jun 6, 2026 · 4:44 AM MT". data.js `?v=` bumped.
1. **💰 Finance tab** (new, after Health; tab id `finance`, gold active color). Data home: **`const FINANCE` at the end of data.js** (income/outflow, payday anchor 2026-06-04, 16 bills w/ due chips, 10 debts in snowball order, 9 cancels, 8 seed to-dos, 16 advice tips). New Firebase global **`fin`** (`saveFin()`; loaded in ST.on). Sections, top to bottom: **The One Number** — monthly gap (−$324 now), turns greener LIVE as cancel-list items get verified (gap = income − outflow + verified cancels), "path to positive" breakdown + biweekly-Thursday payday countdown; **Accounts** — tap a balance → inline input → saves to `fin.bal[id][YYYY-MM-DD]` (dated history, trend line vs prior point; first save trends vs the Apr 30 statement seed); **Goal #1 $500 buffer** — bar driven by the SAVINGS balance (update savings → bar moves; FUNDED state at $500); **Bills June** — per-month check-off keys `fin.bills["<id>-<YYYY-MM>"]` (auto-fresh each month); **Debt snowball** — per-debt tap-to-update balances (`fin.debt`), progress bars vs start, 🎯 target auto-advances to next card on payoff (•6605 $489.59 first), unknown-balance debts (Nissan/Lendmark/student/IRS) say "tap to set" and baseline from the first entry; **Cancel list** — check = "verified gone on a statement" (`fin.cancel`), feeds the gap card, $157 target + Amazon Fresh/Prime-trial pending notes + keep list; **Money to-dos** — grocery-style list: 8 seeds + custom adds (`fin.todoCustom`, escaped, × to remove), done state `fin.todoDone`; **Coach's Corner** — 1 big + 2 small tips rotating daily + "🎲 Show me three more" (`finTipOff` transient), footer states budgeting-only/no investment advice. All finance helpers prefixed `fin*`, inserted right before `renderAll()`.
2. **Nav made uniform:** `.tabs` now `flex-wrap:wrap`, `.tab{flex:1 1 22%}` → 7 uniform cells (Today / Plants / Tank / Food / Print / Health / Finance), Today is a plain cell, wraps 4+3 on mobile. `switchTab` view+tab arrays and `renderAll` include `finance`; no subtabs; tab bg falls back to evening image (no TAB_BG entry — intentional, safe).
3. **Call DT glow:** new `@keyframes dtCallPulse` (red, 2s, mirrors the amber `dtTestPulse`); applied to BOTH unchecked Call DT card states (normal + "Test or no?" question) replacing their static box-shadows. Pulses until dealt with; done-strip unchanged.
4. **Magnesium removed entirely:** the `medMini('mag',…)` Health row deleted; data.js `TASKS.rx_setup` label/note now esomeprazole-only. Zero "magnesium" matches in served blobs (verified).
5. **🔄 Sleep Reset Day — 2026-06-06 ONLY, auto-expires:** gated `today===RESET_DAY` ("2026-06-06" const in `renderToday`). Flow items (new `toggleReset()`, date-keyed `laundry["reset-*-<date>"]`): banner card (passive) at order 278, ⏰ Up at 10:30 AM (282), ☀️ bright light within 30 min (284), ☕ no caffeine after 2 PM (750), 🚫 no naps (760); breakfast time label reads "Right after waking" today only. Banner closes with "Tomorrow morning, 8:00 AM — that's the one that locks the cycle in." Evening items untouched (same `evening-wind-*` keys → streaks keep counting). Harness-verified zero leftovers on Jun 7.
6. **RHYTHM evening shift (permanent, in data.js):** windDown 9:30→**10:00 PM**, reading→**10:00–11:00 PM**, lightsOut 10:30→**11:00 PM** (per the reset-day spec "screens off 10, read 10–11, lights out 11"); breakfast stays 8:00 AM. The RY fallback in index.html matches.
**Verify trail:** node --check ×5 (data.js / recipes.js / sw.js / extracted inline / concat); **56-check vm harness** — all 10 tab renderers + renderAll, FINANCE counts (16/10/9/16/8) + cancel sum 156.93, gap card −$324 → −$304 after verifying one $20 cancel, balance edit open→save→history+trend (▲ $599.42 vs seed), bill check-off 0→1 of 16, snowball target advances past a $0 •6605 to •26529 + PAID strike, custom to-do escaped (`&lt;b&gt;`) + removable, magnesium absent / esomeprazole+Litfulo present, reset-day items render+check off today and are GONE on a mocked Jun 7, breakfast label override, dtCallPulse in Today HTML, evening shows 10/11 PM, nav regex (Finance directly after Health), SW hg-v23. Push `faf9bcd..2c8b497`, origin blobs byte-identical ×3, live 200 ×3 byte==pushed, markers + hg-v23 + `const FINANCE` served, zero magnesium served, House mirror byte-identical ×3.
**Note:** transient finance UI state (`finEdit`, `finTipOff`) is JS-only, never persisted. Privacy rule kept — last-4 labels only, no account/routing numbers anywhere in the app.

### ✅ LIVE — Call DT test gate (Jun 6 2026, commit `faf9bcd`, pushed `a1bd2dc..faf9bcd`)
**On GO LIVE.** Fresh clone of `a1bd2dc`, ONE commit: `index.html` + `sw.js` (**hg-v22**), restamp "Jun 6, 2026 · 4:17 AM MT".
- **Call DT can no longer be checked directly.** Tapping the unchecked card flips it in place to **"Test or no?"** with two big buttons — 🧪 Test / No test — plus a small "never mind — haven't called yet" cancel line (cancel marks nothing). One extra tap total; question state (`dtAsk`) is transient JS, never persisted.
- **🧪 Test →** Call DT completes (sinks normally) AND a **pulsing amber "Go take the test" card** (new `dtTestPulse` keyframes) pins at flow order **301, directly under Call DT** — "🚨 Test Day — Time-Sensitive" — until tapped off; then it sinks to the done pile. Per-date keys in `laundry`: `dtmust-<date>` (test required) + `dttest-<date>` (test done) — auto-reset next day, Firebase-synced.
- **No test →** Call DT just completes and sinks like any item.
- **Streak untouched:** both paths set the same `calldt-<date>` key `dtStreak()` counts. **Undo path:** unchecking Call DT from the done pile also clears that day's `dtmust-`/`dttest-` so a mis-tap can be redone cleanly (`toggleDT` now only unchecks; checking always routes through the gate).
- Toast on Test: "🧪 Test day — added to the top of your flow".
**Verify trail:** node --check (sw.js + extracted inline); 22-check vm harness — gate intercepts tap, question + both buttons + cancel render, cancel marks nothing, NO path completes w/o test item, TEST path completes + pulsing card + flag keys, streak counts on both paths, uncheck clears flags, test check-off sinks as done strip + pulse stops, order 301 under 300, full per-tab render pass. Push `a1bd2dc..faf9bcd`, origin blobs byte-identical ×4, live 200 ×2 byte==pushed, 7 feature markers + hg-v22 served, House mirror byte-identical ×2.

### ✅ LIVE — Six-fix batch: scaler, photos, print to-dos, fun-print rebuild, food-only grocery (Jun 6 2026, commit `a1bd2dc`, pushed `035cffd..a1bd2dc`)
**On GO LIVE.** Fresh clone of `035cffd`, ONE commit. Changed: `index.html`, `data.js`, `recipes.js`, `sw.js` (**hg-v21**), +45 new `images/recipe_*.jpg`. Stamp "Jun 6, 2026 · 3:49 AM MT".
1. **Serving scaler rewritten** (`scaleIngredient` + new `_qval`/`_fmtMixed` in index.html, replacing `_parseNum`/`_fmtQty`): kitchen-real output — never stacked/compound fractions; proper unit downshifts (¼ cup × ½ → "2 tablespoons", tbsp<1 → tsp, lb<1 → oz); parenthetical weights round with "about" ("½ Can (about 8 ounces)"); single-glyph mixed numbers ("1½"); temps/pan-sizes/times/percents never scale; **at the base serving count the original recipe text always shows untouched**. Also stripped the "Directions —" junk prefix from chili (+ any like it) in recipes.js.
2. **Recipe photos: 45 of 48 recipes now have REAL photos** (all 44 imports + tenderloin; only Zoey's 3 dog recipes use the emoji hero). Sourced via curl against the Wikimedia Commons API (web_fetch times out in the sandbox; **plain curl works** — remember this), license verified per-file from `extmetadata.LicenseShortName` (PD/CC0/CC BY/CC BY-SA only), downloaded at 800px, optimized to JPEG q72 (~3.4 MB total), committed to `images/recipe_*.jpg`, wired as `photo:` + `photoCredit:` (credit renders small on the detail page). Every pick visually checked via contact sheet; 12 bad matches re-queried (the salmon-was-a-cityscape incident). Photo-less cards (dogs) get a **gradient emoji-hero** (`recipeHero()`, palette hashed by id). The old hotlinked tenderloin URL is gone — localized as `images/recipe_beef_tenderloin.jpg` (Filet mignon, Paris.jpg, CC BY-SA 4.0).
3. **Print To-Do:** the 3 stale buys (Sovol blue filament / TPU / submersible pump) REMOVED per Cyrus. Added **"🫐 Buy a Blueberry Klipper kit"** (`funPrints['buy_klipper_kit']`). Added a **"✨ Just for fun"** checklist: bug day-pass to the bioactive resort, eat-the-first-strawberry-in-the-garden rite, name the boldest Blue Dream shrimp, hide a tiny print in a houseplant (`fun_bug_daypass`/`fun_strawberry_rite`/`fun_shrimp_name`/`fun_plant_gnome`).
4. **Fun Print Ideas (Print → Wishlist) rebuilt:** old page rendered ONE card/day from a hidden 47-item list. Now: `PRINT_VIBES` + `PRINT_IDEAS` (28 curated ideas) in five vibe sections — 🐟 Aquarium & Water (**PETG-only banner — aquarium-safe = PETG, PLA breaks down underwater**), 🌱 Garden Gadgets, 🐛 Bug Resort Gear, 🍳 Kitchen Hacks, 🧸 Toys & Desk Joy — each idea with difficulty/material/time chips and a check-off; **🎲 Print of the Day** spotlight rotates daily; per-vibe progress counts; old ids (octopus, capybara_mini, …) reused so check state survives. `fpPct` denominator now dynamic.
5. **Grocery is FOOD-ONLY (standing rule going forward):** `groc-alarmclock` removed from `GROC_BUILTIN`/`clearGroc` (the ⏰ #1 pinned Today card stays until checked — untouched). **8-inch vacuum bags moved to the household to-do list**: new `TASKS.vacuum_bags` (room "priority", with errands; in `NO_PROGRESS` + TIME_META) — visible under Rooms → ⭐ Priority. A one-time idempotent **Firebase migration** in the ST.on handler purges any "vacuum bag"/"alarm clock" strays from `grocCustom` + `inventory` on next load. `addGroc` now **nudges non-food adds** (NONFOOD_RX: vacuum/alarm/battery/bulb/filament/tool words) to the Priority to-do list with a toast; grocery header says "Food only…".
**Verify trail:** node --check ×4; scaler unit tests (16 cases incl. base-ratio passthrough, can/oz "about", cup→tbsp→tsp, 9x5-pan + 350° immunity); 41-check vm harness (photos wired ×45 + files resolve ×45, credits, emoji-hero, 3 buys gone, Klipper + 4 fun present, 5 vibes + spotlight + chips, PETG note, grocery nudge → priority + food add still works, alarm pin intact, full per-tab render incl. all sub-tabs, search intact); push `035cffd..a1bd2dc`, origin blobs byte-identical ×4, live 200 ×4 + sample image 200, hg-v21 + markers served, House mirror byte-identical ×4 + 45/45 images.
**Note for Cyrus:** two scratch files `_recipe_photo_check.jpg` / `_recipe_photo_check2.jpg` in the House root are leftover contact sheets (the sandbox can't delete files) — safe to delete by hand.

### ✅ LIVE — Recipe Box data cleanup pass (Jun 6 2026, commit `035cffd`, pushed `1a0c4ec..035cffd`)
**On the same GO LIVE.** Two chats executed the import in parallel; the `1a0c4ec` deploy (entry below) landed first, this pass kept its architecture/ids/search untouched and shipped ONE commit on top fixing what its parse missed. Changed: `recipes.js` (array contents only), `index.html` (recipes.js `?v=` + restamp "Jun 6, 2026 · 2:25 AM MT"), `sw.js` (**hg-v20**). `data.js`/`deploy-github.py` untouched.
- **Scrape junk actually stripped:** `1a0c4ec`'s blob still served contributor names as steps ("BAKING NANA", "MY HOT SOUTHERN MESS", "LUTZFLCAT"), 3× "FRED HARDY II, FOOD STYLIST…" credit lines (Baby Back Ribs), the whole Allrecipes article tail on Chile Rellenos (FAQ + reader-praise quotes), "My Latest Videos" as a trout *ingredient*, "Note: click on times…" (Sweet & Sour), and one "tap or hover to scale" servings. All gone now (verified zero matches in the served blob).
- **Step quality:** bare section-header lines ("PREPARE THE FILLING", "Fry the pork") merged into their step as "Header — text" so no numbered step is just a shout; Pork & Broccoli's duplicated narrative half dropped (kept the actual 6-step recipe); doubled trout ingredient list (13 items listed twice) halved.
- **Curation:** per-recipe emoji (🌶️🥟🍌🥧… instead of one default), searchable tags added (chicken/beef/pork/instant pot/slow cooker/mexican/asian/side/baking/dessert/chili… — the search box already indexes tags), all-caps names tidied ("INSTANT POT AWARD WINNING CHILI RECIPE" → "Instant Pot Award Winning Chili"), servings normalized for the stepper ("6-8", "40 ct" parse fine).
- Same 44 entries, same `r_box_*` ids, same `RECIPES_BOX` schema — search, source chip, serving stepper, Zoey section all per `1a0c4ec`.
**Verify trail:** node --check ×3 (recipes.js / sw.js / extracted inline); vm harness 28/28 — RECIPES=48, ids unique, 3 dog recipes, zero supplement strings, search (1-match / sardine→3 dogs / instant pot→6+ / source-field / no-match), all 48 cards build, detail + 2× ingredient scaling + source chip, no bare all-caps steps, full per-tab render pass; push `1a0c4ec..035cffd`, origin blobs byte-identical ×4 (incl. unchanged data.js); live 200 ×3, served index/recipes/sw byte==pushed, hg-v20 served, stamp served, served recipes.js = 44 clean entries; House mirror byte-identical ×5 (index.html, recipes.js, sw.js, data.js, deploy-github.py).
**Also this pass (docs):** `Recipes/SCHEDULE_ZOEY_HANDOFF.md` got a ⚠️ SUPERSEDED banner (it still described the old Balance IT / fish-oil design — banner says whole-foods is final, do not rebuild from it). Confirmed zero "revoke the token" reminders anywhere in House/Recipes docs, and the three Jun-6 ⛔ DROPPED items (thighs card, Jun-4 tank log, yogurt-tenderloin) stay dropped.

### ✅ LIVE — Recipe Box import: full Paprika library + search (Jun 6 2026, commit `1a0c4ec`, pushed `3ace8da..1a0c4ec`)
**On GO LIVE.** Fresh clone of `3ace8da`, ONE commit. Changed: `index.html`, **new `recipes.js`**, `sw.js` (hg-v19, recipes.js precached), `deploy-github.py` (recipes.js staged). `data.js` untouched.
- **44 recipes imported** into Food → Recipes from `Recipes/Recipe Box/recipes_export.json` (the 43-recipe Paprika library + the 2 customs, minus 1 exact duplicate "Pork and Broccoli Stir Fry" — identical ingredients/steps, only the title differed). Box now holds **48**: tenderloin (still featured) + 44 imported + 3 Zoey dog recipes.
- **Architecture:** new same-origin **`recipes.js`** (`const RECIPES_BOX`, ~86 KB), loaded right after `data.js` (`?v=20260606120000`); it pushes into `RECIPES` at load (id-deduped), so every existing card/detail/serving-stepper path works unchanged. To add a recipe later: append to `RECIPES_BOX` in recipes.js, bump its `?v=` + SW cache.
- **Search/filter:** parchment-styled search box under the box lid (Food → Recipes). Live, word-AND match on name + ingredients + tags + source; only the `#recipe-list` div re-renders on input (typing never loses focus); match count, dog cards searchable, "no match" card, clearing restores featured + Zoey sections.
- **Import hygiene:** Paprika junk steps stripped (ADVERTISEMENT / Instructions Checklist / "ppl"), servings normalized for the stepper ("Servings: Servings: 12"→"12", "tap or hover to scale" dropped, "Yield: 0" dropped), prep/cook merged into one time chip, per-recipe emoji, `source` kept and shown as a 📖 chip on the detail page (small `openRecipe` meta addition).
- **Two stale lines fixed in `index.html`:** lid title "Grandma's Recipe Box" → **"Recipe Box"** (standing naming rule: never "Grandma's"); Zoey divider line "supplements go in fresh at serving time" → "eggshell calcium, liver, eggs, and weekly sardines keep it balanced" (matches the whole-foods recipes from `3ace8da`).
- **Recipe Box txt side confirmed already clean:** all 4 Zoey files in `Recipes/Recipe Box/Shareable/` are whole-foods (zero Balance IT / fish-oil strings) — no file edits needed there this pass.
**Verify trail:** node --check ×4 (data.js / recipes.js / sw.js / extracted inline) + concat check; vm harness — RECIPES=48, 44 box entries, 3 dog intact, tenderloin featured, search box + filter (sesame/buttermilk/ingredient/dog/no-match/clear), detail page + source chip + stepper 6→7 rescale on an imported recipe, zero supplement strings, full per-tab render pass; push: HEAD==origin/main `1a0c4ec`, all 5 blobs byte-identical; live 200 ×3, served recipes.js byte==local (44 ids), hg-v19 served, new stamp + v-param served; House mirror byte-identical ×5.

### ✅ LIVE — Zoey whole-foods + doc cleanup (Jun 6 2026, commit `3ace8da`, pushed `57fd4c0..3ace8da`)
**On GO LIVE.** Fresh clone of `57fd4c0`, ONE commit, only `data.js` changed in the app (+ SW `hg-v18`, v-param, restamp).
- **Zoey's 3 recipes are now whole-foods only (Cyrus's explicit choice — keep it this way):** Balance IT + fish-oil-capsule lines deleted from ingredients, steps, and notes. Replacements in each recipe: kitchen-made **eggshell calcium** (bake clean dry shells ~10 min at 300°F, grind fine, ~1/2 teaspoon per pound of food), **canned no-salt sardines in water** swapped in for part of the meat 1–2×/week, **plain cooked egg** a couple times a week; liver and the varied-veg mix unchanged. Each note now carries one matter-of-fact line: "Eggshell calcium, liver, the occasional egg, sardines, and varied vegetables keep this balanced — no store-bought supplements." Exclusion list + age-8 vet note kept. Verified zero "Balance IT"/"fish oil" matches in the served data.js blob.
- **Recipe import NOT done — Recipe Box unreachable.** `…\Projects\Recipes\Recipe Box\Shareable\` (~45 human recipe files) is outside this session's mounted folder (only House is mounted); confirmed via both mounts and file tools. The bulk import + search/filter box is still TO DO — Cyrus will relay the recipe data another way (or grant the folder). When it lands: architecture suggestion was data.js or a new same-origin recipes file (then add to sw.js precache + deploy staging).
- **Dropped by user decision (Jun 6) — never build, never re-raise:** the "Elevate leftover thighs" card (6/1), the Jun-4 tank-state log wiring, and the yogurt-tenderloin recipe idea. Entries above re-marked ⛔.
- Doc hygiene: the old-token reminder is gone from all project docs (resolved; not to be mentioned again).
**Verify trail:** node --check ×3 + extracted inline; vm check of all 3 dog recipes (markers incl. 300°F / 1/2-tsp-per-lb / sardines / 660 kcal / exclusions / bloodwork, zero supplement strings); origin blobs byte-identical ×3; live 200, "Eggshell calcium" served, no supplement strings served, hg-v18 served; House mirror byte-identical ×3.

### ✅ LIVE — Today flow v2 + Zoey integration (Jun 6 2026, commit `57fd4c0`, pushed `2efc271..57fd4c0`)
**User feedback pass on the rhythm redesign + the staged Zoey dog-food handoff, deployed on GO LIVE.** Fresh clone of `2efc271`, ONE commit, SW `hg-v17`, stamp "Jun 6, 2026 · 1:39 AM MT".
**What changed in `index.html`:**
- **Today = ONE chronological flow, no grouped sections.** The Meal Rhythm / Daily Essentials / Evening Wind-Down headers are gone. Order of the day: alarm-clock #1 pin (stays on top) → weather → compact one-off pins → progress chip ("Today, in order — tap things off as you go · n of N") → Call DT → Litfulo → breakfast → **Zoey tracker** → dog walk → garden-if-due → lunch → the ONE house task (order 500 weekend mornings / 800 weekday afternoons; Monday = passive rest card) → dinner (eaten-by-8 cue) → phone-away → reading → lights out.
- **Sink-to-bottom check-off:** every checked flow item moves to a "✨ Done — n today" pile at the VERY bottom (compact struck-through rows), unchecked always float above. The just-checked item gets `.flow-just-done` (new `sinkIn` + `glowPulse` keyframes, added before `</style>`) and a random gentle `toast()` fires; a rotating encouragement line sits under the pile; "Day complete" card when all checkables are done. Mechanism: `window.__flowPrev` snapshot diff per render.
- **Zoey 12-day transition tracker** (in the flow near meals): new Firebase global **`zoey`** (`zoey=d.zoey||{}` in ST.on; `saveZoey()`), helpers `zoeyStart()` (Day 1 = the day Cyrus checks it — NOT auto), `zoeyDayN()`, `zoeyMix()` (1–3: 25/75 · 4–6: 50/50 · 7–9: 75/25 · 10–12: 100/0), `toggleZoeyFeed()` (daily check, key `laundry["zoey-feed-<date>"]`), `zoeyHold()` (⏸ button — pushes startDate +1 day for soft stool; note on card: "watch stool/appetite/energy; soft stool = hold 2–3 extra days"). Day 13–14 shows "Transition complete — 100% homemade 🎉", then the card retires.
- **Recipe Box:** new "🐕 Zoey's Kitchen" divider section after the human cards + 🐕 DOG FOOD badge via `r.dog` flag in `recipeCard`.
**What changed in `data.js`:** 3 dog recipes appended to RECIPES — `r_zoey_chicken` (thigh + ~5% liver + white rice + carrots/green beans/spinach), `r_zoey_beef` (90/10 + beef liver + sweet potato + green beans/carrots/zucchini), `r_zoey_pork` (lean loin + liver + rice/sweet potato + green beans/carrots/kale). Each carries the full note: ~660 kcal/day, 460–480 g in 2 meals, 10-day batch (fridge 3–4 days, freeze rest), fresh-at-serving 2.5 mL fish oil + Balance IT Canine 2–3 green scoops (confirm via balance.it), full exclusion list (onion/garlic/grapes/raisins/xylitol/chocolate/macadamia/avocado/cooked bones/salt), vet note (age 8 — baseline weight + senior bloodwork).
**⛔ Beef tenderloin photo — SKIPPED (license unverifiable).** Wikimedia Commons was unreachable from the sandbox (web_fetch empty/timeouts), so the license of any candidate (incl. the currently hotlinked `Filet_mignon_at_Rioz_churrascaria.jpg`) could not be verified, and no unverified image gets used per instruction. The existing hotlink was left untouched. Retry the localization (verify license → optimize into `images/` → repoint `photo:`) when Wikimedia is reachable.
**Section-10 sweep result:** both findings (thighs card, Jun-4 tank log) were DROPPED by Cyrus on Jun 6 — never build.
**Verified:** node --check (data.js / sw.js / extracted inline / concat), harness2 — alarm pin top, NO group headers, full chronological order asserted, sink-to-bottom (breakfast checked → below lights-out, pile divider, `.flow-just-done` present), Zoey day-5=50/50 + all 4 phases + stool note + hold + complete/retire states, Monday rest passive, 3 dog cards after humans, dog-recipe note complete, full per-tab render pass. Push verified: origin blobs byte-identical ×3, live 200 with new stamp + flow/Zoey markers, r_zoey_* in served data.js, hg-v17 served, House mirror byte-identical ×3.

### ✅ LIVE — Today rhythm redesign (Jun 6 2026, commit `2efc271`, pushed `fd1302e..2efc271`)
**Executed start-to-finish in a fresh chat on the recorded GO LIVE.** Fresh clone of `fd1302e`, all edits in the sandbox, ONE commit.
**What changed:**
- **`data.js`:** new editable **`const RHYTHM = {...}`** at the TOP of the file (right above `const ROOMS`) — breakfast 8:00 AM, lunch 12:30 PM, dinner 6:30 PM, dinnerBy 8:00 PM, windDown 9:30 PM, reading 9:30–10:30 PM, lightsOut 10:30 PM. **Edit bed/meal times THERE.**
- **`index.html` — `renderToday()` fully rewritten (decluttered):** order = ⏰ "Get an alarm clock" pinned **#1** (until checked; id `get_alarm_clock`, in NO_PROGRESS) → Call DT (streak kept) → `wxAlerts()` → compact "still open" pins (Fix Smart Start + the 2 regrow projects, steps collapsible — all 3 still unchecked per the Jun-5 Firebase backup, so kept compact instead of dropped) → 🏡 **Today's One Thing** (one task card via `planCard`, room mini-progress; Monday = 🛋️ rest-day card) → 💊 Daily essentials (Litfulo via `toggleWellness('litfulo')` so the Health streak still counts, Zoey walk, garden-if-due) → 🍽️ Meal rhythm (RHYTHM times, "Eaten by 8:00 PM — house rule") → 🌙 Evening wind-down (3 per-day checkables: phone-to-other-room / reading hour / lights out; keys `evening-wind-{phone,read,lights}-<date>` — reset daily) → slim "Today's rhythm n of N" bar + a calm "Day complete" card when everything is done. The old habit wall (bed/blinds/pushups/tips/sleep-position/etc), TIPS, freeze-watch, and the agenda-bucket flow are GONE from Today.
- **One-task engine:** new pure `oneTaskForDate(dateStr,isDone)` + `anyHouseTaskLeft(isDone)` + `RHYTHM_ROOM_ORDER` (kitchen→living→bathroom→bedroom→office→garage→backyard), inserted just above `todaysPlan()`. Mon=null, Tue–Fri=easy, Sat/Sun=moderate-then-hard; stays in the first unfinished room; falls back to easiest in that room if no level match (garage/backyard have no easy tasks). Skips NO_PROGRESS + counts taskCustom.
- **`renderTimeline()` rewritten** to project with the SAME engine (sim done-set walking real dates): one task rows + 🛋️ rest-day rows for Mondays, "X task days · one task a day, Mondays off · finish by <date>". At deploy: 64 task days + 11 rest rows.
- **Grocery:** `groc-alarmclock` "⏰ Alarm clock" added to `GROC_BUILTIN` + `clearGroc()` reset list.
- **Housekeeping:** `data.js?v=20260606070549`, SW cache `hg-v15`→`hg-v16`, LAST_DEPLOY "Jun 6, 2026 · 1:05 AM MT". `todaysPlan`/`agendaHtml`/SCHEDULE left in place (unused by Today now — safe dead code).
**Verified:** node --check on data.js/sw.js/extracted inline + concat (no dup consts); stubbed-DOM harness — Sat=moderate kitchen / Tue=easy kitchen / Mon=rest (date mocks), alarm card above Call DT, meals+wind-down+essentials render, old wall absent, Timeline rows == 64 remaining tasks, full per-tab render pass (Today/Timeline/Rooms/Garden/Shopping/Grocery/Print/Wellness/renderAll), alarm clock present in grocery. Pushed, **origin blobs byte-identical** to local for all 3 files, live serve 200 with new stamp + `oneTaskForDate` + wind-down markers, RHYTHM in served data.js, hg-v16 in served sw.js, **House mirror byte-identical**. Kitchen inventory, RECIPES, per-tank tabs, weather watering, PWA all confirmed present in the served blobs.
**Shows on device after one app reopen** (network-first HTML + SW bump).

### ✅ EXECUTED Jun 6 (see LIVE entry above) — was: 🔴 HANDOFF — Today-tab "RHYTHM" REDESIGN, approved + GO LIVE already given (Jun 5 2026)
**Cyrus approved this and said GO LIVE. A fresh chat should execute it start-to-finish. Remote HEAD at handoff time: `fd1302e` (other chats deploy too — ALWAYS fresh-clone and build on CURRENT files).**

**THE SPEC (verbatim intent):** Today feels overwhelming. Make it calm, simple, attractive, encouraging.
1. **DECLUTTER TODAY** — strip to a minimal flow: the day's ONE house task, the meal rhythm, the evening wind-down block, plus only true essentials (Call DT, meds/Litfulo, dog). Kill the wall of cards. Gentle language, small visible progress, satisfying completion feedback. Less is the point.
2. **ONE-TASK-PER-DAY pace:** Weekdays except Monday = one EASY task; **Monday = rest day (no house task)**; Sat+Sun = one MEDIUM-to-HARD task. Rotation starts with KITCHEN then room-by-room toward 100% as before, just calmer. Timeline/projections must reflect the new pace.
3. **MEAL RHYTHM** on Today at consistent times: breakfast, lunch, dinner — hard rule **DINNER BEFORE 8PM** (schedule ~6:30–7pm, "eaten by 8" cue).
4. **EVENING WIND-DOWN block:** 1 hr before bed = NO phone/TV; nightly READING block fills that hour; **phone sleeps in another room** (nightly checklist item). Defaults (put in data.js as an easily-editable config + tell Cyrus where): wind-down/phone-away 9:30pm, reading 9:30–10:30pm, lights out 10:30pm.
5. **#1 PRIORITY pinned at very top of Today until checked: "Get an alarm clock"** (enables the phone leaving the bedroom). Also add it to the grocery list.
Preserve existing features (inventory, recipes, per-tank tabs). Tonight-project cards can retire if done.

**IMPLEMENTATION MAP (current code locations, verified Jun 5):**
- `renderToday()` ~L1094 in index.html — the rewrite target. Pinned cards live in `topHtml` (Call DT card first, then `wxAlerts()`, Fix Smart Start, NIGHT_PROJECTS forEach ~L1137). Keep Call DT; alarm-clock card goes ABOVE it (it's priority #1). Pinned-until-done pattern: `if(!checked[id])` + `onclick="toggle('id')"`; add new ids to `NO_PROGRESS` (~L772) so they don't skew progress.
- One-task engine: current scheduler = `todaysPlan()` + `SCHEDULE` (46 dated entries in data.js) + `autoCarryOverdue()`. New pace: pick ONE task/day from uncompleted `TASKS` (kitchen first — room order kitchen→living→bathroom→bedroom→office→garage→backyard), filtered by `level` ("easy" weekday / "moderate|hard" weekend) and day-of-week (Mon=none). Suggest a pure helper `oneTaskForDate(dateStr)` so Timeline projections (`renderTimeline` ~L1296) can call the same engine for future dates instead of SCHEDULE.
- Meal/wind-down config → data.js as e.g. `const RHYTHM={breakfast:"8:00 AM",lunch:"12:30 PM",dinner:"6:30 PM",dinnerBy:"8:00 PM",windDown:"9:30 PM",reading:"9:30–10:30 PM",lightsOut:"10:30 PM"}` (data.js is the editable home; tell Cyrus it's there).
- Alarm clock → grocery: add `{id:'groc-alarmclock',label:'⏰ Alarm clock'}` to `GROC_BUILTIN` (~L653 index.html).
- Nightly checklist (phone in other room / reading / lights out) — per-day checkable like the Call DT pattern (`laundry["calldt-"+today]` style per-date keys, e.g. `checked["wind-"+todayKey()+"-phone"]`) so it resets daily.
- **HTML hygiene:** no raw inch marks, balanced braces, no duplicate consts; node --check data.js + extracted inline after EVERY change.
- **Verify before push (gate):** stubbed-DOM harness — render Today (alarm card top, one task only, meals, wind-down), check Mon=rest/weekday=easy/weekend=hard via date mocks, Timeline renders with new pace; then full per-tab render pass (the harness pattern from this chat: concatenate data.js + inline in ONE vm context, stub document/firebase).
- **Deploy:** fresh clone → edit in sandbox → node --check → restamp `LAST_DEPLOY` (sed `// DEPLOY_STAMP`) → bump `data.js?v=` if data.js changed → bump SW `hg-v16` (was v15) → ONE commit → push via `.deploy-token` (mask output) → verify origin/main blobs byte-identical + served markers (new Today structure, one-task engine, wind-down block, alarm-clock priority) → mirror index.html/data.js/sw.js to House.

### ✅ LIVE — Tank/print cleanup + new buys (Jun 4 2026)
**Built on latest live (had Aquarium chat's changes).** Changes in `index.html`:
- **Removed** the whole bug shopping block from Tank → To-Do (Bug Starter Kit, Petco sourcing, Aphid+Ladybug culture list, Shared buy-once, and the Auto Fly Feeder) — all completed/scratched (Cyrus has the ladybugs + cultures; spider/katydids gone).
- **Moss ball** added to Tank → To-Do shopping: `buy('shop-mossball','Petco', …Java/Christmas moss…)`. Note: Cyrus has 2 CO₂ tabs in the substrate, so moss/plants will do well.
- **Print tab:** Active Print → "🌱 Desktop Hydroponics" (Droplet Watering Can ×2 marked done). Added two buy check-offs: `buy_flex_filament` (flexible/TPU filament) and `buy_sub_pump` (tiny submersible pump for the hydroponics).
**Tank state logged (not all wired into app yet — Aquarium chat owns the status panels):** biggest tank got all decorations EXCEPT the big fake dead-wood stump (held back) + brown gravel started; small divided tank not cleaned yet; TopFin tall tank filled & leak/strength-testing; shrimp tank temp fine, 0 deaths, ammonia down to 1 ppm after water change, shrimp active + eating; 2 CO₂ tabs in substrate.
**Moss rec:** Java moss (foolproof) or Christmas moss (fuller). Skip HC "Cuba" (carpet plant, not moss). Marimo balls are algae, different thing.
**Deploy:** `node --check` PASS → SW bump → push → mirror.

### ⛔ DROPPED by user decision (Jun 6) — never build — was: 📋 LOGGED — Tank state update, Jun 4 2026
- **Biggest tank:** added all underwater decorations EXCEPT the big fake dead-wood stump (natural death, not cut — held back for now). Brown gravel laid on the bottom as a starter substrate.
- **Big fake dead-wood stump:** not placed yet.
- **Small divided tank** (divider down the middle): not cleaned yet — still on the list.
- **TopFin tall tank:** filled with water, currently leak/strength-testing it.
- **Shrimp tank:** temp under control (non-factor); 0 dead shrimp; ammonia down to **1 ppm after a water change**; shrimp very active and eating.
- **Plan:** buy a weighted moss ball; choosing a moss type — does NOT want HC "Cuba" again. (Claude recommended Java moss / Christmas moss — easy, no CO2.)
- Jun 6: Cyrus dropped this — do not wire into the app.

### ✅ LIVE — Bug project pivot: ladybugs in / spider + katydids out (6/4, commit `a8b2a09`)
**What changed in `index.html`:** (1) NIGHT_PROJECTS `proj_ladybugs` + `proj_bioactive` rewritten — ladybug care now reflects 3 ladybugs in the sealed bioactive tub (feed soaked raisin / honey-water, mist daily, no open water) plus the rest released to the garden (cleared jalapeño aphids); bioactive tub = moist coco + full isopod/springtail cultures + 3 petrified-wood rocks + red cholla skeleton, sealed with poked air-vent holes. (2) Tank cards: #2 (rounded/cracked) → "Bioactive cleanup tub" (was jumping spider); #3 (tall) → "Empty / open" (katydid display scratched). (3) Removed the bugP "Jumping Spider Condo" + "Herbivore Display (katydids)" shopping sections; de-spidered the Aphid+Ladybug and "Shared" sections. (4) Setup checklists: #2 → bioactive tub steps; #3 → empty/scratched. (5) Future Plans, Bug-Starter-Kit blurb, and Auto-Fly-Feeder title updated to mark spider/katydids scratched and the feeder shelved.
**Why:** Cyrus reported the katydids vanished when released and the jumping spider got loose and is missing — both ideas scratched. Ladybugs went into the new bioactive tub + the garden.
**Validated:** `node --check` PASS; remaining katydid/spider strings are only the "scratched/gone/shelved" notes.
**Deploy steps used:** fresh clone of live → edits → SW bumped hg-v7 → `node --check` → push (token masked) → mirror index.html + sw.js to House. Shows on device after one app reopen.

**📌 Current state (June 3, 2026 · live `7f637d7`):** App is live at mrosyrus21.github.io/home — **Today** (Call DT + Fix Smart Start + auto weather alerts + routine/room plan), **Plants** (Watering/Harvest/Care, real photos, storm-aftermath banner, **weather-driven watering**), **Tank** (5-tank System dashboard + To-Do/Setup/Params/Log), **List** (grocery), **Print** (To-Do/Wishlist/Upgrades — active print = Droplet Watering Can, flexi queue), **Health** (**pain log now Firebase-synced + Litfulo refill countdown**). **Roadmap items 1–8 all done.** App data now lives in a same-origin `data.js` (loaded before the inline script). Deploys are friction-free via the `.deploy-token` file. Entries below are the chronological change log, newest first — skim the top few; the rest is historical record.

### ✅ LIVE — Roadmap items 6, 7, 8 (6/3, head `7f637d7`; pushed `64afb2f..7f637d7`)
Done autonomously in a dedicated chat, exhaustively verified, deployed on the standing "finish it" authorization. Remote HEAD==origin/main==`7f637d7`, all 5 changed files' git blobs byte-identical to the locally-verified files, live `index.html`+`data.js` serve 200, served `index.html` blob carries the `<script src="./data.js">` tag (line 620) + new deploy stamp, served `data.js` blob resolves all 9 data objects, House mirror byte-identical.

- **Item 6 — BIG REFACTOR: data → `data.js` (the riskiest item, now LIVE).** Extracted the 9 top-level data objects — **ROOMS, TASKS, SCHEDULE, PLANTS, PLANT_INFO, WATER_INFO, FUN_FACTS, CARE_INFO, HARVEST_INFO** (old `index.html` lines 634–980) — into a new same-origin **`data.js`**, loaded by `<script src="./data.js"></script>` inserted immediately **before** the main inline `<script>` (now ~line 620). Classic scripts share one global lexical environment, so `data.js`'s top-level `const`s stay visible to the inline script — confirmed at runtime, not assumed. **Kept in `index.html`:** `CONFIG`, `EYEBROWS`, and **`LAST_DEPLOY`** (the deploy stamp regex still targets `index.html`, so it must stay there). In `index.html` the extracted block is replaced by a one-line marker comment `// ── APP DATA moved to data.js …`. `index.html` shrank 277,262 → ~214,117 B; `data.js` is ~64 KB. **sw.js:** added `'./data.js'` to the precache SHELL and bumped the cache name `hg-v1`→`hg-v2` so installed clients refetch the new shell. **deploy-github.py:** added `"data.js"` to the staged-files list. **Backup:** pre-refactor `index.html` saved in the sandbox before any edit. **VERIFICATION (exhaustive):** (1) `node --check` on `data.js`, `sw.js`, and the extracted inline script — all PASS; (2) concatenated `data.js`+inline `node --check` — PASS (proves no duplicate-`const` redeclaration across the two scripts); (3) `vm` load of `data.js` resolves every object with counts ROOMS 9 / TASKS 88 / SCHEDULE 46 / PLANTS 21 / PLANT_INFO 21 / WATER_INFO 21 / FUN_FACTS 21 / CARE_INFO 21 / HARVEST_INFO 13 — **identical to the pre-refactor backup's counts**, so nothing was lost; (4) confirmed none of the 9 consts are still declared in `index.html`; (5) a stubbed-DOM/Firebase harness ran `data.js`+inline as ONE shared scope (faithfully modeling the browser's shared global lexical environment) and **invoked every tab renderer** — `renderToday` (32.6 KB), `renderGarden` + all 3 sub-tabs Watering/Harvest/Care, `renderRooms`, `renderTimeline`, `renderForecast`, `renderShopping` + all 5 Tank sub-tabs (System/To-Do/Setup/Params/Log), `renderGrocery`, `renderPrint`, `renderWellness`, and `renderAll` — all produced HTML with no ReferenceError, proving every tab's data source still resolves from `data.js`. **To revert:** paste `data.js`'s contents back in place of the marker comment in `index.html`, delete the `<script src>` line, drop `data.js` from sw.js SHELL + deploy stage list. *Editing plant/task/room data now happens in `data.js`, not `index.html`.*
- **Item 7 — FULL PHOTO RE-MAP: verified already-correct; nothing re-wired (intentionally).** Built labeled contact sheets of all 20 distinct `plants/_archive_old/` originals (one dup: `greenhouse__curled_parsley` == `indoor__heartleaf_philodendron`; `outdoor__husky_cherry_tomato.jpg` is CORRUPT/truncated) and independently identified each by sight + the legible nursery tags. The `_archive_old` **filenames** are badly scrambled (e.g. `heartleaf_philodendron`=a tomato, `raspberry`=a daisy, `daisy`=rosemary by its tag, `dianthus`=curled parsley by its tag, `basil_2`=dill, `dill`=basil, `candytuft`/`peppermint`=mint, the `dianthus`/`candytuft` flower photos are swapped), **but the LIVE images were already wired by sight in prior batches and every one is correct.** Verified all 21 plants' live photos show the right species: philodendron, ivy, **croton (the strappy yellow-green leaves ARE a banana 'Banana' croton — Cyrus-confirmed)**, jade, fittonia, basil1, basil2 (both real basil), parsley (curled), mint, dill, rosemary, strawberry, strawberry_pot, tomato (heirloom, fruiting), cherry_tomato (husky), jalapeño, raspberry, dianthus, daisy, candytuft. **Conclusion: no mismatch to fix and no confident new match that beats the current photo, so re-wiring was deliberately skipped** (it could only regress confirmed-correct images). No `data.js`/`index.html` change for item 7. **Archive photos not used / not identifiable to a *specific* id:** the 4 redundant tomato photos (can't reliably split heirloom vs husky-cherry, and both already have a confident photo); the corrupt `outdoor__husky_cherry_tomato.jpg`; the second copies of mint/rosemary/pepper. No plant lacks a correct photo.
- **Item 8 — PHOTO DROP-IN auto-wiring: new tool `wire-photos.py` (repo root, LIVE).** Drop a plant photo into `home-and-garden-project/plants/<indoor|outdoor|greenhouse>/` named after the plant (e.g. `jade.jpg`, `heirloom_tomato.jpg`, `peppermint.jpg`) and run the script; for each photo it can confidently match to a known plant id it (1) web-optimizes → `images/<id>_<YYYYMMDD>.jpg` (EXIF-upright, ≤1200 px long edge, q85), (2) repoints `PLANT_INFO["<id>"].photo` in `data.js`, (3) moves the original into `plants/_archive_old/`. **Safety:** dry-run by DEFAULT (needs `--apply` to write/edit/move); the valid-id list is parsed live from `data.js` PLANTS so it always matches the app; a built-in alias table maps common filename variants (peppermint→mint, heirloom_tomato→tomato, husky→cherry_tomato, nerve→fittonia, etc.); unrecognized filenames are listed and left untouched (never guesses); plants whose card hard-codes its image (**strawberry, strawberry_pot**) are skipped with a warning (edit those in `index.html`); idempotent (only acts on files currently in the subfolders). **Tested** end-to-end on a throwaway `data.js` copy: correct id/alias resolution, hardcoded-card skip, unknown-file skip, ≤1200 px EXIF-upright optimization, correct `data.js` repoint (then `node --check` PASS, PLANTS still 21), original archived, dry-run makes no changes, re-run is idempotent. **Usage from the deploy clone:** `python3 wire-photos.py --plants "<session>/mnt/House/home-and-garden-project/plants" --apply` → `node --check data.js` → review diff → `git add data.js images` → commit + push → mirror to House. (Not auto-run during normal deploys; it's an explicit maintenance step.)

**Deploy mechanics used (this push):** fresh sandbox clone of `64afb2f` → all edits in the sandbox via python (never the OneDrive mount) → `node --check` + the full render harness as the deploy gate → restamp `LAST_DEPLOY` (`Jun 3, 2026 · 8:18 AM MT`) → `git add data.js index.html sw.js deploy-github.py wire-photos.py` → commit → `git push origin main` (token from `.deploy-token`, masked) → verified HEAD==remote + per-file blob identity + live 200 + served-blob markers + served-`data.js` object resolution + House mirror byte-identical. **Live HEAD `7f637d7`.**


### ✅ LIVE — Roadmap items 1–5 (6/2, head commit `64afb2f`; pushed `e453643..64afb2f`)
Built in a fresh continuation chat, one at a time, each node-checked + functionally tested, then all deployed together on "GO LIVE." HEAD==origin/main verified, live returns 200, unique deploy stamp present in served file, all five feature markers confirmed in the served git blob, House mirror byte-identical (277,262 B).
- **Item 1 — Pain log → Firebase (commit `df5728f`):** added `let painLog={}` global + `savePain()` (`ST.child('painLog').set`); `painLog=d.painLog||{}` in the ST.on load; `logPain()` now writes Firebase instead of localStorage; `renderWellness` reads the `painLog` global (the 7-day strip + history now sync across devices). Includes a one-time idempotent migration that lifts any legacy `localStorage.painlog` entries up to Firebase on load.
- **Item 2 — Weather-driven watering (commit `329b96d`):** `plantsDueOn(dateStr, ignoreWx)` is now weather-aware **for today only** (future calendar dates unaffected): outdoor plants get `need+=2` on `WXF.rainToday` (rain auto-skips them) and outdoor/greenhouse get `need-=1` on `WXF.hot` (heat brings watering forward). New `rainDeferredPlants()` helper; the Watering tab shows a "🌧️ Rain (x%) is covering N plants — auto-skipped" banner + a per-card "🌧️ Rain's got it" badge. The old inline rain override line was removed. Tested: normal vs rain (outdoor skipped, greenhouse still due) vs hot (all bumped) vs future-date (weather ignored) — all correct.
- **Item 3 — Litfulo refill countdown (commit `6cea35c`):** added `let litfulo={}` + `saveLit()` + `litfulo=d.litfulo||{}` load; helpers `logRefill(n)` / `clearRefill()` / `litRefillHtml()`; injected `out+=litRefillHtml()` right under the Litfulo med card. Model: stores `{startDate, startDoses, reorderAt:7}` in Firebase; computes doses-left = startDoses − days-elapsed (one/day) and a reorder-by date = startDate + (startDoses − 7) days, with green/amber/red urgency + progress bar. When unset, shows "Log 30-day / 90-day refill" buttons. Tested unset / fresh-30 / day-25 (amber, reorder now) / day-31 (out) — all correct.
- **Item 4 — Evening check-in (scheduled task `garden-evening-checkin`, 8pm daily):** no index.html change. The evening mirror of the 7am garden-weather-check: flags the day's unchecked anchors (Call DT, water/vitamins, **log Litfulo dose + glance at refill countdown**, **log pain**), then previews TOMORROW's Denver weather → tonight prep, plus hail-recovery (through ~Jun 8) + peppermint-hold context. Signs off with the app link.
- **Item 5 — Auto-backup (scheduled task `garden-state-autobackup`, 11pm daily):** no index.html change. Fetches the app's public Firebase REST state (`https://schedule-c6dc7-default-rtdb.firebaseio.com/state.json`) via web_fetch and writes it verbatim to `home-and-garden-project/backups/state-<YYYY-MM-DD>.json`, with a parse/expected-keys sanity check (never overwrites a good backup with an empty/`null` fetch) and 30-day pruning. An initial proven backup `backups/state-2026-06-02.json` (5,258 B, valid) is already in place.

**Deploy mechanics used (reusable):** fresh sandbox clone of the repo → all edits made in the sandbox via python (never the OneDrive mount, to dodge the ~140 KB truncation cap) → extract inline `<script>` + `node --check` after every change → restamp `LAST_DEPLOY` via the `// DEPLOY_STAMP` sed → `git add index.html` + commit → `git push origin main` (token from `.deploy-token`, output masked) → verify HEAD==remote + live 200 + served stamp + git-blob markers → `cp` index.html into House.

**Roadmap status:** items 1–5 ✅ LIVE. Item 6 (big refactor) intentionally deferred to its own dedicated chat (highest risk). Items 7–8 await Cyrus's photo IDs.

### ✅ LIVE — Plant photos finished + PWA install/offline (6/2)
**Photos (commits `81b42c6`, `8e61bd8`, `dfe21c4`):** fittonia wired to `images/fittonia_20260531.jpg` (real nerve plant); banana croton wired to `images/croton_20260531.jpg` (it was the photo mislabeled "strawberry"); husky cherry tomato corrected — Cyrus confirmed the mapping, root cause was browser CACHE (same filename reused), fixed by renaming to `images/cherry_tomato_20260602.jpg` and repointing `PLANT_INFO.cherry_tomato`. All three live-verified loading. Source `plants/` folder reorganized: indoor/outdoor/greenhouse now hold only the 8 Cyrus-confirmed correctly-named photos; all 22 scrambled originals preserved in `plants/_archive_old/` (drive blocks delete from sandbox — can only move). Junk left for Cyrus to trash: House/_contact_sheet*.jpg, House/_icon_preview.png, plants/_archive_old/*.
**PWA (commit `e453643`):** added `manifest.webmanifest` (name Home & Garden, standalone, theme #06050A, sprout icons), `sw.js` (network-first for HTML so deploys show, stale-while-revalidate for same-origin static, NEVER intercepts cross-origin so Firebase/fonts/wikimedia are untouched), `icons/icon-192.png` + `icon-512.png`, head meta (theme-color, apple-mobile-web-app-*, apple-touch-icon) + SW registration before </body>. Also added manifest.webmanifest/sw.js/icons to `deploy-github.py` stage list so local deploys include them. Live-verified: all four files serve 200 w/ correct MIME; SW registered, scope https://mrosyrus21.github.io/home/.

**"DO EVERYTHING" ROADMAP — status:**
1. ✅ DONE/LIVE (`df5728f`) — Pain log → Firebase sync.
2. ✅ DONE/LIVE (`329b96d`) — Weather-driven watering.
3. ✅ DONE/LIVE (`6cea35c`) — Litfulo refill countdown.
4. ✅ DONE — Evening check-in scheduled task (`garden-evening-checkin`, 8pm daily; not an index.html change).
5. ✅ DONE — Auto-backup scheduled task (`garden-state-autobackup